# 教訓歸檔（applied）

> `/ps-lesson` 直接套用的教訓逐筆記錄於此（症狀／根因／落點／實際修改摘要）。
> 本機套用即生效；**團隊生效以內部 git PR merge 為準**（SOP-1）。
> 本檔是歷史紀錄，**不載入任何 agent 的 context**。

### L0 純 prose 規則對小模型效力最弱（2026-07-24）
- 教訓：本專案調校期間效果最好的修復全是機械化的
  （tools deny、UUID 格式判定、行號覆蓋檢查），效果最弱的全是純 prose
  規則——promotion 時永遠先問「這條能不能機械化」。
- 落點：pending.md 的落點分類優先序即由此而來。
- 套用：設計原則，無單一 commit。

### L1 新註冊的 MCP server 預設全開，繞過 subagent 架構（2026-07-27）
- 症狀：主 agent 畫面直接出現 `PeoplecodeMe(e)tadata_find_field_usage`
  呼叫，未經委派；主 context 被工具結果灌大。
- 根因：OpenCode tools map 是**覆寫表**（沒列＝預設開）。agent 檔只
  deny 已知三個 server，新註冊的第四個不在名單上 → 九個 agent 全部
  預設可用。附帶風險：其回傳塞不進 CHUNK/SQL 證據契約，會再度誘發
  捏造證據（同 SQL-XLAT-1 型）。
- 落點：機械化（tools deny）——9 個 agent 檔全加
  `"PeoplecodeMetadata_*": false` 與 `"PeoplecodeMeetadata_*": false`
  （確切拼法待管理者確認，多列無害）；SOP-8 加「新增 MCP 先全面
  deny」檢查項；test-scenarios 加 F4。
- 套用：本 commit；後續正式整合（預定歸 ps-metadata-flow）待管理者
  提供該 server 的確切註冊名與完整工具清單。
- 後續（2026-07-27）：管理者確認註冊名為 `PeoplecodeMetadata`（單 e）→
  已移除九檔的 `PeoplecodeMeetadata_*` 備援項；正式整合仍待完整工具清單。
- 正式整合（2026-07-27）：工具＝`find_field_usage`（fieldName／pageName／
  componentName）與 `search_component_metadata`（keyword）。開放給
  ps-ui-flow／ps-metadata-flow（定位優先步驟）與 ps-auditor（任務 C
  反查角度）；**定位線索不作 evidence**（契約仍僅 CHUNK／SQL 兩種），
  其餘 agent 維持 deny。測試情境加 F5。
- 補強（2026-07-27）：自製索引不保證完整 → 空／稀少結果不得當
  「不存在」證據，必回退 cookbook／ES 正規管道；auditor 任務 C 需
  兩角度交叉才可列疑似遺漏；F5 加對應檢查點。
- 追記（2026-07-27）：server 第三個 tool `get_ae_sql_metadata`
  （aeApplid）→ 開放 ps-ae-flow（AE 結構定位優先）與 ps-metadata-flow
  （批次血緣鑽查）；同樣定位不作證、空結果必回退。
- 修正（2026-07-27）：管理者澄清**查詢鍵只有「欄位名」與「Component
  關鍵字」**（AE tool 只吃 AE 名）——Page／Record／選單名帶入必查空。
  原文件誤把 pageName／componentName 列為 find_field_usage 可查參數，
  已全數改正；ui／metadata-flow 與 auditor 加「輸入類型限定」硬規則
  （帶錯類型＝方法錯誤，不是「不存在」，改走 cookbook §2／§6 對映），
  F5 加對應檢查點。

### L2 write 工具呼叫 JSON 被截斷——大檔整檔覆寫超出小模型可靠輸出長度（2026-07-27）
- 症狀：持續出現 invalid[tool=write, error=... JSON Parse error
  expected '}']，寫入反覆失敗。
- 根因：整檔覆寫讓單次工具呼叫 JSON 隨檔案長大而變長，超過小模型
  可靠輸出長度即被截斷、JSON 沒閉合。與先前的三反引號圍欄汙染不同源。
- 落點：機械化——(1) 進度／Gaps 拆到獨立小檔 checklist.md，
  00-overview.md 階段一後凍結（agent／command／模板／lint／SOP-6
  同步改，舊領域自動遷移）；(2) 單次寫檔約 150 行上限、超過拆續篇；
  (3) 新增 SOP-9 服務端排查（輸出 token 上限、tool-call 約束解碼）。
- 套用：本 commit；測試情境 G1／H1 對齊、G2 加「00-overview 零改動」
  檢查點。

### L3 稽核只寫記分卡、跳過回灌——交付物完成後尾端步驟被丟（2026-07-27）
- 症狀：/ps-audit 產出 90-audit.md（PASS 8／FAIL 2／PARTIAL 2）後即
  結束，checklist 沒有任何 A<n> 補查行；且模型自創契約外狀態
  partial_pass。
- 根因：小模型寫完主要交付物即視為任務完成，排在其後的步驟（回灌、
  自檢）被靜默丟棄——與「複述計畫就停」同族。
- 落點：機械化順序——回灌**先於**記分卡（非 PASS ≥ 1 而 checklist
  無新 A 行＝禁止寫 90-audit.md）；回灌對象改「任何非 PASS 判定」
  （網住自創狀態）；命令加結束前 read checklist.md 自檢；模板加
  順序約束註記；H1 改致命檢查點＋狀態詞彙檢查。
- 套用：本 commit（ps-deep-research／ps-audit 命令／audit-template／
  test-scenarios H1）。
- 後續確認（2026-07-27）：舊版 90-audit 的「已回灌」節其實列好了
  5 個 A 項——模型**在報告裡聲稱已回灌，實際檔案一行未寫**
  （填表代替做事：模板有該節，就把它當一般章節填完）。遷移時已把
  這 5 項搬進 checklist.md。新順序（checklist 先寫、報告節只准抄錄
  實際行）＋結束前 read 自檢即針對此機制。

### L4 第二輪稽核沒重驗——記分卡數字原封不動（2026-07-27）
- 症狀：消化完 A 項、全數打勾後結束，90-audit.md 與上一輪完全相同
  （自創章節與 partial_pass 原樣保留）＝報告未重寫。
- 根因：(1)「總計 2 輪上限」**沒有持久化的輪次可查**——跨 session
  模型看到 90-audit.md 已存在，即可判定「稽核做過了」跳過；
  (2) 勾完 A 項「感覺完成」，收尾自動稽核是尾端步驟再度被丟
  （L3 同族）；(3) 沒有「禁止沿用舊報告」規則，就算重寫也可能抄舊數字。
- 落點：機械化——(1) 觸發改「本 run 勾過 A 項或全勾且未稽核 → 必須
  稽核」，明文「90-audit.md 已存在不是跳過理由」；(2) 輪次持久化：
  checklist.md 記「稽核輪次：N」，每輪 +1 並寫進報告表頭（新鮮度
  肉眼可驗）；(3) 上限改「單次 run 最多一輪」，不需跨 session 計數；
  (4) 判定只准來自本輪 auditor 回報，禁止 read 舊 90-audit.md；
  (5) H1 加致命檢查點（輪次未 +1＝抄舊帳）；lint 警缺輪次表頭。
- 套用：本 commit（ps-deep-research／兩個命令／兩個模板／README／
  test-scenarios H1／lint）。

### L5 首輪全量稽核量大：回灌改以檔彙整；自創判定詞就近映射（2026-07-27）
- 症狀：輪次 1 真實重驗產出 84 筆證據判定（58 PASS／10 FAIL／
  16 UNVERIFIABLE）＋48 筆 claim 判定（19 VERIFIED／29 筆用自創詞
  weakened/contradicted）。非 PASS 共 55 筆——逐筆回灌會塞爆
  checklist.md（回到大檔覆寫→JSON 截斷的老問題）；自創詞再現
  （堵不完，改吸收）。
- 落點：機械化——(1) 回灌單位改「一檔一行」彙整（上限＝檔案數），
  禁止逐筆開項；(2) 自創判定詞**就近映射**（claim 層→DISPUTED、
  證據層→FAIL），lint 擴充詞彙警告；(3) A 項處理規則明確化：FAIL
  重取證、DISPUTED 二選一（補證據保 CONFIRMED 或降級 INFERRED，
  不得原樣保留）、UNVERIFIABLE 重驗一次不迴圈。
- 套用：本 commit。
- 解讀備忘：首輪 69% 證據存活率屬正常起點；29 筆 DISPUTED 中預期
  相當比例是 9B 稽核員把「找不到支持」誤標為「矛盾」——定向補查後
  會翻回或正確降級。指標看輪次間趨勢，不看單輪絕對值。
- 追記（2026-07-27）：輪次 1 報告 16 筆 UNVERIFIABLE **全無明細與
  原因**——根因是模板缺陷：明細節標題只寫「FAIL / DISPUTED」，9B
  照格子填表，沒開格子就不寫。模板改「FAIL / DISPUTED / UNVERIFIABLE
  明細」＋範例列＋「每筆一列、原因逐字取自 auditor 回報」；step 4 與
  H1 加對應要求。原因欄是判斷環境問題（逾時類）vs 資料問題的依據。

### L6 認知修正：目標模型實為 Qwen3.6-35B-A3B（262K），非 Qwen 3.5 9B（2026-07-27）
- 事實：管理者確認地端模型為 Qwen3.6-35B-A3B（262K context）。依
  Qwen 命名慣例，A3B＝MoE 每 token 活躍參數約 3B——知識廣度屬 35B
  級、**程序紀律屬 ~3B 小模型級**，與觀察到的失敗光譜完全一致
  （內容會寫、程序會掉：唸而不做、填表代做、丟尾端步驟、自創詞、
  長 JSON 截斷）。
- 影響評估：**全部機械化規則維持不變**——每一條都源自本模型實際
  犯過的錯（非 9B 規格推測），對更強模型亦無害。
- 本檔先前條目中的「9B／小模型」字樣一律實指本模型；歷史紀錄
  不回改（作廢不刪除原則）。
- 待查環境疑點：(1) 早期「context 很快用滿」與 262K 不符——檢查
  serving 端 context 上限（如 num_ctx 類設定）是否遠小於 262K；
  (2) write JSON 截斷屬「輸出 token 上限」問題，與 context 無關，
  SOP-9 兩槓桿（加大輸出上限＋約束解碼）仍為最高價值環境修法。
- 套用：test-scenarios／README 的型號字樣已更正；原始需求 spec 與
  lessons 舊條目保留原樣。

### L7 輪次 2 報告被「覆核上輪判定」取代——想寫的東西沒格子就掀桌（2026-07-27）
- 症狀：輪次 2 機制全對（輪次 +1、回灌先行），但 90-audit.md 記分卡
  被「Auditor 正確 5／部分不可查 2／Auditor 有誤 4」取代——模型整輪
  在處理 A 項，就把「上輪判定 vs 我的覆核」寫成整份報告，全量記分卡
  消失。
- 價值發現：覆核數據本身很有用——**稽核員誤報率 4/11 ≈ 36%**，證實
  DISPUTED 灌水假說；文件真實缺陷 ≈ 5，非 55。
- 落點：模板加「上輪回灌項覆核」節（屬實／誤報／不可查，第 2 輪起
  必填）**給它格子**；同時明文「該節不取代記分卡；記分卡＝本輪全量
  重驗數字」「稽核範圍＝全部已打勾項」；H1 加致命檢查點（僅覆核無
  全量＝不合格）；lint 章節清單同步。
- 原則收錄：模板沒開的格子它不寫；想寫的沒格子它就換掉整張表——
  **兩個方向都要開格子**。
- 套用：本 commit。

### L8 task 委派 JSON 截斷——整份檔案被貼進委派 prompt（2026-07-27）
- 症狀：invalid[tool=task, json parsing failed]，出現於寫完領域
  markdown 之後（打勾前快驗附近）。
- 根因：與 L2 同族（單次輸出過長被截斷）——模型剛寫完檔，順手把
  整份內容貼進給 auditor 的委派 prompt。auditor 本就有 read 權限
  （任務 A 第一步即「Read 目標檔」），委派只需要檔案路徑。
- 落點：機械化——委派瘦身規則（prompt 約 30 行上限；禁貼檔案／
  報告全文；驗檔委派只傳路徑）；task 失敗 → 縮短重委派、失敗 2 次
  標 ⚠ 跳過；SOP-9 補條目；新增 F6 測試情境（總數 37）。
- 套用：本 commit。
- 探針結果（2026-07-27）：輸出上限探針**能完整數到 3000**（輸出
  ≥ 約 8K tokens）——輸出 token 上限**不是**瓶頸。截斷／壞格更可能
  是「長結構化 JSON 的格式可靠度」問題（A3B 長輸出時跳脫／閉合
  出錯機率隨長度上升）。結論：瘦身與小檔規則維持不變（治發生面）；
  伺服器端唯一高價值槓桿＝ **tool-call 約束解碼**；不必追「加大
  輸出上限」。
- 追記（2026-07-30）：輸出偶見 `</think>` 等內部標記洩漏＝chat
  template／reasoning parser 未對齊（Qwen3 系混合思考模型），且
  是 JSON 壞格的可能來源之一。SOP-10 加對齊檢查與「關閉 thinking」
  評估項（工具密集用途通常更穩更快）；lint 加髒標記污染檢查。

### L12 一檔一行工單讓「紙上打勾」有機可乘——id 修復改走 lint 清單手術式（2026-07-30）
- 症狀：A29~A39 兩批跑完＋輪次 6 稽核，lint 縮寫 id 僅 14→13
  （幾乎未修），且一檔被破壞性覆寫（新增 3 筆缺章節）。
- 根因：per-file 彙整工單粒度太粗——模型「處理過該檔」即可打勾，
  檔內逐筆的 id 修復被靜默跳過；「逐字取回傳」規則存在但**缺交付
  驗證**（沒有人核對修了哪幾筆）。
- 落點：(1) **修復必附收據**：舊→新 id 對照表，無收據不得打勾；
  (2) **lint 清單驅動**：把 lint 輸出的 檔×id 清單直接貼進指令
  逐筆處理（原子化、可核對、模型不需自己找目標）；(3) 修復波
  前後各 commit 內部 git（快照）；(4) 受損檔走 SOP-4 部分還原；
  (5) 輪次 6 報告視為不可信，lint 歸零後以新 session /ps-audit
  重測（輪次 7）作畢業依據。
- 套用：本 commit（agent 硬規則／SOP-2）。
- 結果驗證（2026-07-30）：手術式一次修復 **13/13 全數成功**。
  成功要因＝拿掉模型的「自主發現」段、粒度降到原子、收據交付、
  lint 前後驗收。**通用原則入庫：確定性工具負責「找目標」與
  「驗結果」，模型只負責中間非它不可的工具操作**——任何
  「叫模型修東西」的場景都適用此形。
- 輪次 7 里程碑（2026-07-30）：首份全合規＋健康文件的全量體檢——
  證據 PASS 83%（91/110，趨勢 69→78→73→83）、UNVERIFIABLE 僅 3、
  縮寫 id 類 FAIL＝0。殘餘 FAIL 16（新組成待原因分類）與
  DISPUTED 28/68（41%，回到稽核員噪音帶）進入人工抽驗畢業程序：
  真問題定向收尾、噪音行政結案（打勾＋⚠人工判定誤報）並以本輪
  數字為該領域基準地板。
- 措辭更正（2026-07-30，管理者指正）：縮寫 id＝0 是**存量清零**
  （手術修復的成果），**不是類別根除**——寫入端防線（模板範例＋
  禁縮寫規則）尚未經歷大量新寫入的實戰。驗證計畫：斷鏈補課的新
  entity 檔（首戰）與新領域首輪 research（第二戰）寫完各跑 lint，
  新檔持續乾淨才可宣告根除。原則：**修好存量 ≠ 堵住流量，
  兩者要分開驗證**。

### L13 輪次 7 FAIL 16 的三家族——lint 盲區、稽核過嚴、手術缺驗貨（2026-07-30）
- 症狀分類：(A) 截斷 id 仍現身稽核＝**lint 盲區**（只掃「ChunkId」
  前綴寫法，表格等其他位置的 8 碼漏抓，手術只修了看得見的）；
  (B) case mismatch／wrong_line／count_mismatch＝**稽核過嚴＋時效**
  （程式碼大小寫不敏感；quote 命中行號漂移；線上 DB 筆數本來會變）；
  (C) data flow 表無 chunk id、修復 id 內容不符 claim＝**真缺陷**
  （證據義務未履行；手術重取抓錯 chunk 仍硬填——缺「回傳必含原
  quote」的驗貨步驟）。
- 落點：機械化——(1) lint 廣域截斷偵測（任何位置的獨立 8 碼 hex
  含字母者）；(2) auditor 比對正規化（大小寫不分、空白摺疊）＋
  PASS(LINE_DRIFT)＋FAIL(STALE_DATA)（時效與造假分流）；
  (3) 修復前驗貨：重取 ChunkText 必含原 quote 才准寫入，抓錯禁止
  硬填；(4) STALE_DATA 修法＝更新數字。
- 待決：ES 查無那批取決於「索引近期是否重建」（是→SOP-11 正常
  死亡重取；否→id 本身有誤）。
- 套用：本 commit。
- 抽查解謎（2026-07-30）：「ES 查無」實為**假死**——程式碼存在，
  是 Component 事件（PreBuild／PostBuild）。死因＝定位鍵用錯：
  Component 層級 PeopleCode **沒有 Record.Field**，正確路徑＝
  Component 名搜檔 → get_file_structure → 按 Event 挑單元；拿事件名
  當全庫關鍵字＝滿庫都是、等於沒搜。落點：peoplecode-flow 加
  Component 事件定位鍵規則；auditor 判 NOT_FOUND 前強制「二次定位」
  （用 evidence 自帶的 filePath／ObjectName／EventName 重找），
  找回→FAIL(ID_RELINK) 附新 id（修法＝換 id，最便宜）。
- 再深挖（2026-07-30，管理者抽查）：被喊查無的 id **本身有效**
  （確為該 event 13 個 chunk 之一）→ 稽核員根本沒直接解引用，
  而是用搜尋／瀏覽「看到與否」代替——ES 一頁 10 筆、event 有 13
  chunk，尾巴 3 顆永遠不在第一頁。**分頁紀律（單頁 ≠ 全部）當年
  教了研究 flows，稽核員漏打疫苗**。落點：auditor 明文「解引用
  一律直接 get_chunks_details(ChunkId)，禁止以搜尋有無代替」＋
  二次定位全程綁分頁紀律（§5.1）。原則：**新 agent 上線時，共用
  檢索紀律要逐一確認有綁，不會自動繼承**。

### L24 ES MCP 程式 bug 造成假查無——歷史判定修帳（2026-08）
- 事件：lint 手術期間，管理者發現某 Component event 的 peoplecode
  「ES 查無」但實際存在——追查為 **PeoplecodeElasticSearch MCP
  程式錯誤**，已修復、可正常查得。
- 歷史修帳：過去「ES 查無」類判定（文件 gaps 的查無、稽核
  NOT_FOUND、部分假死案）**多了一個共犯**——先前歸因的定位鍵／
  分頁行為問題仍真實，但部分案例的主因可能是本 bug。冤案由
  「稽核二次定位＋已修復的 MCP」在後續 audit／補課輪自動平反，
  不開專案式重查。
- 落點：SOP-8 加「MCP 程式修改」對齊檢查項（直通測試各 tool；
  檢索類修復→歷史查無帶嫌疑；重建索引→走 SOP-11）。
- 原則：**工具鏈本身也是嫌疑人**——連續出現「不合理的查無」時，
  除了模型行為與資料，第三個要查的是 MCP server 程式。
- 套用：本 commit。
- 追記（管理者實測揭露真空區）：MCP 修復後重跑 lint，錯誤的
  「查無 PreBuild、可用 PostBuild 替代」敘述**未被平反**——lint 只驗
  格式、audit 任務 A/B 只驗既有證據與正面主張，**負面宣告寫入後
  無任何機制重測**（先前「下輪自動平反」的說法錯誤，修帳）。
  堵法：(1) 寫入端「查無必附查法收據」（無查法不得寫查無）；
  (2) auditor 任務 A 新增「查無宣告抽驗」→ 查得到判
  FAIL(FALSE_NEGATIVE) 回灌補查；工具鏈修復後首輪**全量**抽驗
  （歷史平反機制）。本案個案：/ps-correct 修敘述＋SOP-6 手加
  補查項實查 PreBuild。

### L23 畢業宣言——寫入攪動地板與維運模式（2026-08）
- 判定：PASS 率 86%（史上最高）、FAIL 於 6↔12 帶區震盪、殘餘全為
  已知慢性類——達成收斂判準。「感覺沒盡頭」的機制解釋：**修復即
  寫入，寫入以低固定率播新小瑕疵**（寫入攪動地板）；research 後
  緊接 audit 的衝刺迴圈＝永動工單機。
- 處置：首領域**畢業**。收尾批次（lint 手術清縮寫存量＋欄位錯待
  cookbook 規則 8 後修＋其餘 A 項行政結案）→ 轉維運模式（SOP-13：
  問答隨時、lint 每寫入波、research 按需、audit 僅 CR 後／月度）。
- 慢性類策略入帳（回應稽核的兩個 lesson 提議，不另開）：
  truncated-chronic＝lint 手術按波清理，不為它跑稽核；
  wrong-column 類＝cookbook 規則 8「查詢前欄位驗證」（不確定欄位
  先查 all_tab_columns，禁憑記憶）。
- 記分卡格式仍會飄（本輪又回總數式）——資料在就不追殺，lint 章節
  警告持續留痕即可。
- 套用：本 commit。

### L22 「過簡」不是反駁——深度建議與品質缺陷分流（2026-08）
- 症狀：15 檔 9 紅燈，但證據層僅 FAIL 6／UNVERIFIABLE 2（史上最
  健康）；紅燈大戶＝DISPUTED 12，主力為「過於簡化的業務描述」
  集中五檔。
- 判定：「寫得太淺」提不出事實矛盾點，依 L14 前提本就不合法
  DISPUTED——敘述為真、只是不夠深，屬**編輯建議**非品質缺陷。
- 落點：auditor 明文「過簡 → VERIFIED＋gaps 建議補充；不得
  DISPUTED、不染紅、不生成強制工單」。五檔要不要加深＝管理者的
  產品決策（可作選配批次）。
- 附：PS_ 假說經管理者抽驗證實（36 筆全為 PS_TW_XXX 自家表）——
  task C 覆蓋帳待下輪正規化重判後翻正。
- 套用：本 commit。

### L21 task C 首跑的三個發現——PS_ 前綴比對假象、空白附錄盲區、合計列（2026-08）
- 發現：(1) 「37 筆核心表 36 筆未覆蓋」高度可疑——SQL 表名帶
  `PS_` 前綴、文件寫 Record 名，逐字比對必然全滅（待管理者抽 3 筆
  驗證）→ auditor task C 加正規化規則（去 PS_、大小寫不分、
  [[連結]] 算出現），未正規化的未覆蓋清單無效；
  (2) 某檔 Evidence 附錄「有標題無內容」——lint 只驗章節存在
  → 加空白附錄違規檢查；(3) 記分卡回到正版一檔一列格式但無總數
  → 模板加「合計」列。
- 處置：A80~A87 中「遺漏表」類先擱置（等正規化後下輪重判）；
  縮寫×2／SQL 欄位錯×3／五檔過簡描述／空白附錄照常消化。
- 套用：本 commit。

### L20 checklist 熱檔會隨輪次肥大——已勾項歸檔（2026-08）
- 症狀（管理者預警）：調查進度隨每輪稽核回灌累積 A 項、已勾項
  永留 → 整檔覆寫的 JSON 隨歷史變長，走回 L2 截斷老路；找
  「第一個未勾項」的掃描噪音也隨之增加。
- 落點：機械化——每輪稽核回灌時把所有已打勾項 append 到
  `checklist-archive.md`（冷檔，永不回讀），checklist.md 常態只留
  輪次行＋未勾項＋Gaps；lint 對帳改「checklist＋archive 合併」。
  現存肥檔於下輪稽核首次歸檔時自動瘦身。
- 原則：**熱檔（反覆整檔覆寫）必須有歸檔機制，否則終將肥死**
  ——log.md／applied.md 的「冷熱分離」哲學套用到 checklist。
- 套用：本 commit。

### L19 oracle 裸表名查不到——CURRENT_SCHEMA 機械化（2026-08）
- 症狀：稽核 FAIL 7 筆「view 不存在」；管理者實測加 [schema].TABLE
  前綴即查到＝cookbook 樣板用裸表名、登入帳號不是 PeopleTools 表的
  擁有者。可能同時解釋歷史部分 UNVERIFIABLE(view 不可用)。
- 落點：機械化——連線生命週期插入第 3 步「ALTER SESSION SET
  CURRENT_SCHEMA=<local-env.yaml 的 oracle.currentSchema>」
  （唯一准許的非 SELECT；一次設定、全程裸表名照舊——遠優於要求
  小模型每句加前綴）。schema 實名屬機敏 → local-env.yaml 本機檔
  （.gitignore 擋、人工搬運不覆蓋；example 範本入 repo）。
  三個 flow agent＋auditor 生命週期同步更新。
- 另修：auditor 原因欄「寫人話」標準（管理者反映 E5/E6 類代號
  不可讀）——固定格式「文件說／實際取到／差異」。
- 本輪其餘三類系統性（LINE_DRIFT／ID_RELINK／QUOTE 類）已由 L18
  三鐵律與修法選單覆蓋，未重複開 lesson。
- 修正（管理者）：本案 schema 名**非機敏** → 淘汰 local-env.yaml
  機制，設定移入 customization-profile.yaml 的 oracle.currentSchema
  （隨 repo 搬運、免額外人工步驟）。
- 套用：本 commit。

### L18 證據格式三鐵律——本機 /ps-lesson 首次自主產出、PR 收編（2026-08）
- 來源：地端模型執行 /ps-lesson（稽核三類系統性 FAIL：無 id 檔案
  行號型／行號飄移／8 碼縮寫）自主分類並套用——落點選擇正確
  （契約＋稽核模板＋測試檢查點）、未越界碰 scripts/、有記帳。
  **H2 現場測試通過**。
- 內容（收編後正式版）：證據三鐵律——(1) CHUNK 必附完整 36 字元
  id；(2) 行號對應當前內容；(3) 全欄位禁止縮寫、逐字取自工具回傳。
  缺一放 gaps 不入 findings。落點：subagent-report-contract／
  audit-template FAIL 詞彙表／F7 測試情境（總數 39）。
- 治理備註：本機帳本曾將此課編為 L16，與 repo 的 L16（/ps-correct）
  **撞號**——人工搬運期間兩本帳各自生長所致。以 repo 為準收編為
  L18；本機 applied.md 以整檔覆蓋歸一。原則：**流水號以 canonical
  repo 為準；本機產出的教訓經 PR 收編時由審核者定號**。
- 套用：本 commit。

### L17 oracleMCP 跨視窗搶用——單通道紀律（2026-08）
- 症狀：問答常回「OracleMCP 無法執行 SQL」；輪次 9 稽核 5 筆
  UNVERIFIABLE(view 不可用) 同族。
- 根因：SQLcl 單一有狀態連線；使用模式常「一邊跑稽核一邊問答」，
  兩個 OpenCode 視窗互不知情地搶同一通道 → connect 卡死／逾時。
  序列化規則只管單 session 內，跨視窗管不到（也管不了）。
- 落點：(1) SOP-12 單通道操作紀律（一次一個 oracle 使用者＋快篩
  三步）；(2) orchestrator 轉譯規則——BLOCKED 不得說成「無法執行
  SQL」，照實說「通道忙碌，稍後重試」並照常回答非 DB 部分。
- 附註：wiki 補課完成後，多數資料題可由 verified 條目直接回答，
  對 oracle 的依賴會自然下降。
- 套用：本 commit。
- 修正（2026-08，管理者實驗）：build 模式 3 subagent **同時呼叫
  oracleMCP 成功**——推翻「絕對單行」強命題。理論修正為：
  (1) 重載排隊逾時（稽核 SQL 風暴打滿通道 → 並行查詢排隊 >30s →
  觸發自家逾時規則 → BLOCKED）；(2) 待驗的 fratricide 假說
  （connect→查→disconnect 生命週期在共用連線下，一方 disconnect
  拆掉他方）。SOP-12 措辭由「一次一個使用者」放寬為「重載期間
  不並行」。決定性實驗：audit SQL 段並發問答 3 次，看塞車 vs
  連線錯誤。原則重申：**假設要被實驗修正，包括我們自己立的**。

### L16 新增 /ps-correct——資深同事指正業務知識的一鍵入庫（2026-08）
- 動機：最高頻的真實修正場景＝資深同事指正業務邏輯（資料病，非
  行為病）——原路徑（/ps-research 全跑或 SOP-5 人工改檔）太重。
- 設計：/ps-correct <正確知識> → 查重 → 作廢不刪除更新 entity →
  來源標 `human:<日期>`、status verified＋reviewed: true（**本機立即
  生效；人審＝內部 git PR 看 diff**，流程內不設額外蓋章步驟，與
  /ps-lesson 同治理）。
- 配套機制：auditor 新約定——human 型來源＋reviewed: true 免解引用
  （PASS(HUMAN_VERIFIED)）；claim 層不得僅因「查無程式證據」反駁
  human 已驗知識（需明確矛盾證據）。堵住「人教的知識被稽核反覆
  質疑標 stale」的缺口。
- 落點：新 command／auditor 兩處／entity 模板 sources 註記／
  orchestrator 指正協定／AGENTS.md／README／簡報／H3 測試情境
  （總數 38）。
- 套用：本 commit。

### L15 OpenCode 內建 subagent 側門——同名覆寫補鎖（2026-08）
- 症狀：問答（orchestrator）主畫面出現直呼 PeoplecodeMetadata 等
  MCP、以及「EXPLORE TASK」；同題最終回「查不到」。
- 根因：OpenCode（1.17.15 **本就內建**，非升版帶來）有三個內建
  subagent（general／explore／scout）——**內建 agent 不在本專案
  九檔封鎖體系內，MCP 預設全開**（L1 覆寫表教訓的新變種：側門，
  從部署第一天就開著、此次首次被委派漏入）。委派未指名 ps-* 時
  會漏到內建；explore 只探索本機 repo 檔案，查不到 PeopleSoft →
  回報查無被當成最終答案。
- 落點：機械化——(1) .opencode/agent/ 放 general／explore／scout
  **同名覆寫檔**封 4 個 MCP；(2) orchestrator／deep-research 硬規則
  「委派必須指名 ps-*；內建回來的查無無效」；(3) F3 加致命檢查點；
  (4) SOP-8 加「內建 agent 盤點＋同名覆寫」檢查（升版時必查、
  部署時也該查一次）。
- 原則：**平台的預設能力（現在有的＋未來空降的）都在封鎖體系的
  守備範圍**——同名覆寫是唯一可預先部署的鎖。
- 套用：本 commit。

### L14 DISPUTED 抽查歸零——稽核員邊界病＋不可裁決病歷；畢業裁決成立（2026-07-30）
- 抽查結果：DISPUTED 樣本**零筆證實文件為錯**。組成＝(1) 邊界病：
  目標在第 121 行、chunk 只到 120 → 不取下一段就喊矛盾（§5.1
  邊界接續沒綁）；(2) 取證未竟：只取 SavePostChange 就判、SaveEdit
  沒驗——依契約該判 UNVERIFIABLE 卻判 DISPUTED；(3) 病歷不可裁決：
  明細無「claim／取到什麼／矛盾在哪」三要素，人與覆核都無法判。
- 落點：auditor 判 DISPUTED 前置兩前提（取證完整含邊界接續與
  多 event 全取；必附三要素病歷）——缺一改判 UNVERIFIABLE；
  audit-template 明細加三要素要求。
- 畢業裁決：輪次 7 的嚇人數字（FAIL 16／DISPUTED 28）經人工抽驗
  定性為**稽核程序債為主**（查無批＝id 有效、case/行號/數值＝已
  分流雜訊、DISPUTED 樣本零實錘）；文件實際健康。處置＝殘餘實項
  （廣域 lint 縮寫、資料流補證、WRONG_KIND 改引）收一批定向批次，
  其餘行政結案；以修補後稽核員跑**輪次 8 作為基準輪**。
- 套用：本 commit。

### L9 ChunkId 被縮寫成 8 碼 hex——git SHA 習慣汙染 UUID（2026-07-27）
- 症狀：輪次 3 證據層 FAIL 10 的大宗是「非 UUID 格式」——實際是
  文件裡 ChunkId 只剩前 8 碼 hex（縮寫習慣同 git SHA），證據本體
  （filePath／行號／quote）多為真，被機械檢查誤判 FABRICATED。
- 根因：模板僅示意 `<uuid>` 佔位、未示範完整長度；「逐字複製」是
  prose 規則（L0：效力最弱層）；模型以縮寫求可讀性。
- 落點：機械化——(1) 兩模板改用完整 36 字元假 UUID 範例＋禁縮寫
  註記；(2) 硬規則「ChunkId 禁止縮寫」；(3) auditor 區分
  FAIL(TRUNCATED_ID)（8 碼樣式）與 FAIL(FABRICATED)；(4) A 項處理
  加 TRUNCATED_ID 廉價修法（依 filePath＋行號重找補全，不重做
  分析）；(5) lint 對 8 碼樣式給專屬訊息；(6) G2 加檢查點。
- 套用：本 commit。
- 附註：此發現大幅下修真實缺陷率——輪次趨勢（claim 層
  VERIFIED 19→30、DISPUTED 29→13；證據 UNVERIFIABLE 16→9）
  顯示系統在收斂，FAIL 卡 10 主因即縮寫誤判。

### L10 長 run 尾端的自動稽核必塌縮——改為超過門檻就換 session（2026-07-27）
- 症狀：連續處理 13 個 A 項（中途已停機一次要人推）的 run，
  尾端自動稽核產出：無記分卡、僅 13 筆（＝A 項數）、明細只有
  A16~A28——範圍塌縮成「只覆核剛做的事」；「全量重驗」prose 規則
  與 H1 致命檢查點皆未擋住。
- 規律：新 session 的獨立稽核（輪次 3）格式最合規；長 run 尾端
  稽核（首輪、輪次 4）皆塌——**稽核執行品質 ∝ context 新鮮度**。
- 落點：機械化——(1) 收尾稽核加門檻：本 run 打勾 ≤ 5 項才當場
  稽核；> 5 項禁止當場稽核，改提示開新 session 跑 /ps-audit（或
  重跑 /ps-research，全勾態自動接）；(2) lint 全量對帳：每個 NN 檔
  必須出現在 90-audit 內文，缺列＝範圍塌縮警告；(3) 13 筆全過
  亦證明 TRUNCATED_ID 廉價修法有效。
- 套用：本 commit（agent／command／README／lint）。

### L11 輪次 5 全量重驗的三類指紋：紙上修復＋證據類型誤用＋環境噪音（2026-07-27）
- 症狀：FAIL 13／UNVERIFIABLE 8 的原因分布＝「chunk 又是 8 碼截斷」
  （多數）＋「AE SQL 無 SELECT 可重跑」＋「task JSON 失敗／空結果」。
  無「chunk 查無」→ 模型未憑記憶編假 UUID（假說 B 未發生）。
- 根因：(1) 紙上修復——A16~A28 的修復輪發生在 13 項馬拉松 session
  的劣化 context（與稽核塌縮同病灶）；(2) 契約缺口——程式內 SQL
  語句（AE_SQL 等）被標成 `kind: SQL`，稽核員無法重跑非 SELECT；
  (3) 稽核執行期的工具噪音被正確記錄於原因欄（隔離成功）。
- 落點：機械化——(1) 處理端對稱門檻：單次 run 至多處理 6 項，達標
  即停換 session；(2) 契約明文：`SQL` 證據僅限實際執行過的 SELECT，
  程式內 SQL 一律 CHUNK；auditor 新判定 FAIL(WRONG_KIND)（不執行、
  不判 UNVERIFIABLE）；A 項處理加 WRONG_KIND 改引用規則；
  (3) id 補全防造假：必須逐字取自本次 get_chunks_details 回傳，
  禁止憑記憶補尾巴；(4) lint 的 8 碼清單＝截斷修復的確定性工單
  （不依賴稽核計數）。
- 套用：本 commit。
- lint 首跑全量盤點（2026-07-27）：FAIL 24＝縮寫 id ＋**缺章節**
  （行為邏輯／Evidence 附錄——劣化 session 破壞性覆寫的直接證據，
  「整檔覆寫必須保留其他內容」prose 規則被違反）；WARN 109 多為
  wikilink 斷鏈＝歸戶欠課。處置：缺章節優先走**內部 git 考古還原**
  （SOP-4，零重跑、零捏造）；縮寫 id 走 A 項工單；斷鏈另開分批
  補課（一次 10 個、先查重）。

### L25 「追加」在工具層是整檔重寫——append 目標檔是隱形的無界熱檔（2026-08）
- 症狀：輪次 15 稽核在「歸檔＋寫 log」步驟 Preparing write 卡死
  20 分鐘無輸出。回灌 A 項已先落檔（該步驟前完成），損失僅記帳。
- 根因：write 工具只有整檔覆寫——規則寫「append 到
  checklist-archive.md」，執行上是 read 全檔＋重寫全檔；archive
  每輪成長，第 15 輪已超出單次 write 可靠上限（L2 同族），
  且被迫回讀違反「archive 永不回讀」本意。L20 替 checklist 瘦身時，
  肥大被**搬進 archive 而非消滅**——熱檔問題換了地址。
- 落點：機械化——歸檔改**每輪寫新檔** `checklist-archive-r<N>.md`
  （單次小 write；禁止 read／改寫既有 archive 檔）；lint 對帳改
  合併 `checklist-archive*.md` 全部分片。原則：**真 append＝開新檔**；
  任何「只追加」規則都必須附分片條件，否則就是下一個卡死點。
- 套用：本 commit（agent／command／lint／checklist-template）。

### L26 全文 exact 查無假象——物件名／事件名是 metadata，不在程式內文（2026-08）
- 症狀：輪次 15 稽核實測：某 Component 事件的 chunk 確實存在——
  `search_chunks(ObjectName=<物件名>, eventName=SavePostChange)`
  結構化過濾查得到；同工具 `query=<物件名> <事件名>`＋exactPhrases
  ＋searchMode=exact 全文查**不到**。
- 根因：物件名／事件名住在 chunk 的 **metadata 欄位**，不保證出現在
  ChunkText 內文（程式碼不會逐字寫自己的 Component 名）；全文搜尋
  搜的是內文——用全文 exact 查「物件＋事件」＝方法錯誤，查無是
  假象（與 PeoplecodeMetadata 拿 Page 名查空同族：**錯 key 必空**）。
- 落點：機械化——檢索聖經 §5.1、auditor 二次定位與 FALSE_NEGATIVE
  重測、A 項 MISSING_CHUNK_ID 修法，一律「物件＋事件定位首選
  ObjectName＋eventName 結構化參數」；全文 exact 查無不得作結論、
  不構成合格查無收據。
- 套用：本 commit。

### L27 subagent 跑到 auto-compact——壓縮後的盤點結果不可信（2026-08）
- 症狀：稽核任務 C（完整性盤點）單一 subagent 吃整份核心表清單×
  三個反查角度，context 滿觸發自動壓縮後繼續作業。
- 風險定性：壓縮＝同一顆小模型把工具回傳改寫成摘要再基於摘要作業
  ——物件名這類精確識別字最易被截短／正規化／漏列；查過的角度
  可能被摘要成沒查（auditor 內部自產 FALSE_NEGATIVE）；壓縮對
  委派方不可見，報告可信度被靜默降級。與新鮮度定律同源：
  auto-compact 是讓 session 越過自然壽命續產的機制，品質必在
  壓縮點斷崖。關掉 auto-compact 不解決——那只是把斷崖換成死掉。
- 落點：機械化分批——任務 C 每批至多 5 張表、一批一個委派，
  委派方做聯集與 diff；auditor 端對稱防守：收到 >5 張只做前 5、
  其餘列 gaps 退回。原則：**subagent 的工作量必須設計成
  「單一 context 裝得下」——需要壓縮才做得完的委派＝委派切錯了**。
- 套用：本 commit（auditor／agent／command）。

### L28 假缺檔事件——貼上輸入是搬運鏈第三種污染載體；單點缺檔違規曾遮蔽整段對帳（2026-08）
- 症狀：研究輪後管理者回報「調查進度空＋Gaps 空＋沒跑 audit 卻看到
  全綠 90-audit」；lint 一項違規「缺 00-overview.md」但檔案實際存在；
  跑 lint 時另現紅字「'#' 不是 cmdlet…」。
- 診斷：前三者全屬合法路徑（全勾接稽核＋歸檔瘦身＋輪次遞增——L20
  機制的正常產物，虛驚）。真問題是 lint **假缺**：ps-fs-doctor 判定 A
  （檔案精確存在、repo 檔案無 FEFF 污染）→ 污染在「那次執行的輸入」。
  推定機制＝-Domain 尾部空白/點觸發 **Win32 尾字元正規化不對稱**
  （路徑最後一段的尾空白會被剝掉→「目錄」查得到；檔案路徑中它變成
  中間段、按字面查找→其下「檔案」全查無）；`#` 紅字＝貼上的指令行
  帶隱形前綴（同族）。決定性 A/B（同筆記貼上重現 vs 手打不重現）
  待管理者驗證——標待驗，不影響落點（防線對兩種假說皆有效）。
- 根因（框架層）：(1) 輸入通道無消毒——貼上指令的隱形字元/尾空白
  直達路徑組裝；(2) lint 的對帳整段包在「overview 存在」分支裡——
  單點假缺讓「無遺失」整輪不可證；(3) 缺檔訊息無收據，人工無從
  分辨假缺/真缺，遠端診斷連猜三輪。
- 落點（全機械化）：(1) lint＋auto-loop 參數消毒（trim 尾空白/點、
  隱形字元 exit 2）；(2) 對帳與 overview 檢查解耦（缺檔違規不遮蔽
  其餘檢查）；(3) 缺檔違規附近似檔名收據（*overview* 掃描，假缺/
  真缺可分辨）；(4) archive 禁未勾項（歸檔只准搬已勾——未勾被搬走
  ＝進度隱形消失）；(5) checklist 節標題檢查（調查進度/Gaps 標題
  消失＝破壞性覆寫指紋；row 清空可為歸檔後合法狀態）；(6) 輪次
  一致性（checklist vs 90-audit 表頭，不一致＝報告可能是舊輪、
  綠燈不可信；StrictAudit 下升違規）；(7) 新工具 ps-fs-doctor.ps1
  ——唯讀健檢、結論代號（A~G）回報，機敏環境「不貼輸出」的
  診斷通訊格式。
- 原則：**「看得到但程式找不到」先驗字元再驗檔案**；人工搬運鏈的
  污染載體有三種——檔案內容（L18 雙帳）、檔頭 BOM（雙 BOM 病）、
  **貼上執行的指令本身**；聊天視窗複製的指令與參數視同未消毒輸入。
- 套用：本 commit（lint／auto-loop／ps-fs-doctor／test-scenarios J4）。

### L29 當場稽核門檻用錯變數——守的是回灌量，塌的是全量（2026-08）
- 症狀：新研究輪尾端 in-run 稽核產出全綠 90-audit；手打參數重跑 lint
  後真相曝光——記分卡缺 15 個檔案列（範圍塌縮）＋24 項違規（縮寫
  uuid 大宗、Evidence 附錄空白）與綠燈並存＝填表稽核（L3 同族）。
- 根因：接稽核規則「本 run 打勾數 ≤ 5」守的是**回灌處理量**，但稽核
  ＝全量重驗（範圍是全部已完成檔）——工作量隨**檔案總數**成長。
  15 檔的全量塞進 research session 尾端 context 必塌（L10 新鮮度定律
  的變數錯置版）；且「新 session 重跑」路徑（原第 139 行）完全沒有
  規模門。守門變數與工作量變數脫鉤＝門形同虛設。
- 落點：窄規則——ps-deep-research 接稽核區塊加**前置規模門**（優先
  於其餘條款）：已完成 NN 檔總數 > 5 一律禁止當場稽核（兩條路徑
  都管），改開新 session /ps-audit；G2 加致命檢查點。lint 端的
  「記分卡缺列」警告（L7）與 StrictAudit 升違規（issue #2）＝既有
  第二道防線，本課補寫入端第一道。
- 原則：**門檻要守的是「這一步的工作量」——變數要選會跟工作量
  一起長的那個**；守門變數與工作量脫鉤的門，等於沒有門。
- 套用：本 commit（ps-deep-research／test-scenarios G2）。
- 追記（2026-08，管理者假說）：塌縮的具體機制疑為 **auto-compact**——
  記分卡缺的 15 列正是 L27 指紋（壓縮把工具回傳改寫成摘要時，精確
  識別字〔檔名〕最易漏列；壓縮對委派方與檔案層皆不可見）。與 L29
  落點相容：規模門就是「單一 context 裝得下才准當場稽核」的機械
  代理（L27 原則從 subagent 延伸到主 session 收尾稽核）。驗證法：
  回看該輪 session 畫面/紀錄有無壓縮事件——證實後本行改「已證實」。

### L30 凍結檔的慢性過時——凍結必須配保鮮標示與換版程序（2026-08）
- 症狀：管理者發現 00-overview.md 停在 2026-07-27（凍結機制上線日），
  十幾輪 research／audit 之後內容嚴重過時——盤點快照被當現況讀。
- 診斷：非疏漏——L2 凍結是刻意設計（防大檔整檔覆寫截斷與劣化
  session 破壞性覆寫），但只做了「禁寫」、沒做「標示」與「出口」：
  讀者不知道它是快照、想刷新也沒有合法程序。
- 落點：(1) overview 模板頭加快照聲明（凍結不隨輪次更新、現況以
  checklist／NN／wiki 為準）；(2) lint 落後提醒（稽核輪次 ≥ 3 即
  警告，任何模式不升違規——過時是資訊不是缺陷）；(3) SOP-15 換版
  程序：agent 只產草稿到新檔 00-overview-draft.md、人工審後覆蓋
  （凍結零寫入不破例）；換版使收據失效＝預期重驗。既有領域的
  快照聲明由人工換版時一併補上，不回頭改檔。
- 原則：**凍結解決寫入病、製造過時病——任何「永不更新」的檔案
  都必須自帶「我是快照」的標示與一條人工主導的換版路**。
- 套用：本 commit（overview-template／lint／SOP-15）。

### L31 新大陸不入圖——凍結地圖配機械覆蓋 diff（2026-08）
- 情境（管理者提問）：小模型一輪 deep research 探勘不完整是常態，
  第二輪之後發現的「新大陸」進 checklist→NN 檔→wiki（三層都會記錄），
  唯獨凍結的 00-overview 功能地圖不會入圖；打勾歸檔後 checklist
  活頁也看不到它——導航層對後續發現失明，直到低頻的 SOP-15 換版。
- 落點：機械化（零新寫入路徑）——lint 新增「功能地圖覆蓋 diff」：
  checklist＋archive 的標準列（含物件名與目標檔）其物件名未出現在
  overview 內文＝地圖缺頁，警告列出清單；該清單同時是 SOP-15 換版
  的必併清單（換版後重跑 lint 警告應消失＝驗收）。任何模式不升違規
  （缺頁是導航品質不是正確性）。
- 否決案：開活頁附錄讓模型接寫新發現——小模型的 append＝整檔重寫
  （L25 老路），且又多一個要靠寫入紀律維護的熱檔。
- 原則：**凍結檔的「現況可見性」用確定性 diff 從活資料推導，
  不用新寫入路徑去維護第二份帳**。
- 套用：本 commit（lint／SOP-15 步驟 2a）。

### L32 寫入鏈不可修——手術升級梯；exactPhrases 假查無（L26 家族第三例）（2026-08）
- 症狀：id 手術波（fresh session）跑完 lint 零減項；前波壞寫實錘——
  UUID 後黏「(36字元UUID)」（指令說明文字迴流進資料欄）、
  「ChunkIds」拼字使 lint 誤抓 id=s。切「模型查、人工貼」後
  5 筆 3 修；2 筆「無法重現」中至少 1 筆為假查無：真實存在的 AE
  用 exactPhrases 查零筆，管理者實測 `query=<AE 名>`＋
  `searchMode: semantic` 精準命中全部 59 chunk。
- 根因：(1) 寫入鏈不可靠且**不可修**——tool-call 約束解碼
  （SOP-10 第 5 步、L8 認定的唯一高價值槓桿）經公司政策否決；
  (2) ES 查法模式誤用（L26 家族第三例）：`exactPhrases`＝ChunkText
  **字面包含**過濾、`exact`＝精確字串匹配——被當定位工具用在
  物件/AE 名上必假查無（metadata 名不保證出現在內文）；
  `semantic`＋`query`＋`offset` 才是名稱定位的正解。
- 落點：(1) SOP-2 第 6 步手術升級梯（fresh 重試一次 → 切模型查
  人工貼：委派只回對照表、禁止寫檔）；(2) 檢索聖經 §5.1 補 ES
  參數語意表與定位優先序（結構化參數 → semantic；exact 家族只作
  引文驗證）；(3) auditor 二次定位加 semantic 第三管道、查無宣告
  抽驗加 semantic 重測；H1 加檢查點 11。
- 原則：**環境修不了的病，把程序降級到環境可靠的層**（寫入交人工，
  模型只做非它不可的檢索）；**查無判定前必窮盡鍵型正確的查法**。
- 套用：本 commit（SOP-2／檢索聖經／ps-auditor／test-scenarios H1）。
- 追記（2026-08，管理者實測第三例）：模型宣稱「證據性質本身產不出
  chunk」（sql view metadata／MD cross-ref／ES 0 命中）——其中 AE step
  的 SQL 經實測 **ES 有 chunk**，`search_chunks(objectName=<AE名>,
  componentType=ApplicationEngineProgram)` 精準命中。藉口分流表入庫：
  程式內 SQL（AE step／SQL definition）一律有 chunk 可取（sourceTypes
  含 SQL_DEFINITION、componentType 可過濾）；真執行過的 SELECT＝SQL 型
  證據本就免 chunk id；MD 交叉引用不是證據列（契約只認 CHUNK/SQL，
  改正文 wikilink）；只有結構化＋semantic 皆空才准移除。原則：
  **模型的「本質上做不到」一律當待驗主張**——本輪已三連誤
  （exactPhrases 查無、驗證正確不附 id、產不出 chunk）。
  檢索聖經 §5.1 與 auditor 二次定位補 componentType 鍵。
- 追記二（2026-08）：第四例「判定不用做」——委派模型完成檢索後回
  「無須更動」（缺 id 列照舊缺）、另一筆排程 SQL 拒查 Oracle。
  SOP-2 升級梯補第 4 階：**個位數殘項人工直通**——chunk 類直通測試
  呼叫 search_chunks（結構化參數）從回傳原文抄 UUID；oracle 類自己
  開 SQL Developer 跑 cookbook 樣板（先設 CURRENT_SCHEMA）。原則：
  收尾殘項的經濟學會翻轉——與模型纏鬥的成本超過人工自取時，
  模型的「完成／不用做」判斷退出迴圈，lint 是唯一裁判。

### L33 空殼檔盲區——「缺章節」只驗標題，空心章節安靜通過（2026-08）
- 症狀：某 NN 檔「## 行為邏輯」「## 資料流」有標題無任何內容——
  lint 的缺章節檢查只驗標題存在，空殼照常通過；行為邏輯靠 confidence
  檢查間接抓到，資料流完全漏網。空殼是劣化 session 破壞性覆寫／
  寫入中斷的典型產物（L11 同族），躲過 lint 就會帶病進稽核。
- 落點：機械化——lint 對行為邏輯／資料流加「章節空心檢查」
  （標題到下一個 ## 之間 trim 後為空＝違規，訊息指路 git 考古或
  開重查工單）。處置流程：git 舊版有內容 → SOP-4 部分還原；
  從未有 → checklist 開「A<n> 重查（章節空白——寫入中斷）」工單。
- 原則：**「存在檢查」要驗到內容層——標題是骨架不是知識，
  驗骨架不驗血肉的檢查會養出空殼檔**。
- 套用：本 commit（lint／test-scenarios J4）。

### L34 subagent 退化迴圈——逾時熔絲是它的自動解，中斷零損失（2026-08）
- 症狀：research 消化 A 項時卡在 auditor subagent——不斷重複相同
  產出、無盡頭；管理者中斷後開新 session 重跑，一次通過。
- 定性：小模型退化重複迴圈（degenerate loop）＝**抽樣事故，非
  確定性障礙**——fresh session 通常一次就過；與 auto-compact、
  長 run 劣化同屬「session 壽命」病族，但發作點在單一生成內。
- 對應（已存在，本課記錄對應關係）：auto-loop 的逾時熔絲鏈＝
  人工「中斷→新 session 重跑」的自動版——session 逾時→taskkill
  整樹→唯讀一致性檢查→下一圈 fresh session 重跑同相位→連續
  2 次逾時才熔斷給人工。checklist 打勾只在完成後發生＝中斷零
  進度損失（檔案是狀態、session 是耗材）。
- 落點：SOP-9 加症狀處置（互動模式：見鬼打牆直接中斷重跑，不用等；
  自動模式：信任熔絲；timeout 可依領域常態調短加快反應）。
- 代價入帳：自動模式一次鬼打牆＝白燒一個 timeout slot（30~45 分）
  ——overnight batch 可接受；不做「重複輸出偵測」的提前熔斷
  （啟發式誤殺風險 > 省下的時間，L21 過敏教訓同理）。
- 套用：本 commit（SOP-9）。

### L35 「同前」在整檔覆寫的工具層沒有指涉對象——註解型假內容（2026-08）
- 症狀：新一輪 research 結束，checklist 的「Gaps 彙整」內容是
  `<!-- 同前 -->`——深查未回填。兩個壞處：(1) HTML 註解**渲染後
  不可見**，人在 Obsidian／GitHub 看到的是空章節；(2)「同前」在
  **只有整檔覆寫**的工具層**沒有指涉對象**——寫入當下前一版已被
  自己蓋掉，檔案裡沒有「前」可以指，是偷懶省略不是內容。
- 落點：機械化——lint 的章節空心判定改「剝 HTML 註解與模板佔位符
  後為空」＝空心（L33 檢查升級，看穿註解），並新增 checklist
  「Gaps 彙整實質空白」警告（真無 gaps 要明寫「（無）」；調查進度
  不檢查——全勾歸檔後本來就空）。「同前／如上／略／unchanged」等
  省略語整段當內容一律視為空心。
- 附註：真 gaps 活在各 NN 檔「## 未解事項」——checklist Gaps 是
  衍生視圖，遺失可低成本重建（模型彙整、人工貼）。
- 原則：**能寫「同前」的檔案格式，必須先有「前」存在**——
  append-only 才有 ditto，overwrite-only 沒有。
- 套用：本 commit（lint／test-scenarios J4-9）。

### L36 記分卡改名——畢業門要驗事實不是標題字串（2026-08）
- 現場：新基準輪的稽核產出**史上最健康**（PASS ~100／FAIL 5／
  LINE_DRIFT 5／UNVERIFIABLE 3 ≈ 88%，母數 ~113＝全量無塌縮，
  並正確回灌 5 個 A 項），但記分卡寫在自創標題
  「本輪已完成檔案總覽」底下（欄位齊全：檔名/chunk/PASS/FAIL/
  LINE_DRIFT/UNVERIFIABLE）——L7 老毛病：資料對、格子自創。
- 風險：StrictAudit 畢業門原本綁 `## 總覽記分卡` 字串 → 純命名問題
  就會擋畢業，且這類違規**不產手術清單、audit prompt 也收不到
  lint 結果**＝紅隊警告過的無修復管道活鎖。
- 落點：機械化——(1) 新增 `Get-ScorecardCoverage`：**以結構找記分卡**
  （依 `## ` 切段，優先取標題含 記分卡/總覽/檔案清單/scorecard 的段，
  無則排除 明細/回灌/系統性/完整性/覆核 類後取覆蓋最多者），驗它是否
  涵蓋全部 NN 檔；(2) 覆蓋不足＝塌縮（預設警告、Strict 違規）——
  取代舊的「全文掃描」（檔名出現在明細節就算覆蓋＝漏判）與
  「綁標題字串」（改名就誤擋）兩個極端；(3) 模板章節缺失一律降為
  警告（標題飄移留痕不擋畢業，L23 精神）。
- 原則：**驗事實不驗措辭**——畢業門該問「全部檔案都重驗了嗎」，
  不該問「標題有沒有照抄模板」；措辭類偏差留警告，事實類偏差才擋。
- 套用：本 commit（lint／test-scenarios J4-9；測試 T20/T21）。

### L37 lint 把時間當行號——誤報派出不可能完成的工單（2026-08）
- 症狀：手術波中模型「執著」於排程 AE 那筆，堅持要找
  PS_PRCSRECUR 的 chunk 當 backing——但那是人工補的 **SQL 型證據**
  （查 Oracle metadata 表），本來就沒有 chunk。
- 根因（在 lint 不在模型）：「檔案行號型」判定用 `冒號+數字`，
  而該列寫著 **23:00** → 被當成 `檔案:行號`；豁免條件又只認字面
  「SQL」不認 `SELECT`。誤報進手術單 → 模型收到**不可能完成**的
  工單 → 反覆嘗試（看起來像固執，其實是工單本身無解）。
- 落點：機械化——(1) 行號樣式收緊：冒號前必須是**含字母的檔名
  token**（純數字如 23:00、1:3 不算）；(2) 豁免加 `SELECT`；
  (3) 手術指令加**逃生出口**：明文「該列若是 SQL／metadata 型
  （查 DB 表非程式碼）＝本就免 chunk id，改寫成 SQL：<SELECT>
  即完成該筆」——工單必須自帶「這題無解時怎麼收」的合法出口。
- 原則：**模型反覆失敗前，先檢查工單本身是否可完成**——確定性
  工具派工的責任是「工單必須有解」；無解工單造成的迴圈不是模型
  紀律問題。
- 套用：本 commit（lint；測試 T22/T23）。

### L38 缺檔收據要自報「查了哪裡」——掃錯目錄與真缺檔長得一樣（2026-08）
- 症狀：lint 報「缺 00-overview.md（近似檔名掃描也無——真缺檔）」，
  但管理者在該領域資料夾**看得到**該檔（L28 修完參數污染後再現）。
- 診斷缺口：訊息只說「查無」，沒說**查了哪個目錄、那裡有什麼**——
  人無法分辨三種完全不同的成因：(a) 真缺檔；(b) 掃錯目錄（雙胞胎
  資料夾／repo 副本／$PSScriptRoot 反推到別棵樹）；(c) 檔名內藏
  隱形字元使 `*overview*` 比對失敗。三者處置南轅北轍（還原／換路徑／
  改檔名），沒收據就只能盲猜。
- 落點：機械化——缺 overview 的違規一律附掃描收據
  「掃描目錄：<path>｜該目錄 N 個 .md」；N=0 時訊息直接指向
  「極可能掃錯目錄，跑 ps-fs-doctor 查代號 C」。
- 原則（L24 的自我應用再進一步）：**查無收據不只要寫「怎麼查」，
  還要寫「查了哪裡、看到什麼」**——否定結論的可診斷性來自現場資訊，
  不是來自措辭。
- 套用：本 commit（lint；測試 T24/T25）。

### L39 Test-Path 的萬用字元陷阱＋腳本位置即設定（2026-08）
- 症狀：ps-fs-doctor 判 **A（檔案精確存在）**，同一台機器同一領域
  lint 仍報「缺 00-overview.md」。
- 根因（兩個都修）：(1) **`Test-Path -Path` 會把路徑當萬用字元樣式
  解析**——路徑或領域名含 `[` `]` 時，存在的檔案照樣判不存在；
  doctor 用 `[IO.File]::Exists()`（字面）故找得到，兩者結論相反。
  本課撰寫時連測試腳本自己的 `Set-Content -Path` 都中同一陷阱，
  可見其普遍性。(2) **腳本位置是設定**：`$root` 由 `$PSScriptRoot`
  反推，腳本沒放在 `<repo>\scripts\` 就整棵樹歪掉，且以前完全無聲。
- 落點：機械化——(1) lint 全面改 `-LiteralPath`／`Get-Content
  -LiteralPath`（11 處）；(2) 開頭加位置護欄：非 `scripts\` 底下
  發警告、`docs\ps-research` 不存在直接 exit 2 並印出反推的 root；
  (3) 目錄不存在的錯誤訊息附「該 research 根下現有領域清單」
  （拼字/雙胞胎一眼可見）。
- 原則：**路徑一律 LiteralPath**——PowerShell 的 `-Path` 是樣式
  不是路徑；以及**由位置推導的設定必須自我驗證**。
- 套用：本 commit（lint；含含 `[]` 領域名的迴歸驗證）。

### L40 表名前綴憑記憶／metadata MCP 當證據／AE step 參照被當檔案行號（2026-08）
- 三個同時發生的現場問題：
  (a) 模型把 `PSPRCSRQST` 寫成 `PS_PRCSRQST` → 必然查無 → 誤以為
      「查不到排程」。cookbook 樣板本來就寫對（PeopleTools 系統表
      **不一定**有 PS_ 前綴，樣板即並存兩型），是模型自行加前綴；
      規則 8 只管欄位不管表名，留了缺口。
  (b) 查不到後改用 PeoplecodeMetadata 的 **get_process_schedule_list**
      （L1 只記載三個工具，這是第四個）回傳當證據 → lint 判證據有問題
      ——判得對：契約只認 CHUNK／SQL，metadata 回傳只作定位線索。
  (c) lint 手術單出現「缺id＠AENAME:2」看似無解，實為 **AE 名:step 序號**
      被當成 `檔案:行號`——但該工單**確實可完成**：AE step 的 SQL 在 ES
      有 chunk（`objectName=<AE名>＋componentType=ApplicationEngineProgram`
      實測可取），修法是取回該 step 的 chunk 補 id。
- 落點：資料修正＋機械化——(1) cookbook 規則 8a：表名同理禁憑記憶、
  **禁止自行加減 PS_ 前綴**，樣板沒有的表先 `all_tables` 查證；
  (2) cookbook 規則 9：明列 metadata MCP 四個工具一律不作 evidence；
  (3) lint 手術指令加 AE step 判讀提示（`<名>:<數字>` 若名字是 AE／物件名
  → 用 componentType 取 chunk）；(4) thinking 標記污染的違規訊息附處置
  「手工刪除即可，不需重取證據」（故不列入手術單，避免誤以為缺工法）。
- 原則：**「加了前綴查不到」不是資料不存在，是表名寫錯**——查無結論前
  先驗物件名本身的正確性（同 L26／L32 的鍵型錯必空家族）。
- 套用：本 commit（cookbook／lint）。

### L41 洩漏標記是寫入脫軌的尾巴，不是孤立髒字；違規要附行號（2026-08）
- 症狀（管理者查證後修正 L40 的判斷）：`</think>` 污染**不是**單一標記
  ——現場是資料流章節的表格**寫到一半斷掉**（最後一列缺欄位），接著
  模型的自言自語與 `<tool_call><function=task>` 直接灌進檔案，
  **下一章節才恢復正常**。只刪標記會留下半截表格與遺失內容。
- 落點：機械化——(1) 洩漏偵測擴大到
  `</tool_call>`／`<function=`／`<|endoftext|>`，訊息改為「檢查該行
  **前後整個區塊**，刪污染並補回被截斷內容；補不回就開重查工單」；
  (2) 新增**表格列欄位數一致性**檢查（連續表格區塊內分隔符數與表頭
  不同＝疑似寫入中斷，警告不擋——跳脫的 `|` 可能偽陽）；
  (3) 所有 Evidence 列違規與手術單項目一律附**絕對行號**
  （`檔名:行號`）——人工確認不必自己搜，工單也直接可跳轉。
- 更正：L40 對 thinking 標記寫的「手工刪除該標記即可」**過於樂觀**，
  已改為區塊級處置（作廢不刪除：原句保留於 L40，本課修正）。
- 原則：**污染標記是損壞的路標不是損壞本身**——看到洩漏要問
  「這次寫入從哪裡開始脫軌、到哪裡恢復」，處置範圍是區塊不是字元。
- 套用：本 commit（lint；五項偵測以污染 fixture 驗證通過）。

### L42 失敗查詢被當證據——訊息措辭把人推向錯誤修法（2026-08）
- 症狀（管理者查證）：那筆「缺 chunk id」的 Evidence 列其實是**排程
  metadata**：說明欄寫「<AE名>:2筆排程定義, RECURNAME=11:00:00PM,
  RunTimes=23:00:00」，機器參照欄寫「PS_PRCSRQST schema 不存在」——
  即 L40 的亂加前綴查失敗，模型把**失敗的查詢**寫進機器參照當證據。
- 兩個框架缺陷：(1) `<AE名>:2`（2「筆」）與 `檔案:行號`／`AE:step`
  形狀相同，機械無法區分——本來就不該靠形狀決定修法；(2) 違規訊息
  寫「缺 chunk id」把人與模型都推向「去找 chunk」這條錯路，而正解是
  「改寫成可重跑的 SELECT」。**訊息措辭本身就是工單**。
- 落點：機械化——(1) 訊息改為「機器參照無效（既非 36 字元 ChunkId、
  也非可重跑 SELECT）」並**同時列出兩條修法**（CHUNK 補 id／SQL 型
  改寫 SQL：<SELECT>）；(2) 新增檢查：無 chunk id 的 Evidence 列若
  含「schema 不存在／table not found／查無此表／ORA-nnnn／查詢失敗」
  ＝**以失敗查詢當機器參照**（證據必須可重現），訊息直接指向
  cookbook 規則 8a（表名禁自行加 PS_）。
- 原則：**證據必須可重現——失敗的查詢不是證據，是 gaps**；以及
  **診斷訊息要描述「不變量被違反了什麼」，不要描述「你少了什麼」**
  ——後者會鎖死修法選項。
- 套用：本 commit（lint；fixture 三項驗證通過）。
- 追記（2026-08，管理者抓漏）：L42 首版只改了**違規訊息**、沒改
  **手術單內文**——工單項目仍寫「缺id＠」、流程仍從「去找 chunk」起手，
  等於訊息與工單兩套說法，模型照工單走還是撞牆。工單已整段重寫為
  **先判型別再動手**：A. CHUNK 型（重取＋驗貨＋補 36 字元 id）／
  B. SQL／metadata 型（免 chunk id，改寫「SQL：SELECT」＋keyRows，
  失敗查詢必須重查）／C. 兩者皆不可得（移除該列＋降級 INFERRED＋
  查法收據），收據格式對應三種出路；auto-loop 的工單擷取正則相容
  （編號項格式不變）。教訓：**同一件事的訊息與工單必須同時改**
  ——只改一半的機械化＝製造兩個互相矛盾的指示。

### L43 工單派工必須照圍堵架構——叫沒有工具的 agent 做事＝逼它亂代償（2026-08）
- 症狀：L42 重寫後的工單 B 路線（SQL 型改寫）貼給 PS-DEEP-RESEARCH，
  模型回「OracleMCP 只有文件類參考、沒有 SQL 執行工具」，然後改去查
  peoplecode source → 無限迴圈。
- 根因（在工單不在模型）：oracleMCP 執行權限只在
  ps-metadata-flow／ps-ae-flow／ps-ui-flow／ps-auditor 四個 subagent，
  **主 agent 本來就被 deny（圍堵設計）**——工單卻寫「重跑正確查詢」
  沒寫「委派給誰」。模型「沒有 SQL 工具」是**照實描述自己的視野**；
  接著用它有的工具（peoplecode 檢索）代償 DB 查詢，才是災難。
- 落點：機械化——工單 B 路線改寫：(1) 明寫**委派對象**（三個 oracle
  權限 flow）；(2) 明寫「你看不到 SQL 工具＝圍堵設計不是故障，
  **禁止改查 peoplecode 代償**（DB 事實不在程式碼裡）」；(3) 合法
  終止出口：委派仍不可得 → 輸出「舊值 → 待人工SQL」收據**停止該筆**
  （管理者照 SOP-2 第 4 階自跑），不得換工具重試——堵無限迴圈。
- 原則：**工單的每個動作都要寫明「由誰執行」，且該執行者必須真的
  有那個工具**——在圍堵架構裡，省略派工對象＝逼收單者用錯的工具
  代償；以及每條路線都要有「做不到時的合法終點」。
- 套用：本 commit（lint 工單 B 路線）。

### L44 整合點沒被測到——auto-loop 呼叫 lint 這條線壞了兩處（2026-08）
- 發現方式：為第一次 loop 測試新增 `-Preflight`（唯讀啟動前檢查）
  後**立刻**曝光兩個潛伏 bug——它們單元測試全綠、真跑必炸：
  (a) `Invoke-Lint` 用 `2>&1` 捕捉輸出，但 lint 以 **Write-Host**
      輸出（information stream，`2>&1` 抓不到）→ `$raw` 空 →
      issue #2 加的「lint 死亡防呆」（exit 0 但無 PASS 字樣＝視為死亡）
      把**每次成功都誤判成 exit 3** → VALIDATION_OK 永遠假
      ＝**永遠畢不了業**，且工單擷取永遠落空、自動手術從不觸發；
  (b) L42 把工單標題從「手術式修復指令」改成「證據修復指令」時，
      **漏改 auto-loop 解析它的正則** → 同樣工單擷取不到。
- 根因：測試只覆蓋「lint 自己」與「auto-loop 的函式」，**沒有覆蓋
  「auto-loop 呼叫 lint」這個跨腳本整合點**；輸出格式是兩支腳本之間
  的**介面**，改它等於改 API。
- 落點：機械化——(1) `Invoke-Lint` 改 `*>&1`（全流合併）；
  (2) 工單標題正則兩版都認；(3) `-Preflight` 模式入庫（環境／相位／
  lint／StrictAudit／收據一次列印，不啟動 session、不取鎖），
  搬運後與每次首跑前必跑——它就是整合點的煙霧測試。
- 原則：**跨腳本的輸出格式是 API**——改標題／改輸出前先 grep
  「誰在解析它」；以及**整合點要有端到端檢查**，單元全綠證明不了
  管線通。
- 套用：本 commit（auto-loop）。
- 追記（2026-08，雙 BOM 第二種面貌）：同一個搬運雙 BOM，症狀依**檔案
  結構**而異——檔首是註解（lint）→ 執行期「'#' 不是 cmdlet」；
  檔首註解後接 `param()`（auto-loop）→ **解析期 InvalidLeftHandSide**
  （多出的 FEFF 讓第 1 行變成一個「指令」，`param()` 不再是檔案第一個
  語句 → 被當函式呼叫解析 → 裡面的預設值賦值全非法，錯誤指向
  param 區塊各行）。兩者根因相同、修法相同（ps-fs-doctor -FixBom）。
  doctor 的 D 檢查已附此症狀對應。原則：**同一根因在不同檔案結構下
  會偽裝成完全不同的錯誤**——診斷要看根因不看訊息措辭。

### L45 搬運完整性要機械驗——doctor 加語法解析＋行數（2026-08）
- 症狀鏈（同一天連三發，全在人工搬運鏈）：ps-auto-loop 雙 BOM →
  param 解析錯（L44 追記）；修完換 ps-graduation.ps1 報
  「Missing close {」＋69:47 unexpected token——該行是
  `Reason = "無收據" }`，**中文字串在貼上時壞掉、引號未閉合**，
  後續全被吃進字串。auto-loop 的版本檢查因 dot-source 失敗而報
  「版本不符」exit 2（快炸設計正確運作，但訊息指向版本、真因是語法）。
- 缺口：搬運後沒有**機械驗收**——BOM 有 doctor 的 D 檢查，
  「檔案有沒有貼完整／字串有沒有壞」卻只能等執行時才炸，
  而炸點的錯誤訊息常指向別處（版本不符）。
- 落點：機械化——ps-fs-doctor 新增**檢查 S**：以
  `[Parser]::ParseFile` 解析每支 `scripts\*.ps1`，失敗者列出前 3 個
  錯誤（行/欄/訊息）並指路「重新從 GitHub Raw 複製整檔」；成功者
  印出**行數**供對照搬運清單（雙重驗收：語法＋長度）。代號 S 入表。
- 原則：**每個人工步驟都要有對應的機械驗收**——人工搬運是本框架
  最大的非確定性環節，它的驗收不能靠「跑跑看會不會爆」。
- 套用：本 commit（ps-fs-doctor；以壞檔 fixture 驗證）。

### L46 記事本之謎——PowerShell 挑 .ps1 shim，cmd.exe 用檔案關聯把它「開啟」（2026-08）
- 症狀：loop 開跑後**每圈跳出記事本**、關掉才繼續；管理者觀察
  「看起來沒找 opencode，直接就走完流程」。
- 根因（在外環不在模型）：npm 同時裝 `opencode`／`opencode.cmd`／
  `opencode.ps1`，而 **PowerShell 的 `Get-Command` 優先回 .ps1**
  （它把 .ps1 當一等公民）。auto-loop 把該路徑交給 `cmd.exe` 執行——
  **Windows 對 .ps1 的預設「開啟」動作是記事本**（防止雙擊誤執行的
  安全設計）→ 記事本開啟並**阻塞**；關掉後 cmd 回 **exit 0** →
  外環判定「session 正常結束」→ 整圈空轉、模型從未被呼叫。
  腳本註解早寫著「npm shim 是 .cmd，經 cmd.exe 呼叫最穩」，
  **解析卻沒挑 .cmd**——註解寫對、程式沒做。
- 落點：機械化——`Select-OpencodeShim`：`Get-Command -All` 後
  **按 .cmd → .exe → .bat 優先序挑**；一個都沒有就 exit 2 並明說
  「cmd.exe 會用檔案關聯開啟它（記事本）而不是執行它」；
  Preflight 印出解析結果與候選數。
- 附帶硬化（同 L1 覆寫表原則）：內建 agent 覆寫檔（general／explore／
  scout）只封了 4 個 MCP，**bash 沒列＝預設開**——委派漏到內建時
  仍可執行 shell。已補 `bash/write/edit/webfetch: false`
  （本案非此因，但同族側門要一起封）。
- 原則：**exit 0 只代表「那個東西結束了」，不代表「那個東西是你以為的
  東西」**——外環驗收要驗「做了什麼」，連「呼叫到誰」都要先驗對。
- 套用：本 commit（auto-loop／三個內建覆寫檔）。

### L47 subagent 契約 JSON 原樣洩漏進交付物——原料未加工就出貨（2026-08）
- 症狀（首次 auto-loop 實跑後檢查 90-audit.md）：「上輪回灌項覆核」
  之後直接接**模型的推理獨白**（自創標題 `## I have to give solution
  based on the reasoning…`）＋**整段 subagent 回報 JSON**（contract
  格式：agent／searchScope／findings／coverage…）。
- 定性：與 L41 同族（寫入脫軌）但簽名不同——L41 是「寫到一半斷掉」，
  本課是**「把原料當成品交出去」**：委派回來的契約 JSON 應由主 agent
  **消化成報告文字**，它卻整段貼進交付物，並把自己的思考當章節標題。
- 落點：機械化——lint 新增檢查：90-audit.md 與 NN 檔命中 ≥3 個契約鍵
  （`"agent"`／`"searchScope"`／`"findings"`／`"coverage"`／
  `"deliveredFallbackUsed"`／`"structureLines"` 等）＝JSON 洩漏違規，
  訊息附行號並說明「契約 JSON 是原料，必須消化成報告文字；同段常伴
  推理獨白，一併清」。門檻設 3 個鍵避免誤傷正常引用單一欄位名的散文。
- 處置：90-audit.md 每輪 audit **整檔重寫**——下一輪稽核會自然覆蓋，
  不必人工修（要現在乾淨才手動刪該段）。
- 原則：**交付物只能裝成品**——工具回傳、subagent 回報、模型自己的
  推理都是原料，出現在交付物裡就是製程沒走完。
- 套用：本 commit（lint；fixture 驗證命中且乾淨檔不誤報）。

### L48 監測門檻要照實測基線設——過敏警告訓練人忽略警告（2026-08）
- 症狀：新加的「輸出沉默偵測」門檻設 10 分鐘，第一圈就報警
  「輸出已靜止 11 分（疑卡在工具呼叫）」——但**同一天的實測基線**是
  健康的 audit session 沉默約 30 分、總計 35 分正常完成。
- 根因：門檻憑直覺設（「10 分鐘沒動應該有問題吧」），沒有用**手上
  已有的實測數據**校準。委派期間 subagent 的輸出不流到父行程 stderr，
  「安靜」是這個工作型態的常態而非異常。
- 危害：過敏警告比沒有警告更糟——每次都叫會訓練使用者忽略它，
  真正的停滯訊號被淹沒（狼來了）。
- 落點：門檻 10 → **20 分**，且措辭改中性（「委派期間長時間無輸出屬
  常態，實測健康可達 30 分；接近逾時上限仍無輸出才需查通道」）。
- 原則：**任何門檻（逾時、沉默、重試次數）都要用實測基線設，不用
  直覺設**；同一原則已用於 timeout 校準（實測 ×1.5），警告門檻同理。
  沒有基線數據時，先跑一輪收集，別急著設。
- 套用：本 commit（auto-loop）。

### L49 判定所依賴的值自己讀不到——外環的裁判瞎了，卻照樣宣判（2026-08）
- 症狀（第一次完整 auto-loop 實跑）：console 顯示
  `SESSION(audit) 結束 exit= 耗時 34 分`——**exit 後面是空的**；
  跑完最後停在「連續 2 次 session 錯誤」，但 out/err 檔看不出任何錯誤，
  兩圈其實都正常完成（未勾 4／已勾 6／稽核輪次 20）。
- 根因：`Start-Process -PassThru` 取得的行程物件，在**只用
  `WaitForExit(ms)` 等待**時 `.ExitCode` 常為 `$null`（PowerShell 已知
  行為：handle 未被快取住，行程結束後拿不回結束碼）。而
  `$null -eq 0` 是 **false** → `$sessionOk` 恆假 → **每個正常結束的
  session 都被記成錯誤**，第 2 圈就湊滿熔絲停機。
- 更嚴重的第二層：`SESSION_OK` 是三層畢業門的第一層——它恆假等於
  **這個框架永遠不可能畢業**，而且症狀只是「跑一跑就停」，不會有人
  往「畢業門壞了」的方向想。
- 併發的第三個 bug：err 尾巴用 `Get-Content` 讀但**沒給 `-Encoding
  UTF8`**，PS 5.1 預設 ANSI 解碼 → 中文全亂碼 → 唯一的診斷線索也讀不到。
  三個 bug 疊起來＝「停機了、原因欄空白、log 是亂碼」。
- 落點：機械化——**不相信 API 承諾，改要可觀測的事實**：
  1. cmd 命令尾巴接 ` & call echo %^ERRORLEVEL% > "<stamp>-<tag>.rc.txt"`
     （`%^ERRORLEVEL%` ＋ `call` ＝延後展開，取到的才是 opencode 的真碼），
     結束碼變成**磁碟上的檔案**，事後也能翻。
  2. 讀取優先序：rc 檔 → `Process.ExitCode` → **仍讀不到就視為 0 並大聲
     記一行警告**。方向刻意選「讀不到＝當正常」：反向已實證會把健康的
     run 誤停，而真正的失敗還有 err 檔／一致性檢查／轉移檢查三道網。
  3. 補 `$null = $p.Handle`（啟動後立刻快取 handle）＋結束後無參數
     `WaitForExit()` ＋ `Refresh()`；err 尾巴補 `-Encoding UTF8`。
- 原則：**任何「拿來下判定」的值，都要先確認自己讀得到**——外環的
  價值全在「確定性地判定有沒有做」，判定輸入是 `$null` 時，外環不會
  沉默失效，它會**照常宣判、而且判錯**。凡是判定輸入，一律
  (a) 落成可觀測的檔案，(b) 讀不到時走明確的預設值＋大聲記錄，
  絕不讓 `$null` 悄悄參與比較。
- 附帶：讀不到判定值時的預設方向，要選**「不會靜默終止整個流程」**的
  那一邊；讓熔絲寧可晚一圈才燒，也不要因為讀不到而誤燒。
- 套用：本 commit（auto-loop；19 項新驗證＋既有 73 項回歸全綠）。

### L50 單一等級的畢業門＝廣度歸零——兩段式畢業（2026-08）
- 症狀（管理者提出，非事後檢討）：第一個領域跑到第 20 輪稽核，未勾項
  不減反增（一輪回灌 11 項全是新項）。管理者的問法直指要害：
  「這些有盡頭的一天嗎？現在才一個 domain，未來多個 domain 每個都沒
  盡頭，wiki 永遠不可能有一個 80 分的版本。」
- 根因：畢業門只有一級，而且是 100 分門（未勾=0＋lint 全綠＋
  StrictAudit 全綠）。未勾項的三個來源裡只有一個會自己收斂：
  (a) 新物件探勘前緣——有限，會收斂；
  (b) 品質補強——SOP-13 已載明「修復寫入以低固定率播下新小瑕疵」，
      遞減但不歸零；
  (c) 標準變動——維護 session 一個月從 L28 加到 L49，每加一條檢查
      ＝所有既有檔案重新不合格。**終點線是我們自己在移動的。**
  三者疊加：第一個領域吃掉全部時間，其餘領域停在零分。
- 更隱蔽的一層：相位判定「未勾>0→research」讓 tier 1 永遠進不了 audit
  ——勾掉 2 個、稽核回灌 3 個，未勾淨增，於是永遠不進稽核＝永遠畢不了
  業。進度熔絲同理：照未勾數判進度，會把「做了 5 項工」的圈判成卡住。
- 落點：機械化——兩段式畢業（SOP-16）：
  1. lint 加 `-CoverageOnly`：**不是新增檢查，是把既有檢查分類**。
     分界線＝內容層算缺料、驗證層算美工。白名單制，未列者一律缺料
     （fail-safe：漏分類只會讓門更嚴）。
  2. tier 1 門＝session＋transition＋CoverageOnly 全過；不看未勾數、
     不看 StrictAudit。tier 2 門＝原三層門一字未改。
  3. tier 1 的相位與進度熔絲改量「缺料違規數」，不量未勾數。
  4. 收據記 `tier`，`Test-GraduationReceipt -RequiredTier` 比對；
     排程器預設兩趟（全領域 tier 1 → 全領域 tier 2）。
  5. schemaVersion／gateVersion 同時 1→2（門判定改了就得 bump）。
- 設計時踩到的坑（測試抓出來的）：原本把「Evidence 附錄空白」歸缺料，
  但 id 格式壞掉的檔會**同時**觸發「格式違規（美工）」與「附錄無有效
  證據（缺料）」——美工降級形同虛設，而該領域正是 uuid 問題為大宗。
  改成整個驗證層歸美工，分界線才自洽。**分類的正確性要由 fixture 驗，
  不能靠讀規則覺得合理。**
- 代價要講明白：tier 1 只保證「讀得懂、查得到、沒被截斷」，
  **不保證每句話能回溯驗證**——那是 tier 2 的工作。
- 原則：**完工標準要分級，否則廣度歸零**。單一的完美門在「產出會自我
  播種缺陷」的系統裡等於沒有終點；把「可用」與「完美」拆成兩道門、
  排程改廣度優先，才會有「所有領域都可用」的那一天。
- 附帶紀律：衝刺期**凍結標準**——每加一條檢查＝全部收據失效＋終點線
  往後移。要加，等領域畢業後再加。
- 套用：本 commit（lint／auto-loop／auto-all／graduation；新增 28 項
  tier 驗證，既有 92 項回歸全綠）。

### L51 檢查只掃了一半的檔——洩漏標記在 checklist 裡活了下來（2026-08）
- 症狀（管理者在 tier 1 畢業後翻檔發現）：`checklist.md` 的「Gaps 彙整」
  節混進 `<think>`，而該領域**照樣通過了 tier 1 畢業門**。
- 根因：L41 加的「模型內部標記洩漏」檢查寫在 **NN 檔的迴圈裡**——
  只掃 `01-*.md` 這類功能細查檔。模型同樣會寫的 `checklist.md`、
  `00-overview.md`、`90-audit.md` **完全沒被掃過**。檢查存在、規則正確、
  訊息也寫得很好，但**掃描範圍少了一半**。
- 為什麼沒早點發現：洩漏在 NN 檔被抓過好幾次（L41），每次都修好、
  每次都以為「這個檢查有在運作」。**檢查有沒有跑**與**檢查掃到哪些檔**
  是兩件事，而後者從來沒有人驗過。
- 影響：洩漏＝寫入脫軌＝缺料類，本該擋下 tier 1。這是**畢業門漏放**，
  不是分類錯誤——tier 1 的承諾（沒被截斷或污染）在 checklist 上沒有兌現。
- 落點：機械化——把洩漏掃描從 NN 迴圈裡搬出來，改成
  **領域目錄下全部 `*.md` 統一掃一遍**（同一個 pattern、同一種訊息、
  每個標記只報一次）。不挑檔：**模型寫得到的檔，就要掃得到**。
- 附帶：新增 fixture 驗四件事——checklist／00-overview／90-audit 的洩漏
  都算缺料且擋 CoverageOnly、NN 檔行為不變且不重複回報、乾淨領域不誤報。
- 原則：**加檢查時要同時決定「掃描範圍」，而且範圍要被測試釘住**。
  一條只覆蓋部分目標的檢查，比沒有檢查更危險——它會讓人以為那類問題
  已經有人在看。凡是「對某類檔做某種檢查」的規則，範圍就是規則的一半。
- 代價：改 lint ＝ `lintScriptHash` 變動 ＝ 既有收據全部失效，該領域要
  重跑一輪拿新收據。這正是「衝刺期凍結標準」要避免的成本；本次是在
  該領域畢業之後才動手，代價為一張收據。
- 套用：本 commit（lint；新增 5 項驗證，既有 120 項回歸全綠）。

### L52 畢業訊息沒提凍結快照——導航頁停在一個月前，畢業照樣宣布（2026-08）
- 症狀（管理者在 tier 1 畢業後的人工抽驗）：文件層全部通過、基礎 lint
  零違規，但 `00-overview.md` 的產生日期仍是階段一那天（約一個月前），
  而畢業訊息與收據都沒有任何一句提到這件事。
- 定性：**不是 bug，是收尾流程缺一句話**。00-overview 凍結是刻意設計
  （L2：防大檔覆寫截斷），SOP-15 也早就寫好換版程序、第一個觸發時機
  就是「畢業收尾」——但那條時機**需要人自己記得**。lint 有機械 diff
  （功能地圖缺 N 項）也只是眾多警告之一，會被淹在警告牆裡。
- 落點：機械化——tier 1 畢業當下讀 CoverageOnly 的輸出，把功能地圖
  diff 的結果**寫進畢業訊息**：有缺頁就點名幾項並指向 SOP-15；無缺頁
  也講一句「產生日期仍是階段一那天，是否換版由人判斷」。
- 為什麼不做成畢業門：agent 對凍結檔維持零寫入是 SOP-15 的前提
  （換版走人工覆蓋草稿），做成門會逼自動流程去寫它、破壞凍結設計；
  而且導航頁失真不影響「查得到、讀得懂」——它是可用性的下一層。
- 順帶記錄：該領域 CoverageOnly 的降級筆數為 **0**——代表基礎 lint 也
  全綠，證據層在數輪手術後真的清乾淨了。tier 2 的剩餘距離不是證據
  品質，而是「未勾清零」與「StrictAudit 全綠」兩項。
- 原則：**流程文件寫了的事，不等於流程會發生**。凡是「某個時機該做
  某件事」的規則，都要問「那個時機到的時候，誰會講這句話？」——
  沒有人講，就等於沒有這條規則。
- 套用：本 commit（auto-loop；不影響畢業判定，故不 bump GateVersion）。

### L53 擋得住門卻沒有修復管道＝保證活鎖——洩漏補進工單並分流（2026-08）
- 症狀（管理者要工單時才發現）：洩漏標記是違規、而且是缺料類（擋 tier 1
  畢業門），但 lint 的手術工單**只收證據型項目**——洩漏永遠不會出現在
  工單裡。自動迴圈拿不到修復指令，只能一圈一圈重跑同一個 FAIL。
- 定性：與 L43 同族。L43 是「工單沒指定委派對象＝模型做不到」，本課是
  **「有違規、沒工單」＝連做都不會被叫去做**。任何「擋門但無修復管道」
  的違規類別，在自動迴圈裡的結局都是活鎖。
- 落點：機械化——
  1. 洩漏併進同一張工單，項目加型別前綴 `[洩漏]`／`[證據]`；auto-loop 的
     手術 prompt 對應改成**先看方括號型別再分流**（否則洩漏型會被套上
     證據型修法，被叫去找一個根本不存在的 chunk id——又是 L43）。
  2. 洩漏型修法寫全：**不是刪掉標記就好**——看前後整個區塊有無被截斷、
     刪非交付內容、補回被截斷的內容（證據照原 id／SQL 重取，禁憑印象
     重寫）、補不回就在未解事項明寫遺失後停止該筆。
  3. **可委派範圍收窄**：只有 NN 檔進工單。checklist／archive 是熱檔
     （模型整檔重寫會吃掉未勾項）、00-overview 已凍結（agent 零寫入）、
     90-audit 下輪稽核整檔重寫會自然消失——這三類另印
     「人工處理清單（**不要**貼給模型）」，各自附不同理由。
- 為什麼要分可委派／不可委派：把做不到的事寫進工單，等於逼自動迴圈
  去違反自己的禁令（手術 prompt 明文禁改 checklist.md 與 90-audit.md）
  ——那是製造矛盾指令，不是給任務。
- 原則：**每一條會擋門的違規，都必須配一條可執行的修復路徑，或一個
  明確的人工出口**。兩者皆無的檢查不是防線，是死結。加檢查時要一起
  回答：這條紅燈亮了，誰去修、怎麼修、修不了怎麼合法收場。
- 套用：本 commit（lint／auto-loop 手術 prompt；新增 9 項驗證，
  既有 125 項回歸全綠）。

### L54 滅不掉的提醒——換版做完了，警告照樣每輪叫（2026-08）
- 症狀（管理者直接反映）：「換版到底要怎麼標註？我有標了但仍說沒換版。」
- 根因：那條提醒的條件只有 `稽核輪次 >= 3`，**從頭到尾沒有讀過
  `00-overview.md`**。它報的是「這個領域跑了幾輪」，不是「導航頁有沒有
  刷新」——所以照 SOP-15 做完換版、標記寫得再標準，它照樣每輪出現。
- 定性：L48 的反面案例。L48 是門檻太敏感所以狼來了；本課是**警告根本
  沒有滿足條件**——使用者做了正確的事、看不到回饋，只會學到「這條可以
  無視」，而它旁邊那些真警告一起被無視。
- 第二層問題：SOP-15 只用散文寫「標『第 N 版（於稽核輪次 R 換版）』」，
  沒說那是**機器要讀的字串**。使用者用自己的話標，機器讀不到——
  規則要求的格式必須被機器檢查，否則就是願望不是規則。
- 落點：機械化——
  1. 提醒改讀 00-overview 的換版標記：落後程度＝`當前輪次 − 換版輪次`，
     ≥3 才提醒（沒標記時＝從 0 起算，行為與過去相同，向後相容）。
  2. 三種狀態三種訊息：無標記→提醒並**明講要寫哪一串字**；有版次但
     讀不到輪次→專屬提示（你標了但格式讀不到）；已換版且落後 ≥3→
     講出「上次換版在第 R 輪、之後又歷 N 輪」。
  3. 標記格式容忍全形／半形括號、有無「於」、冒號與空白差異；
     canonical 字串同步寫進 overview-template 與 SOP-15 第 3 步。
- 原則：**每一條提醒都必須有明確的熄滅條件，而且訊息要講出那個條件**。
  設計提醒時要問：「使用者照做之後，這行字會消失嗎？」不會消失的提醒
  不是提醒，是雜訊——而雜訊會連累旁邊所有真訊號。
- 附帶：規則若要求某種標註，該標註就得有 canonical 格式、寫進模板、
  並由 lint 檢查——用散文描述格式等於沒有格式。
- 套用：本 commit（lint／overview-template／SOP-15；新增 6 項驗證，
  既有 134 項回歸全綠）。

### L55 「有寫 SQL 三個字」就算證據——豁免寫成關鍵字，反面表列漏掉整類（2026-08）
- 症狀（管理者畢業後逐檔翻閱）：Evidence 附錄的機器參照欄出現只有
  「OracleMCP SQL」或只有「PeopleCode chunk」一行字的列——**兩個都是標籤
  不是證據**，稽核重跑時什麼都跑不了。lint 全綠。
- 根因有兩層，都出在同一段判定：
  1. **豁免寫成關鍵字**：判定式是 `$line -notmatch '\b(SQL|SELECT)\b'`
     ——只要那列出現「SQL」三個字母就被當成 SQL 型證據而豁免。
     「OracleMCP SQL」正中這個豁免。附錄空白檢查用同一招（`\bSQL\b`），
     所以整份附錄只要寫著「SQL」就算「有證據」。
  2. **反面表列**：違規條件要求該列「長得像 檔名:行號」才進判定。
     機器參照只寫「PeopleCode chunk」沒有冒號數字，**整列不進判定**——
     不是判它合格，是根本沒判過。
- 定性：與 L26（錯 key 必空）同族的方法錯誤——**用「像不像」判定，
  而不是用「能不能重跑」判定**。證據契約的唯一意義是可重跑，判定式卻
  在比對外觀特徵。
- 落點：機械化——改成**正面表列**：附錄裡每一列都必須自己帶得出
  (a) 完整 36 字元 UUID、(b) 真的 `SELECT … FROM`、或 (c) `待人工SQL`
  （合法待辦出口，不違規但列警告點名「這些主張尚未經機器驗證」）。
  三者皆無＝違規並進手術單。附錄空白檢查同步改用同一組判準。
- 分寸（管理者實測補充）：真實檔案有「證據其實在**位置**欄、機器參照欄
  放標籤」的情況——整列有可重跑的東西＝證據沒丟、稽核追得到，故
  **降為警告**（點名列號說「機器參照欄應放可重跑的那一份」），不擋門、
  不進手術單。判定以**列**為單位，不以欄為單位。
- 自己踩到的範圍錯誤：第一版用「看到分隔列 `|---|` 才開始驗」，遇到缺
  分隔列的表格會整段一列都不驗（與 L51 同型，被自己的回歸測試抓到）。
  改成直接認表頭欄名，缺分隔列時照驗——**fail-safe 方向：判不出來就驗，
  不要略過**。
- 回歸測試改判：T23 原本斷言「純時間 23:00 的列 exit 0」，那是把
  「整列沒證據」當合格——正是本課的洞。改判為違規，但保留 L37 的本意
  （工單不得給出「＠23:00」這種無解路徑，該列改給「無路徑線索」）。
  **改測試預期必須有實證理由**：這裡的理由是真實檔案已經在利用這個洞。
- 原則：**豁免條件要用「這東西能不能被重跑」寫，不要用「有沒有出現某個
  關鍵字」寫**；判定要正面表列（列出什麼算合格），不要反面表列（列出
  什麼算違規）——反面表列永遠會漏掉你沒想到的那一類。
- 套用：本 commit（lint；新增 12 項驗證，既有回歸全綠 153 項）。

### L56 合法出口只寫在 lint 的輸出字串裡——模型沒看過它，只好自己編（2026-08）
- 症狀（管理者實測）：某領域 30 列證據的機器參照欄全是「PeopleCode chunk」
  「OracleMCP SQL」這類標籤，checklist 的 Gaps 寫著「OracleMCP 連線限制：
  部分證據因無資料庫連線而 Unverifiable」——而管理者確認 **Oracle 從未中斷**。
- 第一層解讀（錯的）：「模型偷懶又編藉口」。
- 真正的根因：`待人工SQL`（查不到時的合法出口）**只存在於 ps-doc-lint.ps1
  印出來的手術工單字串裡**——grep 全專案，agent／command／模板**零命中**。
  模型在一般研究流程從來沒看過這條路。它遇到取不到證據的列時，手上沒有
  任何被認可的「我沒有」寫法，於是自己發明了一個：欄位寫標籤、Gaps 寫理由。
- 定性：**規則寫在模型看不到的地方，等於沒有規則**。同族於 L43（工單沒指定
  委派對象＝做不到）與 L53（有違規沒工單＝不會被叫去做），這次是
  **有出口但沒公告**——三者都是「制度上做得到、實務上到不了」。
- 落點：機械化＋文件化——
  1. 出口寫進**模型看得到的兩個地方**：`function-detail-template.md` 的
     Evidence 附錄註解、`ps-deep-research.md` 的證據規則。並且**照型別分**：
     SQL／metadata 型取不到 → 機器參照寫 `待人工SQL`（管理者自跑後回填）；
     CHUNK 型取不到 → **移除該列**並把主張降級 INFERRED。兩者都要在
     「未解事項」記一行查法收據。
  2. lint 加交叉檢查：文件裡出現「無法連線／連線限制／通道中斷」等受限
     宣稱，而**全領域零列標 `待人工SQL`** → 警告點名，並同時給兩條出路
     （真受限就走出口／通道正常就把宣稱刪掉）。理由：一句散文可以免除
     整批舉證責任，而且事後**無法分辨「真的不通」與「沒去查」**；走出口
     才是申報，散文不是。
- 附帶原則：**申報必須是結構化的、可計數的**。允許模型「說自己做不到」，
  但那句話要落在機器讀得到的欄位裡，否則它就是免責聲明而不是資訊。
- 套用：本 commit（template／ps-deep-research／lint；新增 6 項驗證，
  既有回歸全綠 159 項）。

### L57 不是拿不到證據，是把兩欄寫反了——加「欄位」型修法（2026-08）
- 症狀（管理者逐檔比對後的關鍵觀察）：同一份 Evidence 表格裡四種寫法並存——
  (1) 機器參照「OracleMCP」＋位置欄放 SQL；(2) 機器參照「PeopleCode chunk」
  ＋位置欄放完整 UUID；(3) 機器參照放 ChunkId＋位置欄放檔案路徑（**正確**）；
  (4) 機器參照放 SQL（**正確**）。第 3、4 種出現在**同一個檔案**裡。
- 定性：**證據其實查到了，是欄位寫反**。同一檔內格式不一致＝模型對欄位
  語意的掌握不穩，不是取證能力不足。這個判讀直接改變修復成本：
  重查 30 列要 5 輪手術（每輪一個 session）；**對調欄位是純編輯，零工具呼叫**。
- 落點：機械化——工單新增第三種型別 `[欄位]`：
  · 進單條件＝該列有完整 UUID 或真 SELECT，但**機器參照欄**（四欄制的最後
    一欄）放的是標籤 → 不是違規（證據追得到）但要進工單，因為修它最便宜。
  · 修法明寫「**純編輯，不要重查、不要呼叫任何工具**」與「證據內容一個字
    都不要改——改短、改寫、憑印象重打就是捏造」。
  · auto-loop 的手術 prompt 同步加 `[欄位]` 分流（放在最前面）。
- 判定要件：**只在四欄制上判錯放**。舊的三欄列（編號｜位置｜說明）根本沒有
  機器參照欄，拿「最後一欄」當它會把乾淨檔判成錯放——被自己的回歸測試
  T15 抓到。範圍條件寫錯的第三次（L51／L55／本課），三次都是同一種：
  **判定前沒有先確定「這個判定適用於什麼形狀的輸入」**。
- 附帶：本次也順手踩到 PowerShell 反引號——`Write-Host "…`filePath`…"` 裡的
  反引號是跳脫字元，整段字串炸掉導致腳本解析失敗。輸出字串裡要引用程式碼
  一律用「」或不加符號，別用反引號。
- 原則：**先分清「做不到」與「做錯了」，修法完全不同**。看到一批壞資料時，
  先找同一份檔案裡**做對的那幾列**——它們證明能力沒問題，問題在一致性，
  而一致性問題該用最便宜的修法（對齊、對調），不是重做。
- 套用：本 commit（lint／auto-loop 手術 prompt；新增 8 項驗證，全套 167 項全綠）。

### L58 一圈只修 7 筆——修復吞吐被稽核 session 綁住（2026-08）
- 症狀（管理者問「一定要人工建工單？loop 不能自動解掉？」時算出來的）：
  loop **本來就會**自動餵手術 session，但一圈只修 7 筆；而 tier 1 的相位判定
  多半落在 audit——等於**每修 7 筆就先燒一個 60 分的稽核 session**。
  實案：欄位錯放 30 餘列＋缺證據 30 餘列，逐批要 6 圈 ≈12 小時，
  其中一半時間在跑對修復毫無幫助的稽核。
- 定性：批次大小（≤7）是為了**prompt 長度**設的，卻被當成「一圈一批」的
  節流閥用——兩個不同的約束被綁在同一個數字上，於是吞吐被無關的東西限制。
- 落點：機械化——
  1. 一圈可連跑多批（`-MaxSurgeryPerCycle`，預設 3），**續修條件＝上一批
     確實讓手術清單變短**；沒變短就停止本圈續修（防空轉活鎖），交下一圈
     或人工。批次仍是 ≤7（那個限制的理由沒有變）。
  2. `[欄位]` 型工單**按檔合併**成一個任務（一個檔一次改完），30 列錯放
     從 30 個任務變成數個檔案級任務——純編輯本來就該以檔為單位。
  3. 手術後一致性 FAIL 仍要中止整圈：多批迴圈裡的 `break` 只會跳出內圈，
     另立 `$fatalStop` 旗標，否則致命狀況會被降級成「本圈少修一批」。
- 原則：**限制要各自對應各自的理由**。同一個數字同時扮演「單次 prompt
  多長」與「一圈修多少」時，改善其一必然被另一個綁架——先把兩個約束
  拆開，再各自調。
- 附帶（回歸測試抓到兩次）：工單輸出格式一改，斷言就得跟著改；而斷言若
  寫得太鬆（比對「出現 `[欄位]` 三個字」），連工單**說明文字**裡的字樣都
  會誤命中。斷言要對準結構（編號行），不要對準關鍵字。
- 套用：本 commit（auto-loop／lint；新增 6 項驗證，全套 173 項全綠）。

### L59 判讀要寫在事件發生的那一行——事後翻心跳拼真相，等於沒有儀器（2026-08）
- 症狀（管理者回報）：audit 撞 60 分上限被強殺。管理者查了三個地方後說
  「err 檔完全看不出意義、auto-loop.log 看不出問題，因此**我無法回答
  Oracle 是否有連線**」——並補充 Oracle 是 PeopleSoft 的正式庫、有強大備援，
  本來就不可能斷。
- 真相：**答案早就在 log 裡**。心跳一路跳到最後、沒有出現「輸出已靜止」＝
  模型全程都在吐字＝**不是卡死，是真的跑得久**。儀器有作用，但它要求讀者
  自己把「連續心跳」翻譯成「未卡死」——**判讀留給人，人就答不出來**。
- 定性：與 L49 同族但方向相反。L49 是判定值讀不到；本課是**判定值讀得到、
  但沒有人把它轉成結論**。診斷資訊散在三個檔案裡、需要跨檔推理，實務上
  等於沒有診斷。
- 落點：機械化——強殺當下就用輸出檔 mtime 算出「靜止幾分」並**直接寫結論**：
  · 靜止 ≤5 分 → 「輸出仍在增加＝**上限太短，不是卡死**，調高 -AuditTimeoutMin，
    不要去查 MCP 通道」（訊息依 Tag 指名該調哪個參數）
  · 靜止 ≥20 分 → 「疑似卡在工具呼叫，依 SOP-12 查通道，**調高上限沒有用**」
  · 之間 → 「先看 out 檔尾端停在哪個步驟再決定」
- 附帶調參：audit 上限 60 → **120 分**，而且理由不是「怕不夠」——是
  **真實時長目前不知道**（被殺掉了量不到）。設寬是為了**把它跑完、量出來**，
  拿到完成時長後再照實測 ×1.5 收斂。設寬的成本是壞掉時多等一小時；設窄
  的成本是永遠量不到、而且每輪白燒。
- 順帶學到：稽核時長會**隨領域檔案數成長**（34 分 → >60 分），所以固定上限
  遲早都會錯——真正的解是「量得到 → 定期回頭校準」，不是猜一個大數字。
- 原則：**儀器要輸出結論，不是輸出原始資料**。凡是需要讀者跨檔比對才能得到
  的判讀，都要在事件發生的那一行就寫成人話——尤其當兩種可能的處置**完全
  相反**時（調上限 vs 查通道），沒有結論的資料只會讓人猜錯方向。
- 套用：本 commit（auto-loop；auto-all／SOP-14 的最壞時長公式同步更新）。

### L60 headless 的權限詢問不會被拒絕，它會永遠阻塞——而且 log 裡看不到（2026-08）
- 症狀（三輪、共約 4.5 小時才定位）：auto-loop 的 audit session 做完 13/15 個
  檔（約 10 分鐘）後**輸出永遠靜止**，60／90／120 分三種上限都一樣撞滿。
  同時管理者另開 console 實測 MCP 與模型效率**全部正常**。
- 定位過程（值得記，因為前兩步都錯）：
  1. 先懷疑「稽核變慢」——用「56 分做完 13 檔」推出每檔 4.3 分、再推出
     「證據補完整→呼叫變多→時間翻倍」。**整條錯**：拿到「心跳 61 分、
     輸出靜止 50 分」才發現 13 檔其實只花 11 分鐘，其餘 50 分是卡死。
     **把等待時間算進工作時間，就會憑空造出一個不存在的效能問題。**
  2. 接著懷疑通道死。但另開 console 測 MCP 正常——這其實不能證明什麼
     （新 session 開新連線，測不到卡住那個 session 手上那條）。
  3. 真正定位靠**互動式重跑同一個領域**：跳出 permission 提示、點 Allow
     後幾分鐘就跑完。SOP-2 升級梯的精神——自動化說不出為什麼，就降到人工看。
- 根因：`ps-auto-loop.ps1` 檔頭原本寫「不加 `--auto`：ask 類權限**自動拒絕**
  （doom loop 之類會被自動擋下＝特性）」。**這句是錯的。** 查證後（opencode
  官方文件）：`doom_loop`（重複的相同工具呼叫）與 `external_directory`
  （專案目錄外路徑）兩個權限**預設就是 "ask"**，其餘預設 "allow"；headless
  沒有 TTY 可以回答，"ask" 就是**永遠阻塞**。而權限提示是畫在 TTY 上的互動
  元件，stdout/stderr 重導到檔案後**在 log 裡一個字都看不到**。
- 第二個陷阱：TUI 上點「always allow」**只在該互動 session 內有效、不落檔**
  （官方文件明寫 "for the rest of the current OpenCode session"）。所以
  「我剛剛點過了」對 headless 完全沒用——每次開新 session 都會再撞一次。
  維護 session 一度建議去磁碟上找被改動的授權檔，那是**基於錯誤假設的
  無效指示**，白花使用者一次操作。
- 落點：檔頭註解全部改寫成查證過的事實；SOP-17 加第 0 條「必要前提」——
  在**本機全域** opencode.json（已註冊 MCP 的那份，含內部主機名、不進 repo）
  設 `"permission": { "doom_loop": "allow", "external_directory": "deny" }`。
  · **doom_loop 設 `deny`**（本課二次修正，實測定案）。初版寫 allow，理由是
    「稽核會對同一個 chunk id 重複取用＝正當行為，deny 會擋掉真驗證」——
    **那個理由建立在把 doom_loop 讀成『反覆呼叫』上，是錯的**：提示原文
    「continue after **repeated failures**?」管的是**反覆失敗**，重複成功的
    呼叫根本不會觸發它。錯誤的 allow 造成第二次事故：subagent 反覆讀一個
    不存在的 tool-output 檔，**靜止 30 分以上、零輸出、零提示**——
    我們把一個看得見的死鎖換成了看不見的死鎖。改 deny 後同一份稽核順利跑完。
    三種設定的實際行為：ask＝卡死但看得見；allow＝無限重試、看不見；
    deny＝快速失敗、看得見。**確定性失敗不該被重試**（L61 已立此原則），
    allow 等於在設定層打開無限重試，與該原則正面相反。
  · **【實測確認】觸發的是 `doom_loop`**：提示文字「continue after repeated
    failures?」、pattern 顯示 `invalid`。`invalid` ＝工具呼叫 JSON 被截斷
    （SOP-9 的老病），模型重試、再截斷，連續失敗即觸發 doom_loop 詢問。
    **因果鏈是：證據列變多 → 稽核回報 JSON 變長 → 截斷 invalid → 重試 →
    doom_loop → ask → headless 死鎖。** 這也解釋為什麼偏偏是那一個檔
    （該檔證據列最多），以及為什麼「證據補完整之後」才開始發生。
    **權限只是最後一環，根因在寫入長度**——設 allow 只是不要死鎖，
    真正的緩解仍在 SOP-9。
  · 判斷過程的曲折要記下來：維護 session 先猜 doom_loop（對），被
    管理者的「subagent 會用暫存檔」觀察一講就改口 external_directory
    （**過度修正**），最後由管理者實際看到提示文字才定案。**兩次都是
    在沒有直接證據時用推理決定設定值**——正確做法是把兩個都設成不會
    死鎖的值，然後等實證。管理者那個觀察沒有白費：external_directory
    設 deny 確實會擋掉暫存檔讀取，那個故障是真的，只是不是這次的元兇。
  · external_directory 設 **allow**（初版寫 deny，管理者當場擋下——**那會
    造成故障**）：opencode 自己會把過長的工具回傳寫進 `%TEMP%` 再讓模型
    讀回來，那個路徑在專案目錄外；設 deny＝subagent 讀不到自己剛存的資料。
    這也最能解釋「為什麼偏偏卡在第 14 檔」——該檔證據量大到觸發暫存機制，
    所以前 13 檔沒事。**主嫌從 doom_loop 改判為 external_directory。**
  · **不用 `--auto`**：那是把所有非 deny 的 ask 一次放行，範圍過大。
- 原則零（管理者當場修正的那條）：**放寬權限前要先知道那個權限實際在
  擋什麼**。我用「研究只在 repo 內」這個**想當然耳的前提**推出 deny，
  而真實情況是工具鏈自己就在用專案外的暫存路徑。凡是要把某個東西設成
  deny，先問「有沒有我不知道的正當用途會走這條路」——答不出來就不要設 deny。
- 原則一：**「沒有人可以回答的問題」等於死鎖。** 任何在無人環境跑的東西，
  凡是可能觸發互動詢問的設定，都要事先明確設成 allow 或 deny——沒有
  第三種安全狀態。
- 原則二：**看不見的阻塞比看得見的錯誤危險得多。** 這次的提示連 log 都
  進不去，唯一的症狀是「靜止」。凡是有互動元件的工具，headless 化時
  第一件事就是問「它會不會問我問題，而我聽不到」。
- 原則三（給維護者自己）：**推論之前先確認數字的定義。** 「13 檔花了
  56 分」與「13 檔花了 11 分、之後卡了 50 分」是完全不同的世界，而兩者
  在 log 上只差一個「輸出靜止」欄位——我先接受了模糊描述才去蓋解釋。
- 套用：本 commit（auto-loop 註解／SOP-17）；設定本身在管理者本機，
  repo 不放（含內部主機名）。

### L61 名字對、server 錯——工具身分是「前綴＋名稱」，錯一半就等於不存在（2026-08）
- 症狀（管理者互動式重跑時看到的實際錯誤文字）：
  `PeoplecodeSource get_chunk_by_id error=Model tried to call unavailable tool`。
- **第一版診斷是錯的**：維護 session 查了 repo，發現全專案沒有
  `get_chunk_by_id`，就判定「模型自創工具名」，還寫進 agent 檔「沒有
  `get_chunk_by_id` 這種東西」。**管理者當場更正：它存在，是
  `PeoplecodeElasticSearch` 的工具**——`PeoplecodeSource` 的解引用工具才叫
  `get_chunks_details`。那句錯誤的規則若照搬進去，會教模型一件假的事。
  （repo 查無，是因為 `mcp-tool-contracts.md` 自稱「全部 MCP Tool 契約總覽」
  卻**漏列了 ES 的這個工具**——文件不全，於是「查無」被誤讀成「不存在」。）
- 真正的根因：**模型把對的工具名掛到錯的 server 前綴上**。工具身分是
  「server 前綴＋工具名」，錯一半就等於不存在 → `unavailable tool` →
  模型**重試同一個錯組合** → 連續失敗觸發 doom_loop → headless 死鎖整輪。
- 三層診斷鏈（L60 只看到第三層）：
  **掛錯 server → unavailable tool（invalid）→ 重試同名 → doom_loop → ask →
  headless 死鎖**。設 `doom_loop: allow` 只解最後一環。
- 一個訊息、三種成因，長得完全一樣：(1) 名字打錯／自創；(2) **名字對但
  掛錯 server**；(3) **本 agent 對該 server 是 deny**（要改成委派）。
  三者都**不是暫時故障**——重試必然再失敗。
- 附帶的協定澄清：就算掛對 server，**ES 的 `get_chunk_by_id` 也不該用來
  解引用**——ES 回傳一律是候選（SEARCH_CANDIDATE），拿索引當證據違反證據
  契約。解引用只有 `PeoplecodeSource_get_chunks_details` 一條路。
- 為什麼「build 模式測起來正常」不能反證：build agent 沒被同名覆寫
  （L1：tools 是覆寫表，沒列＝預設開），而且人工測的是**掛對 server 的
  那個呼叫**——測的不是同一件事。**「我另外測過是好的」必須先確認測的是
  同一個呼叫（含前綴）。**
- 為什麼偏偏是某一個檔：抽樣事故（L34 同族），不是該檔特殊；fresh session
  重跑多半就過。
- 落點：ps-auditor／ps-deep-research 各加「工具身分＝前綴＋名稱」對照表，
  **指名這個混淆**（`get_chunk_by_id` 是 ES 的、不是 Source 的），並附
  「unavailable tool 三種成因、都不可重試」的判讀；`mcp-tool-contracts.md`
  補上同一段（文件漏列正是這次誤判的來源）。
- **維護 session 在同一課裡第三次犯同一種錯（過程完整記錄，因為它最有教育性）**：
  1. 先以為 `get_chunk_by_id` 不存在（其實只是文件漏列）→ 差點寫進 agent 檔
     一條假規則。
  2. 更正後推論「它回傳的是索引副本，用它解引用＝靜默假 PASS」，於是在六個
     agent 的 tools map 全部 deny。
  3. 管理者更正：**兩邊資料源一致、回傳欄位相同**。那個 deny 的前提不成立，
     而且**deny 一個實際可用的工具＝製造 `unavailable tool` → 重試 →
     doom_loop → headless 死鎖**——正是本課要修的那個事故。
     **我差點親手複製一份同樣的地雷。** 已全部撤除。
  三次的共同點都一樣：**在沒有直接證據時，用推理決定一個會影響執行的設定值**。
  L60 才剛寫過「放寬或收緊權限前要先知道那個權限實際在擋什麼」，隔一則就再犯。
- 定案的處置：**不封鎖，用規則規定用途**。解引用固定走
  `PeoplecodeSource_get_chunks_details`——理由不是「ES 是假證據」，而是
  **索引依定義是副本，CR 上線到索引重建之間會落後**（SOP-11 的對齊流程就
  建立在這個時間差上），固定走 Source 才能讓證據失效在稽核時現形。
  ES 版的**正當用途是交叉檢查**：Source 查無時用它再查一次——ES 有、Source
  無＝該 id 曾真實存在但來源已變（判 stale，**不是捏造**）；兩邊都無才較
  可能是捏造。**兩種失敗的處置不同，值得分辨**，這一步反而讓稽核更準。
- 原則零之二：**封鎖只留給真正有害的東西**。「非正規但無害」用規則講；
  deny 一個可用的工具，在 headless 環境等於埋一顆死鎖地雷——**封鎖的代價
  在這個框架裡不是零**。
- 原則一：**指出「你可能會弄混的那兩個」比列出正確清單有效**。純白名單
  防不住「名字對、前綴錯」——因為模型自認為在用清單上的東西。
- 原則二：**確定性失敗不該被重試**。工具不存在、掛錯 server、權限被 deny、
  表名不存在——重試一次和一萬次結果相同。規則要教模型分辨「暫時故障」
  與「事實錯誤」：前者可重試，後者只能改做法。
- 原則三（給維護者自己，本課第二次踩）：**「我 grep 不到」不等於「不存在」**
  ——先確認那份文件是否本來就完整。這次是拿不完整的契約總覽當事實來源，
  推出一條假規則，差點寫進 agent 檔。
- 附帶修補：`mcp-tool-contracts.md` 補上**兩個 server 的完整工具清單**
  （管理者實測確認 ES 只有 `search_chunks` 與 `get_chunk_by_id` 兩個）——
  這份文件自稱「全部 MCP Tool 契約總覽」卻漏列，正是本次誤判的來源。
- 套用：本 commit（ps-auditor／ps-deep-research／六個 flow agent 的
  tools map／mcp-tool-contracts）。

### L62 規則寫給模型的檔案裡混進了寫給人的敘事——每個 session 都在付費（2026-08）
- 症狀（管理者提問）：「引言這麼多，模型小不會造成問題？」——L61 收尾後，
  `mcp-tool-contracts.md` 檔頭累積 39 行引言區塊，ps-auditor 的對應區塊
  近 30 行。回頭盤點：同一條規則（解引用只走 Source）講了**四遍**，還混進
  「維護 session 一度加了 deny 又撤掉」的**事故敘事**——那是給維護者看的
  歷史（applied.md L61 已一字不差存在），對幹活的模型毫無用處。
- 危害分兩級：`mcp-tool-contracts.md` 是**按需讀取**的參考，代價有上限；
  **agent 檔是每個 session 都載入的 system prompt**，那裡的膨脹每一輪都在
  付費。對小模型的實際傷害：**重複會稀釋**（同一規則講三遍，每一遍的權重
  都變低）、**敘事會分心**（模型不需要知道規則的來歷，只需要規則）。
- 根因：連續事故下逐次「最小新增」，每次都只加不整——五次追加疊出一座
  沉積層。「只加不刪」的本意是**保護既有規則不被誤刪**，不是「同一 session
  剛寫的東西也不准整併」。
- 落點：三檔收緊——規則一條不少，重複與敘事全刪：contracts 檔頭 39→27 行
  （表格＋三條硬規則）；ps-auditor 區塊近 30→17 行；ps-deep-research
  區塊 13→6 行。事故敘事只活在 applied.md。
- 原則：**給模型的檔案只放「現在該怎麼做」，給人的檔案才放「為什麼變成
  這樣」**——兩層混寫，模型付 token、人找不到史。追加規則時順手自問：
  這段話裡有沒有「曾經」「一度」「當時」？有，就該搬去 applied.md。
- 套用：本 commit（mcp-tool-contracts／ps-auditor／ps-deep-research）。

### L63 補救路徑被規則自己封死——「請開新 session」的那個 session 讀到同一條禁令（2026-08）
- 症狀（管理者回報）：loop 的 audit 相位再度卡死；人工開新 session 打
  `/ps-audit <領域>`，模型**不動工**，反問「in run 稽核會因 context 壓縮
  導致計分卡缺漏……需要我幫你做別的嗎？」。
- 根因：L29 規模門原文寫「已完成檔總數 > 5 時**一律禁止當場稽核**
  （**含「新 session 重跑」路徑**），請開新 session 執行 /ps-audit」。
  但 `/ps-audit` 掛在**同一個 agent**（ps-deep-research）底下——新 session
  讀到同一條規則：15 > 5、含新 session 路徑、一律禁止 → 拒絕，再叫使用者
  開新 session。**規則指定的補救路徑，被規則自己的適用範圍蓋住**：按字面
  讀，> 5 檔的領域從此沒有任何合法的稽核入口。之前幾次能跑，只是模型
  剛好把節標題（「全部打勾後接稽核」＝research 收尾）讀進範圍——
  範圍靠標題暗示而不是明文，就是抽樣運氣。
- 同節還有一條直接矛盾：「checklist 全勾且本 run 尚未稽核（如新 session
  重跑）→ 執行稽核」與規模門的「含新 session 重跑路徑一律禁止」互撞，
  靠「優先於下列全部條款」裁決——裁決結果就是死路。
- headless 的對應症狀：`--command ps-audit` 的 session 同樣反問——沒有
  TTY、沒有人能回答，loop 只看到沉默或零轉移。
- 落點：
  1. 規模門節首加**明文範圍**：「本節只管 research run 收尾的自動接跑；
     收到明確稽核指令的 session（/ps-audit、headless --command ps-audit）
     ＝規模門指定的那個新 session 本身，本節全部條款對它不適用，
     立刻執行、不得反問」。
  2. `ps-audit.md` 指令端對稱聲明：「你這個 session 就是那個新 session，
     上限不適用，規模再大也照樣執行——分批委派本來就是你的作業方式」。
  3. 刪掉「（含新 session 重跑路徑）」與矛盾 bullet（≤5 條款的
     「全勾重跑 0 勾也算」已涵蓋其合法情境）。
- 原則：**任何「請去做 X」的補救指示，都要檢查執行 X 的環境會不會又讀到
  同一條禁令**——補救路徑必須被明文豁免，否則規則是遞迴死路。同族：
  L53（擋門但無修復管道）；本課是**有管道，但管道被規則自身堵住**。
  範圍只靠節標題暗示＝沒有範圍（對小模型尤其如此），要用「本節只管／
  不適用於」的明文寫。
- 套用：本 commit（ps-deep-research／ps-audit）。

### L64 索引重建＝全部 chunk id 輪替——收據不知外部世界、成批查無不是成批捏造（2026-08）
- 情境（管理者先問假設、隨即確認**已經發生**）：Source／ES 重建，
  文件與 wiki 引用的舊 chunk id 全面失效。
- 走一遍現有機制會發生什麼：lint 不打 MCP（格式仍合法）→ CoverageOnly
  照樣綠 → audit 解引用全滅 → 二次定位救回、回灌 A 項 → **tier 1 照常
  畢業**（驗證層屬 tier 2 契約）→ tier 2 被 A 項擋住 → research 逐項
  重取新 id。復原路徑存在（SOP-11 設計情境），但有兩個盲區：
  1. **收據不會自動失效**：contentHash 只看檔案內容，重建不動 .md——
     已畢業領域在 batch 被 SKIP，而它每條引用都是死的。收據只證明
     「檔案曾通過門」，**不證明「外部世界沒變」**。
  2. **L61 交叉檢查在成批輪替下系統性誤判**：兩邊一起重建 → 舊 id
     兩邊都查無 → 每筆都落入「ES 也無＝較可能捏造」。但**捏造是零星的，
     成批查無是環境事件**——15 檔同時全滅只可能是重建。
- 落點：
  1. auditor 加優先規則：本輪 ≥3 檔 id 查無＝重建訊號，全部判 stale
     走二次定位、不判 FABRICATED，報告表頭標「疑似索引已重建」；
     contracts 同步一句。
  2. SOP-11 補兩條：重建完成檢查清單必含「對已畢業領域手動 /ps-audit
     或 batch -Force」（讓收據經由內容變動自然失效）；整庫輪替的預期
     行為寫明（tier 1 仍會畢業，第一輪要看 90-audit 不要只看畢業訊息）。
- 原則一：**收據系統的邊界要說出口**——hash 能守住「檔案沒變」，
  守不住「檔案引用的世界沒變」。跨出邊界的失效只能靠程序（SOP-11），
  程序就要有 checklist 條目，不能靠人記得。
- 原則二：**零星錯誤與成批錯誤是不同的事件**（L19 同族：成批 view 查無
  ＝schema 問題不是 view 不存在）。判定規則要先看分布再定性——
  單筆查無問證據，成批查無問環境。
- 套用：本 commit（ps-auditor／mcp-tool-contracts／SOP-11）。

### L65 「看得見的失敗」勝過「看不見的重試」——無人環境的設定判準（2026-08）
- 事件鏈（同一個權限設定，兩次事故）：
  1. headless 稽核卡死 → 查出 `doom_loop` 預設 `ask`、無人可答（L60）。
  2. 維護 session 建議設 `allow`，理由是「稽核會重複取用同一個 chunk id＝
     正當行為，deny 會擋掉真驗證」。
  3. 改 allow 後**第二次卡死**：subagent 反覆讀一個不存在的 tool-output 檔，
     **靜止 30 分以上、零輸出、零提示**——比第一次更難查，因為連提示都沒了。
  4. 改 `deny` 後，同一份稽核**順利跑完**。
- 根因是**語意讀錯**：提示原文「continue after **repeated failures**?」管的是
  **反覆失敗的呼叫**，不是「反覆呼叫同一個東西」。重複成功的呼叫根本不會
  觸發它——建議 allow 的那個理由從一開始就不存在。而管理者早在第一次就
  把原文貼出來過，字面上寫著 failures。**證據在手上，讀錯了。**
- 三種設定的實際行為（實測）：
  · `ask`（預設）＝跳提示、headless 無人可答 → 卡死，**但看得見**
  · `allow`＝**無限重試失敗的呼叫** → 卡死，**完全看不見**
  · `deny`＝停止重試 → **快速失敗、看得見**
- 與 L61 的關係：L61 已立「確定性失敗不該被重試」（工具不存在、掛錯 server、
  檔案不存在——重試一萬次結果相同）。設 `allow` 等於**在設定層打開無限重試**，
  與框架自己的原則正面相反，而且是在模型層規則管不到的地方打開。
- 原則一：**無人環境的設定判準是「失敗看不看得見」，不是「會不會被擋」。**
  可見的失敗有人會查、有 log 可讀、熔絲抓得到；不可見的重試只表現為
  「靜止」，而靜止在任何儀表上都長得一樣。寧可多擋、不要靜默續試。
- 原則二（給維護者自己，本課第五次同型）：**使用者貼出的原文要逐字讀完再
  下判斷**。「repeated failures」四個字就在畫面上，我卻用自己腦中的
  「doom loop＝重複迴圈」去理解它，於是推出一個相反的設定值。
  同型前科：以為工具不存在（其實文件漏列）、以為回傳是索引副本（其實同源）、
  以為暫存在專案內（其實在專案外）、以為 audit 變慢（其實是卡死）。
  **共通點都是：手上有原始資料，卻用推理蓋過它。**
- 套用：本 commit（SOP-17 第 0 條、L60 落點修正）。

### L66 派工只指名一半、以及自己對自己並行——兩個舊教訓的漏網區（2026-08）
- 兩個獨立症狀，同一輪 loop 內先後出現：
  1. **稽核卡死**：err 顯示一次派出 6 個委派（A 項 3 批＋任務 C 3 批），
     完成打勾**亂序**出現後全體靜止、零輸出零錯誤；改循序後同一份稽核
     順利跑完。
  2. **手術卡死 90 分**：err 最後顯示「fix truncated chunk ids」交給
     **general agent**——而 general 是補鎖覆寫檔，**四個 MCP 全封＝零工具**。
- 症狀 1 的根因：oracleMCP 是**單工、有狀態**的單一連線（SOP-12 早已載明
  「各 session 的 disconnect 可能互拆共用連線」）。多個 subagent 同時跑
  connect→查→disconnect，會在同一條連線上互拆，全部等一個不會回來的回應。
  **SOP-12 只規範了「跨視窗不並行」，沒管稽核自己 session 內的並行委派**
  ——外面關得很嚴，裡面自己開了六條。
- 症狀 2 的根因：**L43 只修了一半**。L43 的教訓是「叫沒有工具的 agent 做事
  ＝逼它亂代償」，落點把工單 B 路線（SQL 型）的委派對象指名到三個具
  oracleMCP 權限的 flow；但 **A 路線（CHUNK 型）當時只寫「委派對應 flow
  subagent」，一個名字都沒有**。模型照著模糊指示挑了 general。
  且三個內建覆寫檔（general／explore／scout）**漏封 `task`**——依 L1 覆寫表
  教義沒列＝預設開，於是它「自己做不到、又能再轉委派」，轉圈到逾時。
  九個 ps-* subagent 全數明列 `task: false`，只有這三個漏了。
- 管理者提出的關鍵反問（直接改掉了修法）：「如果某個委派對象找不到、
  但其實另一個找得到？例如 AE 的證據可能是 peoplecode、又可能是排程。」
  ——**這推翻了「型別→agent 一對一路由」的直覺解**：硬路由會製造新的假查無
  （L26 同族：問錯對象必空，而空 ≠ 不存在）。查工具矩陣後發現真相更簡單：
  CHUNK 型有 **5 個** agent 都具 ES＋Source（任一個都取得到 chunk），
  而 `ps-ae-flow` 與 `ps-metadata-flow` **四工具全譜**（程式碼與 DB 兩面都追）
  ——AE 這種跨界情境根本不會斷。真正的死路只有三種：派到零工具的
  general／explore／scout、CHUNK 任務派到無 ES/Source 的 ps-ui-flow、
  DB 任務派到無 oracleMCP 的 peoplecode／sql／sqr-flow。
- 落點：機械化＋規則——
  1. 工單 A 路線改成**允許集合＋首選**（不是一對一硬路由），明文禁止
     general／explore／scout 與（CHUNK 型的）ps-ui-flow；手術 prompt 同步。
  2. **查無不算數，除非換過管道**：首選查無 → 改派全譜的 ps-ae-flow 或
     ps-metadata-flow 再試一次，兩個管道都查無才算查無（照搬 auditor 早有的
     二次定位紀律，補進手術路徑）。
  3. general／explore／scout 補 `task: false`。
  4. 「委派一律循序」寫進 ps-audit.md／ps-deep-research.md，SOP-12 補指向。
- 原則一：**指名派工要指名到底**——同一張工單裡有一條路線指名、另一條寫
  「對應的那個」，模糊的那條就是下一次事故。修 L43 型缺陷時要掃過**全部**
  路線，不是只修觸發當下的那一條。
- 原則二：**約束自己併發，不只約束別人**。凡是宣告「這個資源是單工」的
  規則，都要同時回答「那我自己會不會同時用它兩次」——對外的操作紀律
  管不到系統自己的扇出。
- 原則三：**路由要用「誰有那個工具」定，不要用「這是什麼型別」定**。
  型別與能力不是一對一（AE 同時是程式碼與排程）；用能力定義允許集合，
  並保留一個「全譜」對象當跨界與查無時的第二管道。
- 套用：本 commit（lint 工單／auto-loop 手術 prompt／三個內建覆寫檔／
  ps-audit／ps-deep-research／SOP-12）；既有 173 項回歸全綠。

### L67 把「顆粒度過粗」誤讀成「併發有害」——循序化把單點故障放大成全域故障（2026-08）
- 事件：L66 依「err 顯示 6 個委派、打勾亂序」判定並行是卡死主因，改成
  「委派一律循序」。**下一輪只派一個 subagent，照樣卡死、零輸出零資訊。**
  管理者的判讀推翻了 L66：上一輪真正的問題是**單一 subagent 被交付 3 個任務**
  （顆粒度過粗），不是同時有幾個在跑；而且卡死本身是**機率性的**。
- 我讀錯了什麼：從「畫面上同時列著 6 個委派」推出「它們並行造成互拆連線」。
  實際上那是**顆粒度**的表徵——一個委派涵蓋多個 A 項區間／多個任務，
  輸出量過大而卡住。**同一組現象，兩種讀法，我挑了需要更多假設的那一種**
  （互拆連線是推論；顆粒度過粗是畫面上直接看得到的）。
- 更重要的是循序化的**反效果**：subagent 卡死是機率事故（L34：抽樣事故、
  非確定性障礙），而框架**沒有委派層級的逾時**（外環只殺整個 session）。
  在這個前提下：
  · **並行** → 一個卡住只損失那一個委派，其餘照常完成，**部分進度保得住**
  · **循序** → 單點故障 ＝ **整輪停擺**
  我等於把爆炸半徑從「一個委派」放大到「整輪」。
- 落點：撤回「一律循序」，改為兩條有實測依據的約束——
  1. **一個委派只做一件事**（一個檔的一個任務）；禁止把多檔／多 A 項區間／
     多任務塞進同一個委派。這才是上一輪真正的病。
  2. **併發上限 3**——照 SOP-12 自己的實測值（3 subagent 同呼成功，再多會在
     單工 oracleMCP 上排隊逾時），不是禁止並行。
- 原則一：**在「機率性故障 ＋ 無局部逾時」的系統裡，並行是韌性手段不是風險**。
  討論要不要並行之前先問：某一路卡住時，其他路的成果保不保得住？
  沒有局部恢復機制時，循序等於把所有雞蛋放進一個籃子。
- 原則二：**兩種讀法都能解釋同一組現象時，選需要較少假設的那個**。
  「顆粒度過粗」在畫面上直接可見；「並行互拆連線」需要一層推論。
  我挑了推論那個，而且它剛好與我當時腦中的 SOP-12 印象吻合——
  **符合既有假說的解釋最容易被高估**。
- 未解：**卡死的根因仍然不明**（單一委派、零輸出、零錯誤、無提示）。
  已排除：權限提示（L60/L65）、並行互拆（本課）、零工具派工（L66）。
  下一步是找 subagent 自己的執行紀錄，而不是再猜一個原因。
- 套用：本 commit（ps-audit／ps-deep-research／SOP-12；撤回 L66 的循序條款，
  L66 其餘落點（派工指名、補鎖檔封 task）不受影響）。

### L68 文件描述的能力 ≠ 實際存在的能力——靜默空回傳讓死路看起來像活路（2026-08）
- 事件：管理者告知 PeopleCode ES 的 `componentType` 新增 `sqr`／`sqc`，
  「現在可以查到這兩者的 source code 了」。我盤點 repo 後回報：
  「沒有任何『SQR 查不到』的宣稱，框架一直假設可檢索，所以這是能力升級
  不是 bug 修復。」
- 真相：框架確實一直假設可檢索——**而那個假設從頭就是錯的**。
  SQR／SQC 在 PeopleSoft 是**檔案系統上的獨立檔**（`$PS_HOME/sqr` 與客製
  目錄，由 Process Scheduler 呼叫），不在 PeopleTools 表裡；2026-08 之前
  索引根本沒收，`search_chunks` 對這兩型必回空。而協定 §2 把它們列為正式
  source type、§4.3/4.4 定義了切片單位、SKILL 寫 `sourceTypes:["SQR","SQC"]`、
  agent 寫「程式名搜 → get_file_structure」——**一條描述完整、從未通電的路**。
  §6.0「現行環境對映」從頭到尾沒提 `sourceTypes` 怎麼對映到真參數，
  因為那個對映不存在。
- 我的推理錯在哪：我把「**沒有壞消息**」讀成「**有好消息**」。repo 裡搜不到
  「SQR 不可用」的字串，我就推出「所以一直可用」。但能力缺口的正常表現
  就是**沒有人寫下它**——會被寫下來的是**已知**缺口；未知缺口只會以
  「空回傳」現身。**缺席不是證據。**
- 為什麼外環抓不到：這是少數幾種**外環結構上看不見**的失效。索引沒收該
  型別 → 回空 → 依 L26「空 ≠ 不存在」，agent 沒有任何辦法分辨「索引不收
  這個型別」與「這支程式不存在」→ 它誠實地寫「查無」→ lint 判為合格 gap
  → 畢業門放行。**錯誤長得跟正常運作一模一樣**，沒有任何訊號會亮。
- 落點：
  1. componentType **已確認值表**進協定 §6.0（唯一權威表）：
     `ApplicationEngineProgram`／`sqr`／`sqc`；未列＝未確認，禁止臆造。
  2. componentType 回零筆的 **fail-safe**（§5.1）：換大小寫重試 ＋ 拿掉
     componentType 走 semantic 翻頁，兩者皆空才算查無。值錯不報錯、
     只安靜回空——**與 L61 同形**（錯的識別字不拋錯，只是什麼都不回）。
  3. SOP-8 補「新增 source type ≠ 修既有 server」：前者自動平反無效，
     須專案式補查（**覆蓋缺口** vs 證據缺口）。
  4. 協定核心原則「**Database** 完整分段才是 Evidence」改為「**來源系統**」——
     SQR chunk 來自檔案索引，照舊字面早晚會被拿來主張「SQR 不算證據」。
- 原則一：**一份規格描述的能力若從沒被真實跑通過，它就不是規格，是願望。**
  盤點「文件寫了什麼」永遠不能取代「這條路上次真的跑通是什麼時候」。
- 原則二：**問「這個結論若是錯的，會長什麼樣子」**。這次的答案是
  「跟現在一模一樣」——那就代表現有證據對這個結論零鑑別力，
  不能拿它當支持。
- 套用：本 commit（progressive-source-retrieval §核心原則／§3／§5.1／§6.0、
  mcp-tool-contracts §3、ps-sqr-flow agent＋SKILL、ps-auditor 二次定位、
  ps-doc-lint 工單 A 路線、SOP-8）。

### L69 值域外的判定會讓整批從計分卡蒸發——塌縮且零 FAIL 是「照做」不是「放水」（2026-08）
- 事件：索引重建後第一輪 audit，管理者回報「計分卡嚴重塌縮，且沒顯示
  FAIL，但底下系統性錯誤有正確報出 index 重建的問題」。
- 根因：auditor 在三個地方被明文要求「判 stale」（wiki entity 過期、
  ES 有 Source 無、L64 成批查無），但——
  · 步驟 5 的值域只有 `PASS` / `FAIL(原因)` / `UNVERIFIABLE(原因)`；
  · JSON 回報格式的 verdict 範例只有 PASS；
  · 90-audit.md 模板章節叫「## FAIL / DISPUTED / UNVERIFIABLE 明細」。
  **`stale` 在整條輸出鏈上零定義。** 模型被要求做一個計分卡沒有欄位的
  判定，又被明文禁止判 FABRICATED——那些筆數無處可放，直接消失。
  而「系統性錯誤」那行照樣寫出來了，因為 L64 明文要求加那行、模板也有
  「## 系統性錯誤觀察」章節。**有位置的就寫了，沒位置的就蒸發了。**
- 這不是模型偷懶：它嚴格照規則走，是規則的值域有洞。同一個形狀在
  design review 已被記為 fatal（`待人工SQL` 在稽核端零定義）——
  **同一種洞出現第二次，代表這是類別問題不是個案**：凡是規則裡出現的
  判定字面，都必須在值域、JSON schema、報告模板三處都有落點。
- 第二個洞（外環）：lint 有 `Get-ScorecardCoverage` 專門測塌縮，但那條
  只在 `-StrictAudit`（tier 2 門）升為違規，`-CoverageOnly`（tier 1 門）
  看不到它。**tier 1 因此可以在「報告塌縮到無法回灌」的情況下覆蓋畢業**——
  塌縮屬「報告可不可裁決」（下一輪拿不拿得到工單），不是「證據精不精修」。
- 第三個洞（訊息代為推因）：塌縮訊息原本寫「範圍塌縮跡象——**稽核未全量
  重驗**」。但 `Get-ScorecardCoverage` 實際只量一件事：**記分卡那一節的內文
  有沒有出現每個 NN 檔的檔名**；「稽核跑了沒」它量不到。本案正是反例——
  管理者指出稽核**跑過所有檔案、也抓出全部檔案都有重建問題**，塌縮的成因
  是「成批問題被寫成一句彙總」。我照著那句推論回報「稽核沒跑過大部分
  檔案」，**被自己工具的措辭騙了**——而且是在管理者已經給出反證之後。
- 落點：
  1. 值域封閉：明寫「只有這三個，沒有第四種」；stale 改映射到既有值域
     （找到新 id → `FAIL(ID_RELINK)`；三管道皆無 →
     `UNVERIFIABLE(INDEX_REBUILT)`）。**不新增第四種 verdict**——新增就要
     同步改 lint、模板、計分卡三處，那正是這個洞的成因。
  2. 硬規則「計分卡逐筆落位」：Evidence 每筆佔一列，計分卡列數少於
     Evidence 筆數＝本次稽核無效。
  3. lint：記分卡塌縮在 `-CoverageOnly` 下升為違規（手動執行仍不擋，SOP-2）。
  4. mcp-tool-contracts 與 SOP-11 的 stale 說法同步改掉。
  5. 塌縮訊息改為只陳述量測（「記分卡未逐檔列出 N 個檔名」），
     並註明「成批問題寫成彙總句也會觸發」——不再代為推因。
  6. auditor 加「成批同因也要逐筆列」：整批同一原因失效時仍須逐檔逐筆
     佔列（reason 相同即可），**彙總句不得取代逐筆列**；系統性成因寫進
     「## 系統性錯誤觀察」是**補充不是替代**。
- 原則一：**規則裡的每一個判定字面，都要能指出它在輸出上的落點。**
  指不出來的判定＝指示模型產生一個沒有容器的東西，結果不是報錯，
  是安靜地不見——與 L68 的「靜默空回傳」同族。
- 原則二：**檢查訊息只准陳述它量到的事實，不准附帶推因。** 一條訊息寫
  「A（成因 B）」而它其實只量得到 A，那個 B 會被下游當事實用——本案就是
  我把 lint 的 B 當成現場觀察，反過來否定管理者的直接證據。推因寫註解、
  寫 lesson，不寫進使用者會讀的那一行。
- 套用：本 commit（ps-auditor、ps-doc-lint、mcp-tool-contracts、SOP-11）。

### L70 tier 1 的迴圈在做 tier 2 的工作——三個數字用三把不同的尺（2026-08）
- 事件：管理者回報「lint 怪怪的，手術清單 44 筆、一批 7 筆、分三批？」
- 查出三件事：
  1. **批量 7 是硬編碼**（`Select-Object -First 7`），不是參數；可調的只有
     批數 `MaxSurgeryPerCycle`（預設 3）。44 筆需要 7 批，一圈只給 3 批
     → 至少 3 圈，而圈 2／圈 3 的**前置 session 對清工單零貢獻**。
     L58 的註解自己就寫過這個病（「每修 7 筆就先燒一個稽核 session…
     其中一半是白跑的稽核」），但只把 1 批放寬到 3 批——**症狀減半，
     根因沒動**。
  2. **工單的產生完全不看 `-CoverageOnly`**：降級段在前、工單在後，而工單
     是從 `$truncatedIds`／`$missingIds`／`$leakDelegable`／`$misplacedRefRows`
     四個陣列直接印的，跟 `$violations` 無關。所以 tier 1 與 tier 2 拿到
     **同一張 44 筆清單**。
  3. **auto-loop 的手術迴圈用基礎 lint 驅動**（`$lint = Invoke-Lint`），
     而相位是用 CoverageOnly 決定的。於是一圈之內：相位一把尺、手術一把尺、
     畢業門又一把尺。
- 合起來的後果：**tier 1 會燒好幾個 session 去修 [欄位]／[證據] 型工單，
  而那些型別對應的違規全在 `$polishPatterns` 白名單內＝CoverageOnly 已判定
  不擋門。** 這與 tier 分段的設計目的（L50：tier 1 不保證回溯驗證，回溯驗證
  是 tier 2 的工作；lint 自己的註解也明寫）直接牴觸——**分段是為了讓每個
  領域先到 80 分，結果 tier 1 仍在做 100 分的活。**
- 根因型態：`MaxSurgeryPerCycle` 同時扮演**防活鎖**與**控吞吐**兩個角色，
  但防活鎖早就有更精準的機制（「本批未讓清單變短就停」）。一個旋鈕兼兩職，
  調整任一用途都會傷到另一個。
- 落點：
  1. lint：`-CoverageOnly` 時工單只出 [洩漏] 型（寫入脫軌會讓章節真的缺
     內容＝缺料）；[欄位]／[證據] 不出單，但**明講被抑制了幾筆**
     （無聲截斷會讀成「已經沒事了」）。
  2. auto-loop：手術迴圈的 lint 依 tier 換尺——tier 1 用 `-Coverage`。
  3. 批量參數化 `-SurgeryBatchSize`（預設仍 7），log 與 Preflight 明寫
     「本圈最多處理 N 筆」，不再讓人自己算。
- 原則：**同一圈裡的相位、工單、畢業門必須用同一把尺。** 尺不一致時，
  迴圈會朝著沒有人在驗收的方向努力——外環照樣顯示「有在做」，
  而它做的不是門在要求的事。
- 套用：本 commit（ps-doc-lint 工單分層、ps-auto-loop 手術迴圈換尺＋批量參數化）。

### L71 停機訊息指向一個不存在的檔——診斷資料只在「沒卡住」的路徑上落檔（2026-08）
- 事件：tier 1 迴圈以「連續 2 圈無進度（缺料違規 N→M；卡住的項需人工裁決）」
  停機。管理者問「缺料卡住的項目該去哪看」。收場摘要明寫
  「看 coverage-cycle*.txt（tier 1 門）」——**但那批檔在這個情境下不存在**。
- 根因：`coverage-cycle{N}.txt` 只在**畢業門**裡寫，而畢業門包在
  `if ($phase -eq "audit")` 之內。tier 1 卡住的樣子恰恰是「CoverageOnly 一直
  FAIL → 相位永遠是 research → 畢業門那段從來沒跑到」。
  **診斷檔只在事情順利時才產生**：走到 audit＝缺料已清＝那份清單是空的；
  真正需要清單的 research 相位反而什麼都沒留。
- 圈前其實有跑 `Invoke-Lint -Coverage`（用來決定相位），但只 `Write-Log` 了
  exit code，`.Raw` 直接丟掉——**手上有完整清單，卻只留下一個數字**。
- 落點：
  1. 圈前的 CoverageOnly 落 `coverage-cycle{N}-pre.txt`，並在 COVERAGE 行
     指名檔案。
  2. 無進度熔絲觸發時，圈後的 CoverageOnly 落 `coverage-cycle{N}-post.txt`，
     停機理由直接寫「清單見 coverage-cycle{N}-post.txt」。
  3. 收場摘要改成指向真的存在的檔，並給可直接複製的 lint 指令
     （現況優於快照）。
- 原則：**診斷資料要落在失敗路徑上，不是成功路徑上。** 寫落檔的時候問一句
  「這個檔在我最需要它的那天會不會存在？」——本例的答案是不會，
  而且訊息還理直氣壯地叫人去看它。
- 副則：**已經算出來的東西不要只留摘要。** 相位判斷已經跑過整份 lint，
  丟掉 `.Raw` 只留 exit code，等於花了成本卻沒收貨。
- 套用：本 commit（ps-auto-loop 圈前／熔絲落檔、收場摘要指向修正）。

### L72 我把「唯一能修它的相位」擋在門外——L69 修出來的活鎖（2026-08）
- 事件：tier 1 迴圈以「連續 2 圈無進度」停機，缺料剩兩項：
  歸檔檔含 9 個未打勾項、**90-audit.md 記分卡未逐檔列出 5 個檔名**。
- 根因（我造的）：L69 把「記分卡塌縮」從「只有 StrictAudit 升違規」改成
  「StrictAudit **或 CoverageOnly** 都升違規」。而 tier 1 的相位判斷是
  `CoverageOnly FAIL → research`。於是：
  ```
  記分卡塌縮 → CoverageOnly FAIL → 相位 research
  → research 不重寫 90-audit.md → 記分卡照樣塌縮 → 永遠 research
  ```
  **90-audit.md 只有 audit 相位重寫得了，而它自己把相位鎖在 research。**
  這是 L63（補救路徑被規則自己封死）的同一個形狀，我在寫 L69 時完全沒看見。
- 為什麼沒當場發現：我驗的是「這條規則該不該擋 tier 1」（該，塌縮的報告
  無法回灌），沒驗「**擋下來之後，誰去修它**」。門檻設計只問了「要不要擋」，
  沒問「擋住的人有沒有路走」。
- 熔絲有接住：連續 2 圈無進度正常觸發，沒有無限空轉——**但系統永遠無法
  自癒**，每次都要人工介入。熔絲擋住的是損失，不是缺陷。
- 落點：
  1. lint 在 FAIL 清單後多印一行 `AUDIT_ONLY：N 項只有 audit 相位修得了`
     （`90-audit.md` 開頭的違規）。
  2. auto-loop 的**相位判斷**改看「缺料總數 − AUDIT_ONLY 數」——
     research 修得動的清空了就進 audit，讓 audit 去修它自己的報告。
     **畢業門仍看全部缺料，標準一點都沒放寬。**
  3. 解析不到 AUDIT_ONLY 行時預設 0＝退回舊行為（fail-safe，不會因為
     lint 沒搬新版就誤放行）。
- 原則：**新增一道門檻時，必須指出「被它擋下的人走哪條路出去」。**
  指不出來的門檻不是門檻，是牆。檢查清單上該有這一問：
  「這項違規由哪個相位／哪個工單型別修復？那條路現在通嗎？」
- 套用：本 commit（ps-doc-lint 印 AUDIT_ONLY、ps-auto-loop 相位判斷扣除）。

### L73 一個崩掉的稽核 session 留下三個指紋，而畢業門只認得其中一個（2026-08）
- 事件：tier 1 停在「連續 2 圈無進度」，管理者逐項回報後拼出來的現場：
  1. `checklist-archive-r33.md` 含 9 個未打勾項，內容是 **「任務C批次 1/3」**
     這類 auditor 自己的委派切分；
  2. checklist.md 沒瘦身——r31~r33 的項目**跟 archive 兩邊都留**；
  3. `90-audit.md` 的稽核輪次停在 **32**，而 checklist 已經是 **33**；
     記分卡未逐檔列出 5 個檔名。
- 這是**同一個稽核 session 崩掉**的三個指紋，不是三個獨立缺陷：它做完了
  步驟 3（回灌／輪次遞增／歸檔）的一部分，卻在寫 90-audit.md 時塌掉，
  留下舊輪次與不完整的記分卡；而它把自己的任務 C 批次追蹤寫成了 checklist 列，
  於是那些流程標籤跟著被歸檔。
- **畢業門第 2 層對這種情況是瞎的**：`Get-AuditTransition` 讀的是
  **checklist.md 的輪次**加 **90-audit.md 的 hash**。輪次 32→33 遞增 ✓、
  hash 變了 ✓ → WORK_TRANSITION_OK **PASS**。
  **轉移層只證明「動過」，證明不了「寫完」。** 一份半成品報告完全通得過。
- 唯一識破的訊號是「90-audit 輪次與 checklist 輪次不一致」，而它**只在
  StrictAudit（tier 2）升為違規**——訊息自己都寫著「綠燈不可信」了，
  卻在 tier 1 只是一行警告。**該擋的警告沒擋**（L48 的反面）。
- 落點：
  1. 輪次不一致、表頭缺輪次 → `-CoverageOnly` 也升為違規。兩者都是
     `90-audit.md*` 前綴，自動落進 L72 的 AUDIT_ONLY，**不會製造新死鎖**。
  2. 新增檢查：**checklist.md 與 archive 重複列**（歸檔是搬移不是複製）。
     原本 lint 把兩邊合併對帳，重複只是重複計數，完全不報。
  3. 新增檢查：**稽核流程標籤寫成 checklist 列**（`任務 A/B/C`、`批次 N/M`）。
  4. archive 含未勾項的訊息補上修法，並明寫 **「本項 loop 修不掉，需人工」**
     ——沒有任何相位會去搬那些列，熔絲會如實停機，人得知道該做什麼。
  5. ps-deep-research：90-audit 表頭輪次必須與 checklist 同一個 N+1；
     歸檔是搬移不是複製；只有調查項與 A 項可以是 checklist 列。
- 原則一：**「狀態變了」不等於「工作完成了」。** 用狀態轉移證明「有沒有做」
  是對的（L50 的出發點），但轉移量測必須挑**只有做完才會變**的量。
  輪次＋hash 兩個都是「動過就會變」，選錯了量測對象。
- 原則二：**一條檢查的訊息若寫著「不可信」，它就不該只是警告。**
  嚴重度該由訊息的語意決定，不是由它剛好被寫在哪個 if 分支決定。
- 套用：本 commit（ps-doc-lint 三項、ps-deep-research 歸檔與輪次同步規範）。

### L74 我在同一批 commit 裡違反了自己剛寫下的原則——門檻分類要三分不是兩分（2026-08）
- L72 剛立下：「新增一道門檻時，必須指出**被它擋下的人走哪條路出去**。」
  L73 我就加了三個檢查（archive 含未勾項、checklist 與歸檔重複、稽核流程
  標籤），**沒有任何相位修得掉**——ps-deep-research 的硬規則明令
  「禁止 read 或改寫任何既有 `checklist-archive*.md`」，所以牽涉歸檔內容的
  一律只能人工。我在訊息裡寫了「需人工」，卻沒把它從**相位判斷**裡拿掉。
- 後果：迴圈把它們當成「research 修得動」，跑滿兩圈、燒兩個 session，
  然後停在「連續 2 圈無進度」——一個**完全沒提到人要做什麼**的理由。
  熔絲有接住，但代價是兩個 session 加一次困惑。
- 為什麼漏掉：L72 的修法只做了**兩分法**（audit-only vs 其餘）。
  「其餘」被默認等於「research 修得動」——那個默認在當下是對的，
  但我隨即加進了第三類，而分類法沒跟著長。
  **加新種類的東西時，要回頭看分類法還夠不夠用。**
- 落點：
  1. lint 多印 `MANUAL_ONLY：N 項`（白名單：`個未打勾項`／`與歸檔重複`／
     `稽核流程標籤`），並把那幾項逐條列出來。白名單制＝沒列到的預設
     「research 修得動」，讓迴圈去試（fail-safe：分類漏掉只是多跑一圈，
     分類過頭會把真能自動修的判成要人工）。
  2. auto-loop 三分：`自動 = 總數 − AUDIT_ONLY − MANUAL_ONLY`，相位只看自動那份。
  3. **自動與 audit 兩條路都是 0、只剩人工項時直接停機**，停機理由指名
     待辦與清單位置——不再跑滿兩圈才講一句本來就知道的話。
- 原則：**熔絲接住了，不代表設計是對的。** 「跑兩圈才發現沒事可做」與
  「一開始就知道沒事可做」在結果上都是停機，但前者多燒兩個 session，
  而且把「需要你動手」講成了「無進度」。
- 套用：本 commit（ps-doc-lint 印 MANUAL_ONLY、ps-auto-loop 三分類與早停）。

### L75 外環自己說謊——相位判斷有兩份實作，我只改了一份（2026-08）
- 事件：管理者依 L74 搬完新版，`-CoverageOnly` 正確印出
  `FAIL 3／AUDIT_ONLY 2／MANUAL_ONLY 1`（自動＝0，照設計相位該走 audit），
  但 `-Preflight` 顯示「起始相位：research」。
- 根因：相位判斷寫了兩次——迴圈裡一份、Preflight 顯示一份。L72／L74 把
  迴圈那份改成三分類（自動／僅 audit／需人工），**Preflight 那份還停在
  兩分法**（`CoverageOnly exit -eq 0` 就 audit，否則 research）。
- 為什麼特別糟：`-Preflight` 的存在理由就是「不啟動 session 也能知道會發生
  什麼」。它一旦與實際行為不符，**外環從「保證有沒有做」變成「謊報將要做
  什麼」**——比沒有 Preflight 更糟，因為人會照它做決定。
- 我改迴圈時完全沒想到顯示端也在算同一件事。**同一個判斷寫兩次，就是
  遲早會漂；漂掉的那一次不會報錯，只會安靜地說錯話。**
- 落點：
  1. 抽出 `Get-CoverageBreakdown`（Total／AuditOnly／ManualOnly／Auto），
     Preflight 與迴圈**共用這一份**，解析不到分類行時皆為 0＝退回舊行為。
  2. Preflight 多印「缺料分類：共 N 項＝自動 x／僅 audit y／需人工 z
     （相位只看『自動』那份）」，讓顯示的數字與判斷的數字是同一組。
  3. 只剩人工項時 Preflight 直接顯示「**不會啟動**」，與迴圈的早停一致。
- 原則：**判斷與顯示必須讀同一份計算。** 顯示端自己重算一次，等於在系統裡
  種一個永遠不會被測到的分身——它只在「兩邊剛好不同」的那天現身，
  而那天你正在拿它做決定。
- 套用：本 commit（ps-auto-loop 抽出 Get-CoverageBreakdown，兩處共用）。

### L76 誤報把畢業擋死——lint 第二次「驗字面而不是驗事實」（2026-08）
- 事件：tier 1 只剩一項 AUDIT_ONLY 違規「記分卡未逐檔列出 5 個檔名」。
  管理者打開 `90-audit.md` 實地查看：**根本沒有塌縮，五個檔全在**——
  模型只是把檔案欄寫成「01」「02」「03」而不是 `01-TW_XXX.md`。
- 根因：`Get-ScorecardCoverage` 拿**完整 basename** 去比對記分卡內文，
  編號代稱當然比不到。**檢查驗的是字面，不是它宣稱要驗的事實**
  （「有沒有逐檔列」）。
- 這是同一個函式的**第二次**同類錯誤：上一次是綁章節標題名（模型自創
  「本輪已完成檔案總覽」取代「總覽記分卡」），已改成「以結構找表、
  不綁章節名」。改了標題那一半，檔名這一半原封不動——**當時就該問
  「這個函式還有哪裡在比字串」。**
- 為什麼是死結而不只是雜訊：L69 讓塌縮在 CoverageOnly 也擋門，於是
  誤報 → 相位 audit → audit 重寫 90-audit.md → 模型照樣寫短檔名 →
  誤報 → 相位 audit……**每一輪都在做同一件事、拿同一個結果**，
  直到「audit 相位連續 2 圈零回灌」熔斷。這正是 L23 早就寫過的
  「純命名問題＝無修復管道的畢業活鎖」，我在 L69 加門檻時沒回頭看那條。
- 落點：
  1. 覆蓋判定加**編號代稱**：完整檔名比不到時，退而接受 NN 編號，
     但**只在該編號唯一對應一個檔**時才算（`NN-X.md` 與續篇 `NN-X-2.md`
     共用前綴，一個「01」證明不了兩個都列到）。
  2. 編號必須帶**錨點**——列首／清單項／第一個儲存格開頭，或任一儲存格
     **恰為**該編號（涵蓋有流水號欄的表）。行號、日期（`2026-08-01`）、
     chunk id 裡也有兩位數，不加錨點會反向誤放行。九個情境已逐一驗過。
  3. 以編號代稱通過時**發警告不擋門**：「覆蓋算數，但完整檔名才讓
     『哪一檔沒被驗』一眼可讀」——資料在就不追殺（L23），但講出來。
  4. ps-deep-research 步驟 4 明寫：記分卡檔案欄一律寫完整檔名。
- 原則一：**一條檢查要驗的是它宣稱的那個事實，不是碰巧能比對的那個字串。**
  寫檢查時問：「這件事若以另一種合理寫法呈現，我還測得到嗎？」
- 原則二：**同一個函式犯過一次形式主義，要把整個函式重看一遍。**
  修掉眼前那一半、留下另一半，等於安排下一次同樣的當機。
- 套用：本 commit（ps-doc-lint 覆蓋判定＋警告、ps-deep-research 步驟 4）。

### L77 我把「逐筆落位」寫進了錯的表、還寫進了不寫報告的那個 agent（2026-08）
- 事件：管理者問「你要不要想一下，你改了什麼讓計分卡變成沒有逐檔？之前
  有逐檔的很漂亮」。實地比對 `audit-template.md` 後確認——**我寫的規則
  跟模板直接衝突**：
  · 模板：`## 總覽記分卡`＝**一檔一列**（六個計數欄＋燈號＋合計列）；
    `## FAIL/DISPUTED/UNVERIFIABLE 明細`＝每筆非 PASS 一列。
  · 我 L69 寫的：「目標檔 Evidence 的**每一筆**都要在**計分卡**佔一列，
    計分卡列數少於 Evidence 筆數＝本次稽核無效」。
  十幾個檔、數百筆證據要塞進記分卡——模型只能把記分卡退化成統計表，
  逐筆丟去明細。**它是在解我製造的矛盾。**
- 第二個錯：那條規則被我寫進 `ps-auditor.md`，而 ps-auditor 的硬規則第三條
  就是「**不修文件、不寫任何檔案**」——它根本不寫 `90-audit.md`，寫報告的是
  ps-deep-research 的稽核模式。**把版面規則下給不排版的人。**
- 因果要誠實：第一次「計分卡塌縮」的回報**早於** L69 這個 commit，
  所以不是我造成第一次塌縮。但這條規則**會阻止它復原**——每輪 audit 讀到
  這條，都會再一次把逐筆往記分卡塞、再一次退化。修不修得回來，取決於它。
- 為什麼會寫錯：我當時抓的病是「stale 判定憑空消失」，那是對的；但我把
  修法綁在**我腦中以為的容器**（計分卡）上，**沒有回頭讀模板確認容器長什麼樣**。
  病因判對、藥下錯位置。
- 落點：
  1. ps-auditor：改成「每筆判定都要出現在**你的 JSON 回報**」，並明寫
     「你不寫 90-audit.md，不要在回報裡自製記分卡或明細表」。
  2. ps-deep-research 步驟 4：明寫兩張表分工——記分卡**一檔一列、每個 NN
     檔都要有、最後一列合計**；明細每筆非 PASS 一列；**不要把逐筆塞進記分卡**
     （附上這次的實案作為理由）。
- 原則一：**規則要下給真正動手的那個角色。** 下給旁邊那個，它只會在
  它自己的輸出裡照做，而你想改的東西一點都沒動——或者更糟，兩邊各做一半。
- 原則二：**規定版面之前先讀模板。** 我修的是「東西不見了」，卻沒確認
  「它本來應該在哪個格子」——結果把它塞進了另一個格子，擠垮了那一格。
- 套用：本 commit（ps-auditor 硬規則改寫、ps-deep-research 步驟 4 兩表分工）。

### L78 稽核查到的答案被丟在報告裡，下一輪拿不到——ID_RELINK 永遠換不掉（2026-08）
- 事件：管理者問「ID_RELINK 這件事還是有，在哪時候會換成正確的 id？」
  追鏈路發現：**目前實務上不會換。**
- 鏈路斷點：
  · auditor 二次定位找到新 id → JSON 回報 ✅
  · ps-deep-research 寫 `90-audit.md` 明細（原因欄逐字抄）→ 新 id 進報告 ✅
  · **回灌 checklist 的 A 行只有計數**
    （`A7 補查 03-X.md：FAIL 3／DISPUTED 1／UNVERIFIABLE 2`）——
    不含類型、不含 id ❌
  · 下一輪 research 處理該 A 項：規則寫「直接把新 id 寫回該筆」，
    但**整個 A 項處理流程沒有任何一處叫它讀 `90-audit.md`**；
    唯一提到那個檔的地方還是一條**禁止讀取**（本意是防寫報告時沿用舊數字）。
  → 規則寫得很清楚，但**它指涉的資料不在執行者手上**。
- 沒有其他人會補：lint 只驗 ChunkId 的**格式**，失聯但格式合法的 UUID
  完全過關——不會進手術工單、不會擋畢業門。tier 1 又不看未勾數，
  A 項可以一直躺著。**整條路上沒有任何一個環節會強迫它被修。**
- 為什麼會這樣：回灌格式當初刻意設計成「以檔為單位、一檔一行、禁止逐筆
  開項」（避免 30 列變 30 個任務），那個決定是對的；**但濃縮的同時，
  沒有替細節指定一個保存位置**。細節其實有地方放（90-audit.md 明細），
  只是沒人被告知去讀。
- 落點：
  1. A 項處理**第一步固定是 read `90-audit.md` 明細**取該檔逐筆判定，
     並明文解除與步驟 4「禁止 read 舊 90-audit.md」的表面衝突
     （那條防的是寫新報告時沿用舊數字，這裡是取本輪工單）——
     L63 的教訓：兩條規則互相封死時，要明講適用範圍。
  2. 明細的「處置」欄＝**下一輪的可執行工單**，稽核已經查到答案的類型
     必須把答案寫在那裡：`換 id → <完整 UUID>`／`更新行號 → <新行號>`／
     `更新數值 → <新值>`；其餘才寫「回灌補查」。模板同步加註與範例列。
  3. ID_RELINK 的處理改成「**抄過來，不必重做二次定位**」——稽核已經
     付過那個檢索成本了。
- 原則：**濃縮一定會丟東西；丟之前要先問「被丟的那份，誰還需要它、
  放在哪」。** 這裡的細節其實有現成的家（明細表），只差一句「去讀它」——
  斷鏈往往不是缺資料，是缺一條指路。
- 套用：本 commit（ps-deep-research A 項處理與步驟 4、audit-template 明細欄位）。

### L79 稽核查到的答案要接回修復管道——ID_RELINK 從「永遠不會換」到「下一圈自動換掉」（2026-08）
- 起點是 L78 的修法（明細帶答案、下輪 research 抄），管理者要求對抗式驗證。
  打下來發現 L78 **不夠**，而且最重要的一件事被漏掉了。
- **漏掉的事**：`ps-deep-research.md` 明訂「稽核範圍＝checklist 全部已打勾項
  （**全量重驗**）」。所以**每一輪 audit 都對同一個失聯 id 重做一次二次定位**
  （Source deref → ES 結構化 → file-mode → semantic 翻頁）。30 筆死 id
  ＝每輪 30 次三管道搜尋，而且隨文件成長。**這很可能就是 audit 36 分鐘裡
  的一大塊。** 不修的代價不是「證據不夠漂亮」，是**每輪複利重付檢索成本**。
  而 L78 的修法只在 tier 2 生效（tier 1 相位由 CoverageOnly 決定，
  而失聯 id 的格式合法、不影響 CoverageOnly）——等於 tier 1 永遠付。
- L78 被打出來的四個洞：
  1. 新 id 只存在 auditor `reason` 的自由文字裡，委派方要**抽取**才能填進
     處置欄——這框架有一堆 ChunkId 被截斷的教訓，不該靠抽取。
  2. 步驟 6 明訂「丟掉本項細節」，所以每個 A 項都會重讀整份 90-audit.md。
  3. 舊 id 在文件裡找不到時沒有規定，會硬塞。
  4. **只給新 id 根本換不掉**——修復者要靠**舊值**才知道是哪一列。
     這一條是決定性的：L78 的格式 `換 id → <新 UUID>` **自己不成立**。
- 三個方案，選 C：
  · A（L78）：明細帶答案、下輪 research 抄 → 只在 tier 2 生效。
  · B：audit session 寫完報告當場套用 → **反向操作**。audit 是崩最兇的環節
    （輪次 32/33 就是它在步驟 3、4 之間掛掉），不該再往它身上疊工作。
  · **C：lint 解析明細的「處置」欄，開成 `[回灌]` 手術工單** → lint 只做
    文字解析不需 MCP；手術是既有的獨立 session（有逾時、有一致性檢查、
    失敗不影響主流程）；純編輯無查詢，不會有迴圈風險；**不加重 audit**。
- C 的關鍵設計：
  1. **格式一欄自足**：`換 id：<完整舊 UUID> → <完整新 UUID>`。
     舊值是「定位哪一列」的唯一線索，新值是要寫入的值——缺一不可。
  2. **auditor 用結構化欄位 `newRef`** 傳新 id，`ref` 保持舊 id，
     委派方逐字複製兩個值，不做文字抽取。
  3. **防重複開單**：新 id 已出現在該檔＝已套用，不開單（否則報告重寫前
     每圈都會再開一次）。
  4. **按檔合併**：一個檔的所有回灌一個任務（同 [欄位] 型的理由）。
  5. **兩個 tier 都出**：它不需要任何檢索，經濟性與 [欄位]／[證據] 完全不同，
     不套用 tier 1 的美工抑制。
  6. **手術迴圈不再綁 `lint.Exit -eq 1`**：[回灌] 不是違規，lint 可能 exit 0
     卻仍有工單，綁 exit 會讓它永遠不被套用。
  7. 舊 UUID 全檔多處一起換（同一 chunk 常被多列引用）；找不到＝已被改過，
     記收據跳過；驗貨失敗＝抓錯，禁止硬填。
- 原則一：**評估「要不要修」時，要問的是「不修的成本會不會複利」。**
  全量重驗把一次性的資料瑕疵變成了每輪的檢索帳單——同樣一筆 FAIL，
  在抽驗式稽核裡可以拖，在全量重驗裡不能拖。
- 原則二：**交接一個答案時，要把「用得上它」所需的全部欄位一起交。**
  只給新 id 不給舊 id，收到的人拿著正確答案卻不知道要填到哪一格。
- 套用：本 commit（ps-auditor `newRef`、ps-deep-research 步驟 4 與 A 項處理、
  audit-template 處置欄格式、ps-doc-lint 明細解析與 [回灌] 工單、
  ps-auto-loop 手術 prompt 與迴圈條件）。

### L80 有門檻沒出口——四種違規擋著畢業，卻沒有任何管道去修它（2026-08）
- 事件：管理者回報「lint 有三項違規，兩項 uuid 捏造，**但卻沒出現手術文字**」。
  查證屬實：`ChunkId 非 UUID 格式（疑似捏造）` 只寫 `$violations`，
  **不進任何工單陣列**——有門檻，沒出口。
- 順手做了全面清查（把每條 `$violations +=` 對上工單陣列），找出四種同形：
  | 違規 | 類別 | 後果 |
  |---|---|---|
  | `ChunkId 非 UUID 格式（疑似捏造）` | 美工 | 擋 tier 2，零工單 |
  | `出現自編 id 樣式` | 美工 | 同上 |
  | `以失敗查詢當機器參照` | 美工 | 同上 |
  | **`subagent 回報 JSON 原樣洩漏`** | **缺料** | **擋 tier 1**，零工單 |
  最後一項最嚴重：它與「模型內部標記洩漏」是同一種病（寫入脫軌、內容可能
  被截斷），那一種有 `[洩漏]` 工單，JSON 這一種**只是換個字樣就沒人管**。
- 為什麼會漏：工單陣列是**逐條手工接線**的（`$truncatedIds`／`$missingIds`／
  `$misplacedRefRows`／`$leaks`），新增違規時沒有任何機制提醒「這條也要接」。
  8 碼縮寫那條接了，**同一個 `if/else` 的另一半就沒接**——
  兩行程式碼的距離，一個有出口一個沒有。
- 這是 L72／L74 的第三次同形。前兩次是相位分類漏了一類，這次是工單接線漏了四條。
  **原則寫下來了，但沒有任何機制在新增違規時強制執行它。**
- 落點：
  1. 捏造／自編 id → `[證據]` 工單，並在說明段明寫「那個 id 本身不可信，
     **不要試圖補全**，依 filePath／物件名重新取證，取不到走 C 路線降級移除」。
  2. 失敗查詢 → `[證據]` 工單，指定走 B 路線（表名多半寫錯，禁自行加減 PS_ 前綴）。
  3. JSON 洩漏 → `[洩漏]` 工單（缺料類，兩個 tier 都出）。
  4. 收據的 `contentHash 不符（文件已變動）` 補上逐檔 diff：
     receipt 存 `files` 逐檔 hash，失效時指名「已修改／新增／已刪除 <檔名>」。
     舊收據無此欄位＝退回舊訊息，不炸例外。
- 待辦（本次未做，留記錄）：`Evidence 附錄空白`、`行為邏輯無 confidence 標註`、
  `wiki frontmatter 缺 key／status 值非法` 也沒有工單出口。前兩者屬 research
  相位的內容工作、有路可走；wiki 類是純編輯、值得日後開 `[wiki]` 型工單。
- 原則：**每加一條違規，就要同時回答「誰去修它、走哪條路」。**
  答不出來的違規不是品管，是路障。這一問應該是新增檢查時的固定步驟——
  我寫過三次同樣的教訓，卻還是靠事後清查才發現，代表**原則沒有被接進流程**。
- 套用：本 commit（ps-doc-lint 四條接線＋說明段、ps-graduation 逐檔 hash 診斷）。

### L81 「查了沒發現」與「根本沒查成」共用同一格——完整性節的假陰性（2026-08）
- 事件：管理者說明歸檔裡那些未打勾列的理由是
  **「subagent 未完成，僅回報已讀取契約，須重派」**。那不是標籤，是**記錄了
  一次真實的委派失敗**——而「僅回報已讀取契約」正是 L66／L67 委派卡死的指紋。
- 我上一則的建議因此是錯的：我從**列的外形**（長得像流程標籤）推出
  「刪掉無損」，卻沒問**列的內容**（它記著什麼事實）。刪掉會讓
  「任務 C 沒做成」這件事整個消失。**又一次用形狀取代內容。**
- 但真正的病不在 checklist，在報告。`audit-template.md` 的完整性節只有一格：
  `- 資料角度發現、功能地圖沒有的物件：<清單，或「無」>`
  任務 C 是**分批委派**的、而委派會失敗；全部失敗時模型在這一格寫「無」——
  讀的人看到的是「完整性查過了、沒有遺漏」，實際上是「根本沒查」。
  **這是假陰性的完整性宣稱，直接汙染 wiki 的可信度**，比 checklist 裡
  多幾列噪音嚴重得多。
- 而步驟 2 對委派失敗**完全沒有處置**：只有一條通用的「同一委派失敗 2 次 →
  標 ⚠ 跳過」，沒說 ⚠ 要標在哪。於是它跑進了 checklist，再跟著被歸檔——
  三個症狀（歸檔含未勾項、流程標籤成為工作項、完整性寫「無」）
  是同一個缺口的三個出口。
- 落點：
  1. 模板完整性節加必填行：`任務 C 覆蓋：<完成 N／共 M 批>（未完成批次：…）`，
     並註明「『查了沒發現』與『根本沒查成』不得共用同一格」。
  2. ps-deep-research 步驟 2 加委派失敗處置三件事：寫覆蓋率行並逐批列出、
     寫進 gaps、**不得寫成 checklist 列**（批次是流程紀錄，寫 log.md）；
     有未完成批次時**禁止只寫「無」**。
  3. lint 檢查完整性節是否帶覆蓋率宣告——**只在 `-StrictAudit`（tier 2）
     升為違規**：完整性盤點不在 tier 1 的承諾範圍，提早擋只會拖慢覆蓋畢業。
- 原則一：**任何「無／沒有／查無」的欄位，旁邊都要有一個「查了多少」的欄位。**
  單一欄位承載兩種語意時，失敗那一種一定會偽裝成成功那一種——
  與 L68（componentType 錯值靜默回空）、L69（stale 沒有落點）同族。
- 原則二：**判斷一列資料該怎麼處理之前，先讀它寫了什麼，不要只看它長什麼樣。**
- 套用：本 commit（audit-template 完整性節、ps-deep-research 步驟 2、
  ps-doc-lint 覆蓋率宣告檢查）。

### L82 loop 修得掉的，不要留給人——但要先確定「刪掉的東西已經有家」（2026-08）
- 管理者問：「被搬到 archive 卻沒打勾的項目，有機會 loop 自己處理掉嗎？
  目標明確，就是沒打勾那項。」——**目標明確，但修法不明確**，那是這題的難處。
- 歸檔契約只收已打勾項，所以未勾列只有三種成因，而修法完全相反：
  · **批次類流程標籤＋明示未完成**（實案：任務 C 三批委派全掛，理由寫
    「subagent 未完成，僅回報已讀取契約，須重派」）→ 不能搬回「調查進度」
    （批次不是調查項，搬回去＝沒有 NN 對象的假項目），也**不能直接刪**
    （會湮滅一次真實的委派失敗）→ **刪除，但把事實回收進「## Gaps 彙整」**。
  · **明示未完成、非批次類**（正常調查項／A 項）→ 搬回「## 調查進度」保持未勾。
  · **純批次標籤、無未完成字樣** → 刪除（純噪音，下輪 audit 重做）。
  · **其餘未勾列 → 不動**：「本來就沒做完」與「打勾在寫檔時掉了」機械上
    分不出，而誤打勾會讓真的漏掉的項目**永遠消失**——這一格寧可留人工。
- 判定順序是**先看內容再看外形**（L81 的教訓）：含未完成字樣的批次列不是噪音，
  是紀錄；我第一版把外形擺前面，差點建議刪掉真實的失敗證據。
- 為什麼 lint 做得到而 agent 做不到：agent 的禁令
  「禁止 read 或改寫任何既有 checklist-archive*.md」，理由是**模型的 write
  工具沒有 append、重寫大檔會撐爆**（卡死根因）——那是**工具層限制，
  不是語意禁令**。PowerShell 沒有這個限制。這一點必須寫在註解裡，
  否則以後會被當成兩條規則打架（L63 的坑）。
- 落點：
  1. `ps-doc-lint.ps1 -FixArchive`（預設關，循 `ps-fs-doctor -FixBom` 前例
     ——診斷工具唯讀，修復走 opt-in 旗標），逐列印出處置與理由。
  2. auto-loop 在「自動 0／僅 audit 0／需人工 >0」時，**停機前先試一次**
     `-FixArchive`，重驗後仍無路可走才停；處置紀錄落 `fixarchive-cycle<N>.txt`。
  3. 停機理由改寫成「-FixArchive 也判不出來」，指名兩份紀錄。
- 順序很重要：**先讓報告端能誠實記錄失敗（L81 的覆蓋率行），再讓 lint
  自動清理殘留列。** 反過來做就是自動湮滅證據。
- 原則：**自動修復的門檻不是「判得出目標」，是「判得出修法，而且判錯的
  最壞後果可承受」。** 本例四類裡有三類可自動、一類必須留人工——
  留下來的那一類不是偷懶，是因為它的誤判會靜默地弄丟工作。
- 套用：本 commit（ps-doc-lint -FixArchive、ps-auto-loop 停機前自動處置）。

### L83 我拿一條資料庫連線的限制，去綁不用它的委派（2026-08）
- 事件：管理者回報「這輪 audit 每次只派三個 subagent 各處理一個 NN 檔，
  之前是一次派好幾個——你改了什麼？」**是我改的（L67）。**
- L67 我寫下：「**併發上限 3**（SOP-12 實測值：3 subagent 同呼成功）」。
  但 SOP-12 那個 3 是 **oracleMCP** 的實測值——SQLcl 是**單工單一連線**。
  而任務 A（ChunkId 解引用）走的是 `PeoplecodeSource`＋
  `PeoplecodeElasticSearch`，**完全不碰 oracleMCP**。
  我把稽核的委派**一律**歸到 SOP-12 底下，沒有分辨「這個委派實際會呼叫
  哪個 server」——結果純 chunk 解引用被一條資料庫連線的限制綁住，
  稽核時長無謂拉長（管理者歷來回報的通道爭用**全部**是 oracleMCP，
  沒有一次是 ES／Source）。
- 錯誤形態：**把資源限制掛在錯的維度上**。限制的真實邊界是「共用那條
  單工連線的呼叫」，我卻掛在「稽核的委派」這個組織單位上。
  兩者只在「有些委派會查 DB」時重疊，我拿交集當全集。
- 落點：併發上限依「會用哪個 server」分兩級——
  · **會查 DB 的委派上限 3**（SQL 型證據重跑、任務 C 反查、metadata 類）
  · **純 ES＋Source 的委派上限 6**（任務 A 的 chunk 解引用、多數任務 B）
    ——6 是 2026-08 實際運行過的值，不是我憑感覺挑的
  同時總數不超過 6，其中查 DB 的不超過 3。**判準是「會呼叫哪個 server」，
  不是「屬於哪個任務類型」**——同樣是任務 A，純 chunk 不碰 DB、SQL 型會碰。
  三處同步：ps-deep-research.md、ps-audit.md、SOP-12。
- 同 commit 另加 git 快照（管理者提案）：`ps-auto-loop -GitCommit`
  每圈 commit 一次該領域目錄。session 崩在半路是這個系統的**常態故障**
  （輪次 32／33、歸檔半寫、強殺後半寫都是），有還原點才敢無人看管。
  設計要點：**只 commit 永不 push**（研究產出是公司機密，嚴禁外部 remote）；
  範圍**限定領域目錄**（工作樹裡常有人工搬運中的腳本，`git add -A` 會掃進去）；
  git 不可用／未設 user.name／commit 失敗一律**只記 log 不中斷**——
  快照是加值，不該變成新的停機原因。
- 原則：**引用一個實測值之前，先確認被限制的東西和你要限制的東西是同一件事。**
  「SOP-12 實測 3」是真的，「所以稽核委派上限 3」是我加的一步推論——
  而那一步把限制的適用範圍悄悄擴大了。數字有出處不代表用法有出處。
- 套用：本 commit（ps-deep-research／ps-audit／SOP-12 併發分級；
  ps-auto-loop／ps-auto-all 的 -GitCommit）。

### L84 又把敘事寫進了小模型的 system prompt——L62 的第二次違反是我自己（2026-08）
- 事件：管理者點名 ps-deep-research.md「寫了很多不必要的語句（L67、L83、
  SOP-12…），先回想我們的執行模型是什麼」。
- 執行模型三句話：agent 檔＝每個 session 全文載入的 system prompt；
  讀者是 active ~3B 的小模型；**它查不到 L67、也查不到 SOP-12**——
  教訓編號、實測日期（「實測（2026-08）」）、出處辯解
  （「6＝實際運行過的值，不是憑感覺挑的」）、實案回顧，全部是寫給
  維護者看的，對執行零貢獻，卻在每個 session 稀釋真正的指令。
- 這是 L62 的原封重犯，而且犯的人是寫下 L62 的我。這個 session 我在
  修規則時順手把「為什麼這樣改」寫進了規則本體——**commit message 與
  lessons 才是放這些的地方**，我兩邊都寫了，等於在 agent 檔裡留了第三份。
- 落點：ps-deep-research／ps-audit／ps-auditor 全面剝除教訓編號、SOP 編號、
  實測日期、實案與辯解（合計 -1652 字元）；指令一律保留，僅在「不講理由
  模型會做錯」處留最短片語（如「舊值用來定位哪一列」壓成欄位格式本身）。
- 原則：**改規則檔時，rationale 的去向先想好：commit message 給審查者、
  lessons 給維護者、規則檔只給執行者。** 同一段理由出現在規則檔＝放錯家。
- 套用：本 commit（三檔剝除）。

### L85 wiki 是問答的真相層，卻沒有任何管線在維護它（2026-08）
- 管理者兩問串出來的盤點：「流程有自動歸戶 wiki 嗎？我看 loop 都在動 NN」
  「為什麼不能把 NN 當 wiki？資料有兩手？」
- 盤點結果：歸戶規則存在（階段二步驟 3），但 loop 最常走的三條路全不碰 wiki——
  A 項補查（明文「取代標準深度鏈」，取代版沒有歸戶步驟）、手術（限定清單
  檔案）、audit（迭代 checklist 已勾項＝NN 檔；auditor 規格寫了「適用 wiki
  entity 檔」，卻沒有任何委派迴圈把 wiki 檔送過去）。**規格聲稱的覆蓋範圍，
  沒有執行路徑對應**——與 L68（描述完整、從未通電的路）同形。
- 最重的後果：索引重建後 NN 的死 id 有整條修復鏈，**wiki 的 `sources` 引用
  同一批 chunk id，卻無人驗、[回灌] 不掃、lint 只驗 frontmatter 格式**——
  問答層（直接面對使用者）的機器參照可能全數失聯而零警示。
- 「NN 當 wiki」的裁決：批評對現狀成立、對設計不成立。wiki 提供 NN 結構上
  給不了的三件事——跨領域一物件一檔（NN 按領域切，同物件多領域＝真的多手）、
  問答 metadata（aliases／status／last_verified）、人工知識的家（reviewed
  檔不被機器改寫）。步驟 3d 也明文「細節不重複詳述」＝設計是分工不是複本。
  **解法是把管線走完，不是砍層。**
- 根治三件（本 commit）：
  1. A 項路徑補歸戶：修復**改動結論**時（改寫／降級／數值——純換 id 不算）
     同步該物件 entity 的 Observation 與 last_verified。
  2. [回灌] 擴到 wiki：換 id 配對收集後全掃 `wiki/*.md`，殘留舊 UUID 的
     逐檔開單（已含新 id 不開單＝冪等）；明細檔案欄支援 `wiki/<檔名>`。
  3. 稽核模式加步驟 1b「wiki 抽驗」：本領域 NN 連到的 entity，
     `last_verified` 最舊優先抽 5 個委派任務 A，非 PASS 照常回灌
     （A 行檔名寫 `wiki/<檔名>`）——每輪 5 個輪替，成本可控、覆蓋隨輪數收斂。
- 原則：**規格說「適用於 X」時，追問「哪條執行路徑會把 X 送進來」。**
  沒有路徑的適用範圍是空話，而空話在稽核系統裡特別危險——它讓人以為
  那塊有人看著。
- 套用：本 commit（ps-deep-research A 項＋步驟 1b、ps-doc-lint [回灌] wiki 掃描）。

### L86 wiki 與主迴圈解耦——歸戶移到畢業後，從 NN 提煉、不再檢索（2026-08）
- 管理者提案，三個理由全部成立：
  1. 領域輪廓要好幾輪才清晰，每輪歸戶＝反覆重寫將被推翻的 entity——
     而 entity 規矩是「作廢不刪除」，churn 會在問答層堆出永久的
     Invalidated 尾巴（比白做工更糟）。
  2. 畢業時 NN 的證據已全數過稽核——提煉＝**逐字複製已驗證的 id**，
     再檢索一次反而引入新錯。提煉 session 禁委派禁檢索＝零 MCP 佔用。
  3. audit 已 45 分，不該再疊 wiki 工作。
- 落點（本 commit）：
  1. 階段二不再寫 wiki——只寫 `[[物件名]]` 連結（斷鏈警告屬預期）。
  2. 新增「提煉模式」：tier 1 畢業、收據發出後由 auto-loop 觸發——
     wiki 在領域目錄外、不進 contentHash，**先發收據再寫 wiki 剛好不互撞**。
     新 entity 一律 `status: draft`；tier 2 畢業後跑「entity 升級」
     （draft→verified；reviewed／stale 不動）。
  3. 收斂儀表＝lint 新增 `WIKI_MISSING：N`（**本領域** `[[連結]]` 無
     entity 的數量——刻意不用全域數，否則別的領域的缺口會讓本領域的
     提煉永不收斂）。loop 以它驅動：>0 就再跑一輪（上限 4），
     沒變少就停；歸戶＝查重→就地更新，天然冪等。
  4. 啟動節加第 0 條豁免——否則提煉 session 會被導進階段二（L63 同型：
     指令 session 撞上無條件路由）。
  5. git 快照範圍擴到 wiki/（提煉的變更也要有還原點）。
  6. L85 的三張網保留：[回灌] wiki 掃描與 A 項同步天然自我節制
     （entity 不存在＝no-op），只在維運期起作用；audit 抽驗同理。
- 原則：**會被推翻的東西不要急著發佈；發佈的東西要從已驗證的材料複製，
  不要重新製造。** 蒸餾層的正確時point是原料穩定之後——這條和兩段式
  畢業是同一個哲學：先讓內層收斂，再往外層供貨。
- 套用：本 commit（ps-deep-research 階段二／提煉模式／啟動豁免、
  ps-auto-loop 提煉相位＋快照範圍、ps-doc-lint WIKI_MISSING）。

### L87 F1 終於修了——申報過的人工待辦不是新問題，不得每輪重新回灌（2026-08）
- 現場（tier 2 第 4~6 圈）：未畢業檔數 3→7→3 **震盪不收斂**，lint 工單 0。
  這個模式**七條熔絲全抓不到**：回灌每輪非零（活鎖熔斷不觸發）、research
  每輪有進度（無進度熔斷不觸發）——會一路燒到 MaxCycles。
- 明細確診＝設計審查的 fatal F1（發現至今始終未修）：
  `待人工SQL` 是 research 端的合法申報出口，但**稽核端沒有它的判定規則**：
  ```
  audit 判 UNVERIFIABLE → 回灌 A 項 → research 修不了（本來就在等管理者
  跑 SQL）→ 再蓋一次待人工SQL → lint 視為合法待辦（工單 0）→ 下輪 audit
  又判 UNVERIFIABLE → …
  ```
  兩端各自「照規則做」，合起來是永動機——與 L69 同族：**一個字面在
  producer 端有定義、在 consumer 端沒有**。
- 附帶抓到出口濫用：AE SQL 被標了待人工SQL。**AE step 與程式內 SQL 是
  CHUNK 型證據**（componentType 結構化就取得到），待人工SQL 是查 DB 表
  （排程／權限／Run Control）專用——萬用逃生門會把可自動修的事變成
  永遠的人工債。
- 落點：
  1. auditor：機器參照＝待人工SQL 的列分流——程式碼類 → `FAIL(WRONG_KIND)`
     （回灌，componentType 取 chunk 可自動修）；真 DB 查詢 →
     `UNVERIFIABLE(PENDING_MANUAL)`。
  2. 回灌規則：PENDING_MANUAL **不生 A 項**（照常進記分卡與明細）——
     人工待辦的家在明細清單與 lint 警告計數，不在工作佇列。
  3. 合法出口規則收窄：明文「AE step 與程式內 SQL 屬 CHUNK 型，
     不得走待人工SQL」。
  4. 自創 FAIL 代碼（WRONG_COMPONENT／INCOMPLETE_REF）併入就近映射例。
- 畢業語意的取捨（刻意）：tier 2 **可以帶著 PENDING_MANUAL 列畢業**。
  擋它＝畢業門被人工可用性劫持——沒有任何自動相位能清它，那是 L72
  「門檻必有出口」的直接應用。收據發了、清單留著，管理者照 SOP-2
  批次自跑後回填，下輪 audit 自然升 PASS。
- 原則：**佇列裡只放「有人能做」的事。** 機器修不了的進人工清單、
  人工做完前不重複入列——把「等待」重新入列為「工作」，
  就是把迴圈變成計時器。
- 套用：本 commit（ps-auditor 分流、ps-deep-research 回灌排除＋出口收窄
  ＋映射例、audit-template 註記）。

### L88 契約檔教模型呼叫不存在的工具——對抗審 cookbook 與 contracts（2026-08）
- 管理者指示對兩個核心檔做對抗驗證。結果兩檔病況完全不同：
- **oracle-query-cookbook（279 行）：判定健康。** 六個 agent 載入，
  內容幾乎全是可執行樣板與硬規則——這就是 agent 檔該有的樣子。
  只剝四處殘餘敘事（L 編號 ×3、「治慢性病」標籤、實案句），-1 行。
- **mcp-tool-contracts（292 行）：判定重病，砍到 88 行。** 三個病灶：
  1. **§1／§2／§4 是不存在工具的完整 JSON I/O 規格**——而且 §1.2／§2.1／
     §2.2／§2.3 連 *(proposed)* 標記都沒有，看起來就是可呼叫的真工具。
     對小模型這是最危險的雜訊：照著範例呼叫 `ps_get_object_origin` 必得
     unavailable tool——**契約檔自己在製造 L61 型失敗**。
  2. 檔頭的工具身分表＋三條硬規則與 ps-auditor／progressive §6.0 三份
     重複——多份即漂移（先前 stale 措辭就是在這裡漏改過一次）。
  3. 「(2026-08 管理者實測確認)」等出處標記（L84 未掃到此檔）。
- 重寫後的結構：**現有工具表（唯一可呼叫清單）→ 角色↔現況對照表
  （每角色一行：現在由誰承擔、查哪個 cookbook 節）→ UI 分類詞彙表**
  （choiceType／圖節點／圖邊——報告分類還在用，保留）。完整 JSON 規格
  移到 `docs/mcp-tool-proposals.md`（人讀，agent 不引用）。
  origin 值域改指向 profile 的 objectOrigins（單一真相）。
- 原則一：**契約檔的第一句話應該回答「我現在能呼叫什麼」**——
  未來規格與現況清單混在同一檔，模型分不出「已有」與「將有」，
  而分不出的後果是照著將有的規格呼叫現在的空氣。
- 原則二：**對抗審規則檔的判準是逐段問「執行者拿這段能做什麼」**——
  答不出動作的段落，要嘛移去人讀的檔，要嘛刪。
- 套用：本 commit（contracts 重寫 292→88、cookbook 剝敘事、
  proposals 新檔、ui-flow SKILL 引用句同步）。

### L89 乾淨退場的 session 吃掉整節，四輪沒人發現——完整性檢查只掛在強殺路徑上（2026-08）
- 現場（tier 2 第 56~59 輪）：checklist「## 調查進度」整節被吃、57 輪別的
  session 寫回、58 輪又被吃——**腐蝕在閃爍**，靠管理者肉眼才發現。
  連帶症狀：舊 01~15 檔被判「未列於調查進度」（列可能隨節一起蒸發）。
- 根因：`Test-FsConsistency` 只在**強殺／session 錯誤**路徑執行。
  exit 0 的 session 整檔覆寫 checklist 時把節標題與列吃掉＝**零檢查**。
  lint 會抓「缺節標題」，但那要等到 lint 跑、而且抓到也只是違規一行——
  期間歸檔照跑，把腐蝕蓋進歷史。
- 落點：
  1. `Test-ChecklistIntegrity`：**每個 session 之後必驗**（主 session＋
     每批手術）——(1) 節標題「調查進度」「Gaps」必在；(2) checklist＋
     全部 archive 合併項目總數不得下降。FAIL 即停，停機訊息附**單檔回滾
     指令**（-GitCommit 的每圈快照讓 HEAD 就是上一圈——快照第一次派上
     正用）。
  2. FixArchive 合法刪列後**基準重取**，不誤報。
  3. 不做自動回滾：session 可能同時做了正當工作（NN 寫入、打勾），
     整檔回滾的邊界效應（與本圈新歸檔的互動）比停機貴——停機＋一行
     git 指令＝人 30 秒能精準處置的事，不值得拿自動化去賭。
- 原則：**守護檢查要掛在「每次寫入之後」，不是「寫入失敗之後」。**
  失敗路徑上的檢查只防得住你已經知道會失敗的方式——安靜寫壞的那種，
  恰好都走成功路徑。
- 套用：本 commit（ps-auto-loop Test-ChecklistIntegrity＋兩處掛點＋基準重取）。

### L90 分級反應：會自癒的記 WARN、不會自癒的自動回滾——停機是最後手段（2026-08）
- 管理者兩次糾正把 L89 的設計掰正：
  1. 「loop 停下來不是好解法，這樣變成又要人盯著」——停機＝把無人看管
     換成人肉警報器，違反系統第一目標。
  2. 「你不要衝動，第 57 輪其實有默默把那個東西修掉」——**節標題消失會
     自癒**：下個 session 整檔重寫時模板自帶節標題。我差點為一個會
     自己好的病開刀。
- 分級判準＝「這個傷會不會自癒」：
  · **節標題消失、項目數未損**＝閃爍性腐蝕 → 只記 WARN（下個 session 修）。
  · **項目總數下降**＝列真的丟了，**不會自癒**（下個 session 讀不到的列
    永遠寫不回）→ `Invoke-ChecklistRecovery`：回滾整個領域目錄到上一圈
    快照＋清掉本圈新生未追蹤檔，本圈作廢重來。快照只在完整性通過後
    才拍，HEAD 恆為乾淨還原點。
  · **連續 2 圈仍列遺失** → 才停機（系統性問題）。
  · 未開 -GitCommit＝無快照 → 只能停機（訊息講明原因）。
- 本課還附一個丟臉的插曲：L89 停機訊息裡的內嵌跳脫引號寫壞了
  PowerShell 字串——**已推送的 893 行版是語法壞的**，搬過的人一啟動就
  parse error。訊息文字從此禁用內嵌雙引號（路徑用全形或單引號）。
  自動化程式碼裡的字串跳脫，是每次都要用工具驗、不能目測的東西。
- 原則：**反應的強度要配傷勢的可逆性，不配偵測的嚴重感。**
  判斷順序：這會自癒嗎？→ 機器救得回嗎？→ 都不行才叫人。
  每一級都比下一級便宜一個數量級。
- 套用：本 commit（ps-auto-loop 分級檢查＋自動回滾＋壞引號修復）。

### L91 完成品的登記只活在歸檔流水裡——一次漏寫＝永久除籍，而且無人察覺（2026-08）
- 管理者的觀察：「checklist 感覺只列有問題的，沒把查證完畢的列入」。
  追完寫檔規則鏈，確認一半、修正一半：
  · **checklist.md 穩態＝只剩問題項是設計**：research 打勾 → audit 每輪把
    已勾列搬進新歸檔分片、checklist 只留「輪次＋旗標＋未勾＋Gaps」。
  · 但「這個檔屬於本領域正式產出」的**登記**因此只活在歸檔流水裡——
    59 輪 × 每輪一次歸檔寫入，**任何一次漏寫＝該檔永久除籍**，
    而歸檔鏈歷史上零防護（總數守衛 L89 才上線）。04 檔完整存在、
    卻不被任何一行提及，就是某輪歸檔漏寫的化石。
- 結構判定：登記是**機械可重建**的——檔案在、標題在檔首 H1、完整度可由
  必要章節判——所以這不該是人工考古，該是 FixArchive 的活。
- 落點：
  1. `-FixArchive` 加補登：未被 checklist＋歸檔任何一行提及的 NN 檔 →
     完整者補 `- [x] <檔首標題> → <檔名>（歸檔遺失補登）`、
     缺章節者補未勾列（續查）。下輪 audit 照常歸檔／處理。
  2. 「未被任何一行提及」歸 MANUAL_ONLY——loop 的既有佈線會**先自動跑
     FixArchive** 再說，等於這類違規全自動修復，人不用出場。
- 原則：**流水帳不是登記簿。** 「發生過什麼」的記錄（歸檔）與「現在有
  什麼」的清單（登記）是兩種東西——把登記寄生在流水帳上，流水帳的
  單次遺失就成了登記的永久錯誤。要嘛給登記獨立的家，要嘛讓它可以
  從實物機械重建——本次選後者（檔案系統就是實物）。
- 套用：本 commit（ps-doc-lint -FixArchive 補登＋MANUAL_ONLY 分類）。

### L92 條件 UI 是看不見的業務邏輯——落地前先對抗驗證，設計稿修四刀（2026-08）
- 缺口為真：Classic 的 Group Box 顯示/隱藏（`Visible`／`Hide`…）承載
  業務規則（什麼條件下整塊畫面消失），但管線兩端都看不見它——
  peoplecode-flow 的 findings 義務清單沒有 UI 狀態變異（小模型讀了就略過）、
  ui-flow 只有「文字 → 欄位」反查，沒有「欄位 → Group Box → 受影響
  控制項」正向解析。issue #5 的設計方向正確，但照抄會踩四個坑：
  1. **解析查詢綁死 `FIELDTYPE = 2` ＝靜默回空**（變異目標大多是普通
     欄位）——與 componentType 值錯同一類 fail-safe bug。修法：不濾
     FIELDTYPE，取回再分流（2＝GB／11＝Subpage／其他＝自身）。
  2. **Page → Component 在 Subpage 上斷頭**：PSPNLGROUP 只登記真 Page，
     而 HR 模組的 GB 常長在 Subpage 上（issue 自己的驗證例就是）。
     修法：`SUBPNLNAME 反查 FIELDTYPE=11` 向上遞迴找掛載 Page。
  3. **coverage 若要求 detected 全數 resolved ＝下一個 F1 乒乓**：
     HideRow／HideScroll 是 scroll 層級、沒有 Record.Field，永遠解不了。
     修法：解析結果封閉值域 RESOLVED／NOT_APPLICABLE／UNRESOLVED，
     scroll 層級＝NOT_APPLICABLE 不是失敗；第一版不加 auditor 覆蓋維度
     （UI 變異走普通 finding，任務 A 解引用自動涵蓋，稽核零加時）。
  4. **成本結構倒過來**：先全偵測全解析再分類＝單 component 近百次
     SQL 打在單工 SQLcl 上。修法：偵測零成本（chunk 反正在讀）、
     從條件式先判 businessRelevant，只解析這批；受影響清單只列業務
     欄位 ≤15 項（防 150 行寫檔上限）。
- 已畢業領域的回灌走 **U 項工單**（`U<輪次>-<序號> 條件UI回灌 <NN-檔名>：…`）：
  取代標準深度鏈的定向補掃，檔尾追加小節不改寫既有內容；
  收據 hash 只蓋領域目錄，貼入 U 項那一刻收據失效＝「回灌＝重新開工」
  的正確語意。「將來再補」不算計畫——工單寫進 checklist 才算。
- 原則：**新能力落地的第一問不是「加什麼」，是「它的失敗模式會落進
  哪個既有值域」。** 解析不到、查回空、驗不了——每一種都要在
  PASS／FAIL／UNVERIFIABLE／NOT_APPLICABLE 裡有家，沒家的那種
  就是下一個震盪源。
- 套用：本 commit（cookbook §2h～2j、ui-flow 正向解析、peoplecode-flow
  偵測義務＋suggestedNext、deep-research 深度鏈＋U 項、契約表一行）。

### L93 修復與建檔是兩種生命週期；durable state 的 transaction 不能交給機率性寫者（2026-08）
- 外部協作者（issue #6／#7）對兩個長期病灶的判定經對抗驗證**全數成立**：
  1. **16~20 出生即殘的根因（#7）**：稽核回灌把「任務 C 遺漏候選
     （建新檔）」與「既有檔稽核失敗（修既有檔）」塞進同一種 A 項；
     A 項處理「取代標準深度鏈」、通篇假設檔案已存在、**模板義務不在
     作用域內**——拿修復規則建檔＝即興產出，天生缺章節。指紋全吻合：
     雙重編號檔名（即興命名）、整批同款缺章節（結構性必然）、
     UUID>0（A 項作業本來就取 chunk）。
  2. **checklist 腐蝕的真兇（#6）**：稽核收尾指令「只留輪次行＋旗標行
     ＋未勾項＋Gaps 彙整」——保留清單**沒有列節標題本身**。模型刪掉
     `## 調查進度` 是對指令的合法解讀，不是隨機失誤；這解釋了腐蝕
     總發生在 audit 輪。且總數守衛抓不到「掉一列＋新增一列」的身分
     互換；FixArchive 的插入點又寄生在會消失的節標題上。
- 落點（六件一批）：
  1. 回灌分流：遺漏候選 → **D 項**（`D<輪>-<序> 新發現`，走標準深度鏈
     ＋模板建檔）；A 項加前置防呆「目標檔不存在＝停，改開 D 項」。
  2. auditor 零證據守衛：無 Evidence 附錄／零條目 → 單筆
     `FAIL(NO_EVIDENCE_SECTION)`，禁止回空陣列（零筆＝vacuous 全過
     ＝記分卡綠燈背書殘檔）；deep-research 對應處理型＝定向補研究。
  3. lint 第五種工單 **[章節]**（兩個 tier 都出）——缺章節從
     「有偵測、無執行者」變成有明確 executor；手術 prompt 同步教法。
  4. auto-loop 確定性調帳（issue #6 P0）：`Get-ChecklistInventory`
     身分清點（工單編號→NN 檔名→正規化 hash 三級錨點）、每 session
     前快照後對帳、silent loss 由 PowerShell 層**直接補回**（流程標籤
     不補）、節標題／輪次行**最小插入**機械重建——L90 檢查降格為
     最後一道 assertion。FixArchive 插入前自建消失的節標題。
  5. 保留規則改明列骨架清單：「可移除的只有已歸檔的已勾列；
     固定結構節點任何情況不得刪除」。
  6. 模板註記骨架與 D 項格式。
- 原則一：**工單語彙要覆蓋生命週期全集**——CREATE 與 REPAIR 是不同的
  動詞，共用一種工單，缺的那個動詞就會被即興代償，而即興沒有契約。
- 原則二：**機率性寫者可以提出狀態變更，但 durable state 的最終一致性
  由確定性層保證**——「叫模型別刪」是第二道防線，永遠不是保證；
  保證＝每次寫入之後的身分對帳＋骨架重建。
- 套用：本 commit（ps-auto-loop 調帳三函式＋四邊界接線、ps-doc-lint
  [章節] 工單＋FixArchive 自建標題、ps-deep-research A/D 分流＋
  NO_EVIDENCE_SECTION、ps-auditor 守衛、ps-audit.md、checklist 模板）。

### L94 模板演化、門檻凍結——enforced 與 aspirational 的漂移連「整齊的檔」都量得出來（2026-08）
- 管理者問「正典是不是該有 ## 相關物件」並要求查 git 歷史。查證結果：
  沒有版本「改錯」——lint 五節門檻定案（7/24 稍早）時模板是八節、
  **尚無相關物件**；同日稍晚 wiki 層 commit 把它加進模板，lint 沒同步。
  排除它從來不是決定，是時序巧合，之後無人回頭審。
- 管理者的存量觀察補上兩個關鍵證據：
  1. 01~15 **全部**帶相關物件（wiki 後模板出生即帶）→ 入列零回灌成本。
  2. 01~15 **全體**缺 ## 權限——門檻外的節，模板寫了模型也會靜默略過，
     15 檔無一倖免、無人察覺。門檻即現實，模板只是願望。
- 落點第一版（六節）被管理者**再度校正**：存量整齊的是**八節**——
  畫面與欄位、執行方式也全數在列。我用先驗推理排除它們（「純批次無
  畫面、線上無排程」）是錯的：批次也有 Run Control 頁（模板欄位表
  範例就含）、執行方式本來就是線上／批次擇一填——模板設計即普適，
  存量實證也普適。門檻定案＝**八節**（模板順序）。
- 唯一不入列：## 權限——全存量缺席，成因是**成本**不是不適用
  （權限要 oracleMCP 專查，研究鏈少委派到那）；入列＝15 檔瞬間全違規、
  燒一波 DB session。要補就開明確的回灌戰役（D／U 項模式），
  不靜默翻門檻。
- 原則一：**規格改了，門檻要在同一個 commit 裡回答「我跟不跟」**——
  不回答就是默認凍結，而凍結的門檻會把之後的規格演化全部變成
  aspirational。漂移不會自己浮出，要靠「整齊的檔」被拿去對門檻才現形。
- 原則二：**門檻對齊存量實證，不對齊先驗推理**——「哪些節普適」的
  答案在 15 個整齊的檔裡，不在推理裡；同一題連錯兩次（先漏相關物件、
  再漏兩節）都是先驗推理的鍋，都被實證校正。
- 套用：本 commit（ps-doc-lint requiredSections 八節）。

### L95 搬運完整性改用 manifest 對照——「怕漏搬」不該靠人對清單，該靠雜湊（2026-08）
- 批次越攢越大（本批 13+ 檔），人工對照搬運清單的漏搬風險隨檔數上升，
  且「搬到舊版」人眼根本看不出來。doctor 新增檢查 M：
  `scripts/ps-transfer-manifest.json` 記全框架（scripts＋.opencode 全樹）
  每檔的正規化雜湊＋行數，公司機跑 doctor 逐檔比對——漏搬／版本不符／
  搬壞／BOM 缺全部逐檔點名，多出的未列管檔（舊檔未刪）也提示。
- 正規化＝剝 BOM＋換行統一 LF＋裁檔尾空白後取 SHA256：GitHub Raw 複製到
  Windows 另存造成的行尾／BOM 差異不誤報，內容差一個字就報。
- **操作契約（維護端，不可忘）**：每批 push 前必跑
  `pwsh scripts/ps-fs-doctor.ps1 -WriteManifest` 重生基準並隨批 commit；
  搬運清單必含 manifest 本檔。公司機**只讀不寫**（-WriteManifest 在
  公司機跑＝拿本地現況蓋掉基準，檢查失去意義）。
- doctor 的 -Domain 同時改為選填：搬完檔裸跑＝只做搬運與腳本健檢
  （M/C/D/S），加 -Domain 才驗領域目錄。
- 原則：**核對規模一旦超過一屏，核對就要機械化**——清單是給人排程用的，
  完整性要交給雜湊。
- 套用：本 commit（ps-fs-doctor 檢查 M＋manifest 生成、初版 manifest 52 檔）。

### L96 復活拔河——「身分消失」不等於「列遺失」，補回之前先找錨點（2026-08）
- 實案（D63）：D 項 10 筆，1~6 完成；D63-06 一勾一未勾並存、D63-07
  兩列未勾重複，未勾 3→3 兩圈無進度熔斷。兩個機制疊加：
  1. **調帳層與模型拔河**：session 打勾時改寫工單列文字／編號 →
     工單身分（wo:D63-06）從清點裡消失 → L93 調帳判「遺失」→ 補回
     舊未勾列 → 一勾一未勾並存；下一 session 再改寫、再補回＝無限拔河。
  2. **D 項缺「已存在」出口**：重複 D 列的目標檔已建，D 項規則沒寫
     這情況（與 L93 抓到的 A 項缺口互為鏡像）——session 靜默跳過
     不打勾 → 未勾數凍結 → 假性無進度停機。
- 修法四件：
  1. 復活防衛：補回前先驗**錨點**（工單編號／NN 檔名／物件名）是否
     仍在活頁——在＝被改寫非遺失，不復活並記 log 點名；
  2. 復活斷路器：同列補回過一次又消失＝持續被改寫，永不再補
     （台帳 reconcile-restored.txt 跨圈持久）；
  3. 同文重複列收斂（全文含勾選框完全相同＝零資訊損失才刪；
     一勾一未勾不動）；
  4. 規則側：工單列文字不可變（打勾只改勾選框＋行尾註記）、
     D 項補「已存在→打勾附註」「重複→打勾附註」出口、
     D 項生成前查重＋序號唯一（各任務 C 批次先聯集再編號）。
- 附帶抓到的誤殺鏈：D 項來源慣寫「任務C 反查」，而流程標籤判定
  抓 `任務[ABC]`——lint 會叫人**整列刪除合法 D 工單**、FixArchive 會把
  歸檔裡的未勾 D 列當批次噪音刪掉、調帳層拒絕復活它。三處統一修：
  **帶 A/U/D 編號開頭的列永遠不是流程標籤**。lint 的否定斷言還踩了
  regex 回溯（外層 `\s*` 回退零格閃過 lookahead）——lookahead 內
  必須自帶 `\s*`。
- 原則一：**自動修復系統的第一戒律是「先確認傷是真的」**——修復器
  誤判健康狀態為損傷時，它自己就是損傷源，而且它比誰都勤勞。
- 原則二：**判定規則靠字樣（任務C）就會誤殺帶字樣的合法載體**——
  判身分要用結構錨點（編號、檔名），不用內容關鍵字。
- 套用：本 commit（ps-auto-loop 復活防衛＋斷路器＋同文去重、
  ps-doc-lint 流程標籤三處修正、ps-deep-research D 項出口＋查重＋
  列不變性、ps-audit.md 同步；測試 9 情境全過）。

### L97 首輪驗收的兩道縫：字樣判定被換語言繞過；列不變性斷了歸戶鏈（2026-08）
- 大改後首輪整體站穩（記分卡未塌、骨架未被吃、D 生命週期走通、
  新檔只缺一節而非整批缺四節）。殘餘兩道縫：
  1. **流程標籤英文逃逸**：session 這次寫「task C batch 1（5表）」——
     判定只抓中文「任務[ABC]」，換語言即逃逸，假項目混進 checklist。
     修：判定雙語＋(?i)（lint procLabelPattern／FixArchive isProc／
     調帳垃圾過濾三處同步）。L96 原則二續集：字樣判定天生脆弱，
     能用結構錨點就不用關鍵字；不得不用關鍵字時，至少雙語。
  2. **L96 列不變性 × lint 對帳互相打架**：D 列只帶物件名不帶檔名，
     列文字又禁改寫——完成 D 項後，對帳（IndexOf 檔名）永遠找不到
     該 NN 檔＝MANUAL_ONLY 假警報。修：D 項打勾時**行尾追加**
     「（→ <NN-檔名>）」——行尾註記是列不變性明文允許的通道，
     身分不動、歸戶鏈接上。
- 原則：**兩條各自正確的規則，交集處要重新驗一次**——列不變性（對）
  ＋檔名對帳（對）＝完成的 D 項永遠對不了帳（錯）。規則加法不封閉，
  每加一條要沿既有規則掃一圈交互作用。
- 套用：本 commit（lint 雙語×2＋auto-loop 過濾、deep-research D 項
  檔名註記）。

### L98 同物件重複建檔——三個缺席防線的疊加，與「凍結地圖」的結構性再發現（2026-08）
- 實案：21~26 與 27~32 六對 NN 檔物件名重複——同批物件被完整研究了
  兩次。成因鏈三疊加：(1) D 重複列（L96 復活拔河的遺產）；(2)「已存在」
  出口規則已寫但**尚未搬運上機**（只搬了 lint）；(3) 檔名規則
  「取下一個未用編號」讓第二次處理靜默開新檔。
- 更深一層是結構性的：任務 C 的 diff 基準＝**凍結的功能地圖**
  （00-overview 階段一即凍結），新發現永遠不在基準內——沒有查重，
  每輪稽核都會把同一批物件再「發現」一次。模型自律（L96 查重規則）
  是第一道防線，但**確定性守衛不能缺席**：
  1. lint 新增「D 項重複發現」檢查（未勾 D 列目標已有 NN 檔＝自動類，
     research 打勾附註即修）；
  2. lint 新增「同物件重複建檔」檢查（去編號後同物件、編號不同＝
     MANUAL_ONLY，擇優合併只有人做得了；同編號續篇 -2 不算）。
- 原則一：**規則寫了不等於上線了**——搬運型部署的規則生效點是搬運
  完成那一刻；只搬檢查器不搬行為規則＝檢查器準確地報告災難發生。
- 原則二：**diff 的基準若是凍結的，diff 出來的「新」就要自帶查重**——
  否則凍結點之後的每個產出都會被永遠重新發現。
- 套用：本 commit（ps-doc-lint 兩道守衛＋manualPatterns）。

### L99 D 提案治理——已知物件集收進外環，模型寫的列是提案不是事實（2026-08）
- 外部協作者（issue #8）對 L98 的批評成立：lint 守衛是**事後偵測**，
  真正的防重（L96 查重規則）仍是模型自律的散文——任一 session 沒守
  就再生一批重複檔。根因同 L98：任務 C 的 diff 基準是凍結地圖，
  後續發現每輪被重新「發現」，而散文規則擋不住結構性的重複壓力。
- 落點（外環新層 Invoke-DItemGovernance，掛在 session 邊界）：
  1. **KnownObjects 確定性建集**：NN 檔主物件（去編號、去續篇 -N、
     去 PS_ 前綴、大小寫不敏感）∪ 歷史 D 列物件（active 已勾＋全部
     歸檔，勾否皆算）。
  2. **未勾 D 列一律視為提案**：物件 ∈ KnownObjects 或與本頁另一列
     同物件 → 整列刪除（log 點名出處）；同號異物件 → 重配號
     （該輪 max+1，含歸檔號域）。已勾 D 列＝歷史，永不動。
  3. 執行點在調帳之後、下個 session 之前——**重複列活不過一個邊界**，
     research 永遠看不到它。L98 lint 守衛降格為最後 assertion。
- 與 issue #8 原提案的一個分歧（更強）：不改模型契約成「只輸出
  discoveredObjects」——那條「不准寫列」本身又是散文規則。讓模型照舊
  寫列、外環照單全裁，**無論模型守不守規則結果都正確**。
- 原則：**機率性寫者的每一筆「新增」都是提案；受理與否由持有全量
  身分集的確定性層裁決**——L96 管列的存續，L99 管物件的准入，
  同一哲學的兩層。
- 套用：本 commit（ps-auto-loop 治理函式＋邊界接線；測試情境 10~15）。

### L100 守衛要認得自家清潔工——外環的合法刪列必須進完整性基準（2026-08）
- 實案：L99 治理層上線後第一圈，治理依職責刪掉重複 D 提案 → 合併
  總數下降 → L90 完整性守衛判「列遺失」→ 無 -GitCommit 可回滾 →
  第一圈停機。管理者肉眼確認沒有遺失——**守衛把自家清潔工的работу
  當成竊案報案**。FixArchive 合法刪列時有「基準重取」，新層漏了同款。
- 修法：Invoke-PostSessionReconcile 回報 LegitRemoved（治理刪提案＋
  同文去重），主路徑與手術路徑在驗完整性前把基準同步下修並記 log
  （「完整性基準調整：外環合法刪列 N → 基準 M」）。
- 驗證法（誤報 vs 真遺失的機械判別，記給未來）：log 的治理／收斂
  行數恰等於「項目總數下降 X→Y」的 X−Y ＝ 誤報；基準 commit 的
  git diff 是位元組級終審。
- 原則：**每新增一個會動 durable state 的自動層，全部既有守衛都要
  重新對帳一次「它的合法動作我認不認得」**——L97 是規則×規則的
  交集失效，本課是層×守衛的交集失效，同一族：加法不封閉。
- 套用：本 commit（ps-auto-loop LegitRemoved 記帳＋兩處基準調整）。

### L101 結構語法的修復權不能還給寫錯它的人——lint 抓到就 lint 修（2026-08）
- 實案：D 產檔六連發，Evidence 標題全寫成 `**Evidence 附錄**`（粗體、
  同 session 連續複製同一錯格式）。lint 字面比對正確判缺；[章節] 工單
  把修復發回 LLM 手術——session 打開檔「節明明在」（它讀語意），規則
  沒有「節在但標題變體」分支 → 無操作 → 61→61 卡死隊頭，55 筆機械
  工單被堵。外部協作者（issue #10）的定性成立：**偵測器早已確定性，
  漏洞在修復權仍在 LLM 手上（LLM 寫錯語法 → lint 抓到 → 再叫 LLM 修
  語法），以及解析器用 substring 不是行錨定**。
- 落點四刀：
  1. lint 章節解析改**行錨定**（行首 ≤3 空白＋字面標題）——內文引用
     「## X」不再誤判存在；`### X` 不再因 substring 巧合通過（本次就
     抓到一個潛伏誤放行）。
  2. lint 新增 `-FixHeadings`：變體標題（粗體／#層級／少內部空格／
     尾冒號）**確定性正規化**成正典——只修「整行僅標題」且正典缺席者，
     同節多變體＝歧義跳過留人工；冪等；保 BOM。
  3. auto-loop 每圈 lint 評估前自動跑 -FixHeadings——假缺章節到不了
     工單；[章節] 從此只載真缺章節（需要研究的那種）。
  4. deep-research 建檔規則補「寫檔前必 read 模板、標題逐字照抄」
     （出生品質，第一道防線；但保證來自 1~3）。
- 與 issue #10 的一處分歧：骨架預生成（外環先建八節空殼、LLM 只填
  內文）暫不採——subagent 無 bash 跑不了腳本、空殼在被填之前會觸發
  空心章節違規（暫態紅噪音）、而「邊界正規化」已達成同一終態不變量。
  若未來出現正規化蓋不住的出生缺陷再議。
- 原則：**誰寫錯的不重要，誰「能保證寫對」誰修**——結構語法是位元組
  契約，位元組契約的偵測與修復都該住在確定性層；LLM 只負責內容。
  L99 管物件准入、L100 管守衛記帳、本課管語法所有權，同一哲學。
- 套用：本 commit（lint 錨定＋FixHeadings、auto-loop 每圈正規化、
  deep-research 模板必讀；fixture 六情境驗證）。

### L102 熔絲與排程是兩種東西——停損不等於放棄後方健康工單（2026-08）
- 外部協作者（issue #11）對手術管線的定性經逐條對碼驗證成立：
  1. 隊頭阻塞：選批永遠取頭 7、lint 排序穩定——毒丸卡頭＝後方無限飢餓
     （L67「一個卡住只該損失那一個委派」在手術外環被重新違反）；
  2. 煞車只看 count：掉 A 生 D 的總數平手被判零進度；
  3. 無 backpressure：大量債的下一圈仍先跑 producer 再回頭清舊債；
  4. auditStall 對手術收斂視而不見：backlog 排水期間 audit 連兩圈零回灌
     ＝「無自動修復管道」停機——正在工作的管道被它親手關掉；
  5. 手術 session 無獨立熔絲：逾時/非零 exit 只記 log，可跨圈反覆燒滿
     timeout 而無升級路徑。
- 落點五件（安全熔絲全保留，只拆出排程）：
  1. **排水圈**：圈前可執行債 > 0 → 跳過 producer 直接續清——budget
     用盡是 checkpoint 不是完工；連 2 圈排水無進度退回正常相位（防
     排水活鎖）；producer 圈重置排水熔絲。
  2. **手術台帳** surgery-ledger.json：指紋＝去編號工單文字；lint 是
     工單存在與否的唯一真相，台帳只 overlay attempts/blocked 且每次
     存檔對 lint 現況剪枝（不得復活已解決工單）。
  3. **選批跳 BLOCKED**：健康 session 中得到公平嘗試仍未解 ≥2 次 →
     BLOCKED 靠邊，後方照常服務；lint 不再產出即自動剪枝，人工清單
     ＝台帳檔，處理完刪檔即放行。
  4. **煞車改身分尺**：舊單有被解決才算進度；session 級故障（逾時/
     非零 exit）不冤枉工單、走獨立熔絲（連 2 次停本圈手術）。
  5. **停機訊息帶殘債實況**：只剩 BLOCKED 時列數量與台帳位置——
     L74「新增門檻要指出被擋者的出路」。
- 與 issue #11 的取捨：READY/WAITING_DEPENDENCY 分類暫不建——現行
  工單型別全部自足（[回灌] 答案內嵌、[章節]/[證據] 自行委派檢索），
  WAITING_DEPENDENCY 目前是空集合；排水的退出條件（無進度 2 圈）
  已防死鎖。工單型別將來若出現真依賴，再引入分類。binary split 亦
  不採：attempts 記帳已滿足「毒丸不阻塞」，拆批重試在 session 級
  故障下會放大成本（協作者自己的對抗驗證同此結論）。
- 原則：**熔絲證明損失有上限，不證明佇列有公平性、收斂性或自癒力**
  ——這三樣要各自設計：公平＝跳頭、收斂＝backpressure 排水、
  自癒＝隔離加剪枝。一個旋鈕兼多職遲早互相傷害（L70 的完成式）。
- 套用：本 commit（ps-auto-loop 排水圈＋台帳＋身分煞車＋獨立熔絲＋
  停機訊息；測試 16~19）。

### L103 守衛的三個素樸假設——身分會漂移、輸入有形狀、守備有盲區（2026-08）
- 事故現場（第 67 輪，L102 上線後首個長跑）：
  1. 手術清單第 12 圈起卡 15 筆不動、5 圈零收斂，台帳卻全程空白無
     BLOCKED——L102 的隔離機制一次都沒觸發；
  2. 新建 NN 檔的 Evidence 附錄是裸 ChunkId 清單（chunks GUID1,GUID2,…），
     lint 全數放行；
  3. 一個 NN 檔被 session 改到只剩不完整的 Evidence 節（healthy exit），
     靠人工 git 才救回；
  4. A 項一批 16 項被挑著做——中間兩項靜默跳過、後段全未動；
  5. D 項跨輪堆積（D64/D65/D67 並存、D67 全未勾）。
  （4）（5）是（1）的下游：可執行債永不歸零＝排水圈永遠跳過
  producer，A／D 積壓不是新病，是飢餓的症狀。
- 根因一（身分漂移）：L102 指紋＝去編號工單文字，但文字裡帶
  「檔.md:行號」與「（行 a、b、c）」——同檔**任何**編輯都使行號漂移，
  舊指紋消失＋新指紋出現＝幻影 resolved（假進度騙過煞車）＋attempts
  歸零（BLOCKED 永遠到不了）。**位置座標是狀態不是身分**。
  修：指紋剝 `.md:行號` 與行清單；正規化後同指紋合流，記帳去重
  （否則一批灌兩次 attempts、首批冤枉 BLOCKED）；新工單型別的文字
  一律不帶筆數／行號（筆數也是狀態）。
- 根因二（輸入形狀）：Evidence 節的逐列檢查全掛在「| 開頭的表格列」
  上——裸 id 傾倒一列表格都沒有＝**零檢查零違規**；節級檢查又只問
  「有沒有 UUID」，傾倒恰好有。檢查器自己的輸入形狀假設沒人驗。
  修：[附錄] 形狀守衛（節內有 UUID 卻無資料列、或表格外單行 ≥3
  UUID）→ 兩 tier 都出工單＋手術 prompt 新增型別（重建＝逐 id 解引用
  成四欄表格，不是排版）；deep-research 建檔規則補「表格義務」當
  出生防線——標題義務（L101）擋不住「標題對了、身體是清單」。
- 根因三（守備盲區）：checklist 有完整性檢查＋調帳＋回滾三層守備，
  NN 檔只有 0-byte 檢查——「有內容但被掏空」完全裸奔，healthy exit
  照樣過。修：NN 檔破壞防衛——位元組級快照，session 後任一 ## 標題
  消失（剝（gaps）尾註後比）或 ≥40 行掉到 30% 以下即整檔還原，
  producer／手術每批／強殺三路都掛；90-audit.md（每輪全量重寫）與
  checklist.md（自有守備）除外。還原後工單自然還在 → attempts →
  BLOCKED 進人工清單，破壞不再無聲、也不會無限循環。
  **追記（歸檔案發鏈，管理者 git 考古）**：r59 無新勾 → r60
  checklist-archive 整檔消失 → 調帳層只能把歸檔列復活回「活頁」
  這個**錯的家**（它的合法去向模型只有活頁／歸檔兩格，家被拆了
  就只剩活頁）→ r61~62 session 重新歸檔但抄不刪＋輪次標錯 →
  88 列跨檔重複。教訓：歸檔檔同樣是 durable state，卻連 0-byte
  檢查都沒有。修：歸檔檔納入破壞防衛——agent 禁改寫歸檔的硬規則
  給了最乾淨的判準：**session 視窗內歸檔檔只准長大**（新增歸檔檔
  合法；FixArchive 的合法刪列在圈首快照前，不誤傷），消失或變短
  即位元組還原。跨檔同文重複的確定性去重暫不做——先觀察防衛
  堵住源頭後是否還會再生。
- 根因四（紀律縫）：階段二只寫「一次一項、至多 6 項」，沒寫**取哪
  6 項**——agent 挑軟柿子，跳過無收據。修：由上而下依序、禁止跳項；
  做不動＝照型別出口處置後才算前進。
- 原則：**每個守衛上線時要逐一審它的三個素樸假設——「我認的身分
  穩定嗎」（漂移）、「我讀的輸入長我以為的樣子嗎」（形狀）、
  「我的守備範圍蓋到同類資產了嗎」（盲區）**。L100 審「合法動作
  認不認得」，本課再進一步：守衛本身就是需要被查證的對象，它素樸
  的地方就是下一個事故的入口。
- 追記（通道故障協定與靈活性邊界）：oracleMCP 通道未掛（該 session
  工具清單零 oracle 前綴——SQLcl MCP 在 VS Code 端死了）→ 模型自創
  `oracle_sql_run` 反覆撞牆到 doom_loop 攔截。落點：cookbook 7a
  快速失敗協定 FAIL(ORACLE_MCP_DOWN)，禁猜名重試；管理者對初版
  「禁改道」提出反對，修正為**換路查可以、冒充不行**——ES／程式碼側
  等價線索照常可用，但只能以 INFERRED＋CHUNK 入報告且列不關單；
  SQL 型證據欄只有可重跑的 SELECT 填得動（chunk 證明程式碼怎麼寫，
  驗不了 DB 現在是什麼）。**靈活性放在型別系統內側（降級＋收據）
  是資產，放在外側（代償＋宣稱完成）是假 VERIFIED 的入口**——
  「OracleMCP 連線限制」謊稱與標籤充數的歷史全是後者。
- 追記（BLOCKED 三筆的三種病理，85 檔長跑）：台帳隔離首次實戰，
  三筆修不掉各是一型——
  1. **[回灌] 陳舊**：lint 只查「新 id 已套用」不查「舊 id 還在不在」
     ——檔案重建後換無可換，工單永生、session 收據跳過、attempts
     空轉。修：舊 id 也不在＝壓下（警告點名），90-audit 下輪重寫
     自然刷新；pair 仍餵 wiki 掃描。**工單的前提（可作用的對象）
     也要機械驗證，不只驗「還沒做」**。
  2. **[章節] 不適用**：Function Library 無畫面，「缺 畫面與欄位」
     修不動也不該硬修。修：三處（lint 修法說明／手術 prompt／模板
     註記）明文「標題仍要就位、內文寫（無——原因）」——標題是機器
     契約，誠實申報不適用即合格，只有標題缺席才是違規。
  3. **縮寫 id 真案**：BLOCKED 進人工清單＝機制照設計工作，人修完
     刪台帳即放行。
  另：88 列清完又生 11 列＝歸檔者抄不搬已成慣性——按預告落地
  **跨檔同文去重**進調帳邊界（已勾×歸檔已存→刪活頁；未勾×歸檔
  已存＝歧義留人工；刪除量進 LegitRemoved）。「歸檔＝搬移」的刪活頁
  半步從此由外環保證，L101「誰能保證做對誰修」的又一次套用。
- 套用：本 commit（auto-loop 指紋正規化＋記帳去重＋破壞防衛三掛點＋
  手術 prompt [附錄] 型；lint [附錄] 守衛＋工單＋修法說明；
  deep-research 取項順序＋表格義務；測試 20~22）。

### L104 反查是候選產生器，不是域界判定器——Domain Gate（2026-09）
- 症狀（外部協作者 issue #12，逐條對碼成立）：單領域 NN 檔失控長到
  85 且持續增加。根因鏈：任務 C 從核心表反查讀寫物件 → 回傳**裸
  物件名清單**（無型別／經由表／方向／origin）→ 委派方對候選唯一的
  過濾是**查重** → 「兩個來源證明碰到核心表」被實作成「屬於本領域，
  建 NN」。共用表（JOB 類）天然被全系統讀寫，反查沿依賴圖外擴：
  root → 共用表 → 其他流程 → 更多依賴——**收斂論在共用表面前不成立**
  （此前對「覆蓋 vs 新發現」矛盾的收斂解釋因此修正）。
- 最誅心的一刀：domain policy **早就存在**——business-domain-map 的
  rootObjectPolicy（CUSTOM_ONLY_ROOTS＝原生僅能列 DEPENDENCY）在
  階段一盤點用過，任務 C 路徑完全繞過它。不是缺規則，是**有一條路
  不經過規則**。
- 落點：
  1. auditor 任務 C 回傳結構化候選（name／type／viaTable／direction／
     origin）——「你不做 gate，你的職責是讓委派方判得動」；
  2. 稽核契約 D 項生成前三分（Domain Gate）：DOMAIN_ROOT（業務證據
     ＋origin 符合 rootObjectPolicy）才准成 D；DEPENDENCY 記 NN
     相關物件表＋90-audit，不建檔；OUT_OF_SCOPE 記 90-audit；
     判不準＝不建 D 待人工——**寧漏勿擴**（漏的下輪還會出現，
     擴的要人工清）；`allowDeliveredDependencies` 只授權作依附，
     不授權升格 root；
  3. 外環熔絲 -MaxNewDPerAudit（預設 10）：單輪新 D 超限＝gate 失守
     訊號，停機交人工 scope review——**上限是熔絲，不是 gate 的
     替代品**（協作者明文列在「不接受的修法」，同意）。
- 原則：**產生器與判定器是兩個職位**——反查證明「引用存在」，
  只有政策證明「屬於領域」。任何「候選 → 正式身分」的升格點都要有
  明確的 gate，沒有 gate 的升格點＝scope creep 的入口。
- 套用：本 commit（auditor 結構化候選；deep-research／ps-audit
  Domain Gate；auto-loop -MaxNewDPerAudit 熔絲）。

### L105 durable state 的搬移不能交給機率性 writer——歸檔外環化（2026-09）
- 症狀（外部協作者 issue #13，逐條對碼成立）：歸檔要求模型做
  「寫新 archive＋從 checklist 刪除＋輪次遞增」三個獨立 write，
  之間沒有任何 transaction 邊界。失效矩陣全部發生過：archive 整檔
  消失（r60）、抄不搬跨檔重複（88→11 列）、輪次錯亂（r61 內容
  標 61 卻於 62 歸檔）。既有守衛（完整性／調帳／破壞防衛／
  ArchiveDedup）全是**事後修補**，中間態本身一直合法可見；
  session 中新建的 archive 更不受 pre-session 快照保護。
- 落點：**歸檔所有權整個移交外環**——模型只維護 checklist（勾選、
  輪次行、新列），Invoke-ChecklistArchiveCommit 是唯一歸檔者：
  先寫 archive → 寫後逐列驗證 → 全過才刪活頁（驗證失敗＝活頁不動
  ＝舊態完整）；同輪重跑合併寫；audit 相位後＋loop 啟動時各結清
  一次（啟動結清承接人工 /ps-audit 的殘留）。
- 與 issue #13 的一處簡化（有意為之）：不做 .tmp／journal 全套
  transaction——兩個單次 WriteAllText 之間的崩潰窗只留下「跨檔
  重複」一種中間態，而 ArchiveDedup 在下個調帳邊界確定性收斂它。
  **不變量靠「終態收斂」達成，不靠原子性**：確定性 writer 的崩潰
  窗以秒計，收斂保證比 journal 協定便宜一個數量級且測得動。
  ArchiveDedup／FixArchive 自此降級為 crash recovery（協作者建議的
  終態定位，同意）——正常路徑若仍頻繁出現跨檔重複＝commit 有 bug。
- 原則：L96 說「LLM 可以提出狀態變更，不能當 transaction manager」
  ——當時只管到 checklist 的列，本課補完：**同一原則適用於每一份
  durable state 與它們之間的搬移**。凡「A 檔刪、B 檔加」的跨檔
  語義，寫入方必須是能保證順序與驗證的確定性層。
- 套用：本 commit（Invoke-ChecklistArchiveCommit＋audit 相位／啟動
  掛點；deep-research／ps-audit 歸檔段改所有權宣告；測試情境 24）。

### L106 context 溢出先分端再修——爆點在子代理輸入側，不在檔案總數（2026-09）
- 症狀：85 檔領域 loop 停機，管理者實測 **ps-auditor 子代理**
  context length exceeded。外部協作者 issue #22 定性為 parent
  （稽核 session）累積溢出並提出 manifest／bounded batch／收據／
  可續跑 finalization 的整套設計。三角度調查＋逐條對抗驗證後判定：
- 爆點定性：**主爆點＝auditor 單檔任務 A 的輸入側**——單一委派的
  context 由「Evidence 筆數 × 每筆 get_chunks_details 完整 chunk 內文
  × 失聯筆三管道二次定位『翻到底』× 旗標下查無宣告全量重跑」決定，
  而硬規則「回報筆數少於該檔 Evidence 筆數＝無效」禁止部分回報；
  同一契約的任務 C 有「>5 張表只做前 5、其餘退回」的對稱防守，任務 A
  沒有。**主導成長項是單檔 Evidence 列數與失聯比例，NN 總數只是間接
  放大器**（檔越多、最重的那檔越重、每輪 170+ 次委派必中重尾）。
  次爆點＝parent 線性累積（85×任務 A JSON＋parent 逐檔 read 抽 claim
  ＋任務 B JSON＋wiki＋任務 C＋整檔重寫 85 列記分卡）——契約上逐條
  成立，實測未證。**環境前置疑點**：硬錯而非壓縮，指向 opencode.json
  宣告 `limit.context` 高於 serving 真值 → 既有壓縮永不先觸發
  （SOP-10 探針為此而設，L6 疑點至今未回填）。外環對 context 字樣
  零偵測；子代理溢出多半 exit 0，走不到錯誤分支。
- issue #22 裁決：五條根因成立（根因 3 方向對機制錯——先爆的是輸入
  側不是 JSON 輸出；根因 5 成立但不足——分類須掃 out＋err 且不以 exit
  為前提）；七項實作：manifest（檔級＋範圍級）／PowerShell 確定性抽
  claim（非 seed——parent 與 auditor 皆 bash:false 算不出 hash）／
  bounded batch 採納；外環對 auditor JSON schema 驗證拒絕（子代理回報
  只回 parent、不進 stdout）改為 part 檔不變量；從收據重建 90-audit
  部分採納（表格機械 render、語意節交 bounded finalizer）；overflow
  拆批不做 binary split（任務 A 已是一檔一委派）改「收據續跑＋頁減半
  ＋最小單元 BLOCKED」；精簡稽核 agent 採納為配套。「不接受清單」
  同意，但兩項其實該做：校正宣告 limit 至 serving 真值（不是換模型、
  不是調壓縮參數，是讓既有壓縮能觸發）、精簡 agent。
- 本 commit 落地（安全四件）：auditor 二次定位每管道頁數上限
  （search 2 頁／結構 1 批／semantic 2 頁，到頂→NOT_FOUND＋查法收據，
  沿用既有值域不加新 reason code）＋§5.1「翻到全量」改以 §5 預算為
  上限（消除自我矛盾）；Invoke-Opencode 回傳 FailureKind（掃 out＋err
  不看 exit，標籤不判定）；lint `-EvidenceStats` 唯讀開關（每檔列數＋
  最大／p95／中位）供容量校準。
- **延後至 D0 數據回填後一次落地（不盲猜參數）**：外環 audit manifest
  ＋每 session K 檔部分報告（audit-parts/）＋Test-AuditPart 不變量
  （合計＝列數、明細 UUID⊆附錄）＋audit-ledger 收據續跑＋外環合併器
  （表格／A 列／輪次／旗標／歸檔順序）＋bounded finalizer（Domain Gate
  與語意節）＋GraduationGateVersion 3。頁大小與批次大小要用 serving
  真實 context 上限與 `-EvidenceStats` 分布反推——這是 L104/L105 之後
  最大的一次重構，必須同一 commit 上線（拆開會留下批次各自遞增輪次、
  旗標被批 1 翻掉的中間態）。
- 原則：**context 溢出先分端再修**——子代理端修「單一委派的輸入上限」，
  parent 端修「一輪工作的分批與收據」，環境端修「宣告與真值對齊」；
  三端症狀同名、藥完全不同，先量再開藥。其次：**反 cherry-pick 的
  選擇權必須在確定性層**——誰選 claim、誰切範圍，不能是被審的那個
  模型（L103 根因四的同型）。
- 套用：本 commit（ps-auditor 頁數上限、progressive-source-retrieval
  §5.1、auto-loop FailureKind、lint -EvidenceStats；測試情境 25＋
  EvidenceStats 斷言）；D0 診斷清單交管理者（SOP-10 探針、停機圈
  rc/err/log 指紋、opencode export 委派形狀、-EvidenceStats 分布）。

### L107 稽核也要 bounded——manifest／收據／外環合併（2026-09）
- D0 數據（管理者實測）：serving 真值拿不到；停機圈 rc=1；
  `-EvidenceStats` 67 檔、最大 37 列、p95=26、中位 12、總計 845；
  出事委派＝單一 NN 檔純任務 A。結論：**單檔 37 列的任務 A 就爆**
  ——分頁是必要不是選配；rc=1 分不清 parent 是否也爆 → 兩端一起上。
- 設計（用現有零件拼，issue #22 的必要實作取其骨、去其重）：
  1. **外環凍結本輪工作**：`audit-manifest.txt`（模型唯讀）——目標
     輪次、旗標、本批檔案、每檔 Evidence 列數（來自 lint 唯一實作）、
     範圍切段（頁大小自校準：預設 10＜中位，溢出對半、最小 3）、
     任務 B claims（**確定性 regex 抽**，反 cherry-pick 的選擇權不在
     被審的模型手上；seed 抽法不可行——parent／auditor 都 bash:false）。
  2. **每 session K 檔、只寫 audit-parts/part-<i>.md**（新 agent
     ps-audit-orchestrator：四 MCP 全 deny、只委派、只抄表；新指令
     ps-audit-batch）；批次 0 做任務 C（含 Domain Gate 三分）與 wiki
     抽驗寫 domain.md。
  3. **part 不變量 → 檔級收據**（audit-ledger.json）：合計＝列數、
     範圍覆蓋、明細「內容」欄 id ⊆ 該檔附錄（捏造判定抓得到）、
     非 PASS 必有明細列、無洩漏標記。收據的是「parent 寫的數字通過
     不變量」，不是「子代理真跑過」——與現況風險同，不新增。
  4. **收據齊備才合併**：外環寫 90-audit.md（六章節就位、記分卡逐檔
     ＋合計、明細串接、完整性承接 domain.md、語意節誠實佔位）、機械產
     A 列（一檔一行、PENDING_MANUAL 不開單）、D 列（僅 DOMAIN_ROOT，
     治理層裁決重複）、遞增輪次、翻旗標——L105 的「durable state 搬移
     交確定性層」延伸到 A 列／輪次／旗標。中途崩潰：輪次未遞增、
     收據持久、下圈只補缺檔；檔案內容變 → 該檔收據作廢。
  5. **溢出＝容量事件**：FailureKind 標籤；零收據批 → K 對半；單檔
     溢出 → 頁對半；到底或 attempts≥2 → BLOCKED，記分卡標「未稽核」
     → lint（tier 1／2）判違規不得畢業——未稽核≠已稽核，不能因檔名
     被提及就過覆蓋門。出路＝拆續篇縮小單檔證據。
  6. 門：GraduationGateVersion 2→3（舊收據作廢）；auditStall 對「輪次
     進行中且本圈有收據」不計零回灌；-AuditBatchesPerCycle 時間圍欄。
- 與 issue #22 的取捨：不做 journal／.tmp 全套 transaction（兩寫之間
  唯一中間態由既有守衛收斂）；不做 taskId／claimId 級 receipt（檔級
  ＋範圍級已能證明每筆落位）；不做 finalizer session（Domain Gate 由
  批次 0 的 session 做、其餘語意節誠實佔位——少一個會壞的 writer）；
  不 binary split（任務 A 已是一檔一委派）。人工 /ps-audit 全量路徑
  保留給互動使用。
- 原則：**「規模再大也照樣執行」是反拒答條款，不是容量保證**——
  fresh session 只清歷史，不讓本輪工作 bounded。任何「一個 session
  做完整輪」的契約，在 corpus 成長下必然撞牆；bounded 的單位要由
  確定性層切、收據要由確定性層驗、合併要由確定性層寫。
- 套用：本 commit（auto-loop 分批稽核塊：manifest／台帳／Test-AuditPart
  ／Invoke-AuditMerge／Invoke-AuditRound；ps-audit-orchestrator 與
  ps-audit-batch 新檔；auditor 範圍委派規則；lint 未稽核違規；
  ps-graduation gate v3；audit-template 註記；測試情境 26＋lint
  -StrictAudit 未稽核斷言，63 判定全 PASS）。

### L108 交接規格不是文件，是契約——Legacy Contract 產物線落地（issue #17，2026-09）
- 症狀：#16／#17 要求「新系統開發者可直接實作」的 Behavior＋Persistence
  規格，而現況產物鏈（subagent JSON → 模型手寫 NN markdown → wiki）只有
  「畫面與欄位」五欄表、「行為邏輯」散文、「資料流」四欄表；stable ID、
  storageKind、鍵、生效日規則、read／write 語意、存取策略、五維驗證狀態
  **全無前身**，Markdown 就是唯一真相、模型改一句話就漂移。
- 根因：規格若由模型直寫成散文，就沒有可算的分母、沒有可驗的不變量、
  沒有跨階段（#18～#21）能引用的身分。L107 的教訓在稽核側已證明：
  bounded 的單位由確定性層切、收據由確定性層驗、合併由確定性層寫——
  規格產物線也必須是同一形狀，只是「part 檔」換成「fragment」。
- 落點（切片 1，全部以現有零件形狀拼）：
  1. **模型只寫固定表格 fragment**（`contract-parts/screen-<COMPONENT>.md`、
     分頁檔 `screen-<COMPONENT>-p<k>.md`、`entity-<RECNAME>.md`，每檔 ≤150 行；
     章節標題與表頭逐字、欄位值只准 `legacy-contract-vocabulary.md` 的封閉值域、
     證據只准 `E<nn>.<n>`（nn＝來源 NN 前兩碼、n＝附錄列）／`SQL:<n>`（本檔查詢表
     列）／`UNRESOLVED`）。不寫 JSON（write 工具的 JSON 逃逸與截斷是永久條件；
     audit part 的固定表格已在分批稽核實跑中可寫）、不寫 ID、不寫 spec、不自估
     容量（沒有「續」列——分頁由外環切）。
  2. **外環（`ps-contract.ps1`＋`ps-contract-lib.ps1`）**：`-Plan` 從 NN 檔確定性
     抽事實（Component、欄位列、行為邏輯分類、資料流、權限節、附錄型別）寫
     manifest（控制項依頁大小分頁，預設 30）；`-Accept` 驗不變量發 fragment 級
     收據（INVALID 兩次即 BLOCKED；>150 行＝容量事件→該 Component 頁大小對半重切、
     不記 attempts；NN 內容變即作廢）；`-Merge` 派發 stable ID（`SCR.／CTL.／STA.／
     INT.／VAL.／NAV.／BOP.／EFF.／ENT.／FLD.／WRT.／RDS.／RQ.`＋自然鍵，列序無關）、
     解引用證據、算五維、套 approvals、蓋 verify 收據（只在 currentSchema 已知時採信；
     oracleRead 只認 SQL hash 相符的 RQ 單位收據）→ canonical `legacy-contract.json`；
     `-Render` 由磁碟 JSON 產 18 節 developer-facing `spec/*.spec.md`；`-Gate`
     算 G1～G18（分母＝NN 抽取）→ tier 1（結構類 gate 無 FAIL）／tier 2（全
     PASS 且零 UNRESOLVED debt），獨立 `contract-receipt.json`，不動畢業門。
  3. **未知與不適用是值，不是空白**：每個值域都有 `UNRESOLVED`／
     `NOT_APPLICABLE` 出口；`DIRECT_DB_WRITE_APPROVED` 模型永遠不准填，只能
     由人填 `approvals.md` 升格；Oracle 驗證是「SQL 收據」（cookbook §7，
     樣板全標待公司機驗證），通道未掛＝NOT_RUN 不是 FAIL。
  4. 兩個陷阱當場踩到並寫進測試：PowerShell 對字典 `$d.Keys` 會被名為
     `keys` 的鍵劫持（entity 的鍵欄位整個序列化成垃圾）→ 序列化改走
     GetEnumerator、欄位改名 recordKeys；render parity 若拿記憶體物件比
     磁碟 JSON 會假 FAIL → Render／Gate 一律回讀磁碟 JSON（獨立 -Gate 吃到比
     contract-parts 舊的 JSON 只印 GATE_WARN，重合併走 -All）。
  5. 對抗審查（3 視角 ×12 驗證者）確認 7 條設計缺口，全部落地並寫進測試：
     `E<n>` 無法跨來源→`E<nn>.<n>`；模型自估「（續）」→外環分頁＋容量事件；
     列序派 ID→自然鍵；模型可自填 EXECUTED→參考查詢只准 PENDING、oracleRead
     只認外環 hash 相符的 RQ 收據；FILL_ME 下收據被採信→currentSchema 守衛
     （-VerifyPlan exit 2）；一實體一委派塞爆→OBJ／FLD-a-b／RQ-n 單位收據；
     culture 排序跨機不一致→Ordinal。
- 原則：**規格的每一格都要有分母、有值域、有證據、有身分**——分母來自
  確定性抽取（不是模型說有多少）、值域封閉（自由 token＝INVALID）、證據
  指回已稽核的 NN 附錄（不重抄 ChunkId）、身分由外環派發（自然鍵決定，
  列序無關）。做不到四者之一的欄位，就不該進 contract，該留在 NN 散文。
- 有意不做（切片 2）：外環駕駛 loop（先人工 `opencode run --command
  ps-contract-batch` 實測 fragment 可寫性）、metadata 分母（PSPNLFIELD 全頁
  盤點）、修改 ps-auto-loop／ps-deep-research／lint。
- 套用：本 commit（vocabulary／fragments 兩份契約；ps-contract-batch／
  ps-contract-verify 兩指令；ps-contract.ps1＋lib；cookbook §7；
  scripts/tests/test-contract.ps1 情境 K1～K10 共 102 判定全 PASS；SOP-18；
  設計備忘 docs/design/legacy-contract-phase1-decision-memo.md）。
